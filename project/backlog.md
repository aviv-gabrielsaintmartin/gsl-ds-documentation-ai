# Backlog

_Everything that isn't the current task. Written for Gabriel to read — open it
whenever you want the whole picture. `status.md` holds only what's active._

_Nothing here is started without approval. Updated 16 September 2026._

---

## Tasks — things I can do


### The app demo — iOS and Android, the priority since 24 Sep

Gabriel, 24 Sep: people want generated interfaces. A design skill writes a
neutral spec; the iOS skill `/prototype` builds it.

Gabriel, 25 Sep: **the finish line is the app demo, on iOS and Android.** It is
shown to the CPO next week. The Figma milestone is parked. Android builds through
`/vibe`, which does not read a spec yet.

Gabriel, 25 Sep, on the CPO demo:

- **Recorded, never live.** A build takes too long, and could fail in the room.
- **The iOS workflow comes first.** The plan is defined only once it works.
- **The plan shows deadlines, and follows the product design cycle.**
- **Results matter most.** The plan ends at a screen in production.
- **Production needs steps this repo does not cover yet.** He named three: a
  developer's code review, better developer skills, and an AI agent that makes
  the code work as intended, at SeLoger's quality bar.

Gabriel, 25 Sep, on the plan and the demo's input:

- **The project translates the company product playbook into an AI-enhanced
  process.** Interface creation comes first. Better input, from new idea to
  discovery, comes later. Baby steps.
- **Design UX and UI sit in Delivery**, as stakeholders see it. The playbook
  never names design.
- **The playbook's Definition gate ends in a PRD.** So the demo's brief is a
  PRD. It is easier to present to the CPO, and follows the playbook's script.
- **Less complexity in the process.** Straight to the point.
- **Discovery and Verification being empty is fine.**
- **This reverses a 24 Sep call.** `project/decisions.md` deferred the PRD format;
  the design skill's questions stood in for it. Needs a new entry there.

Gabriel, 25 Sep, on the demo's PRD:

- **PRDs are not unified at the company.** No deep work on a PRD format.
- **A PRD does not describe content or flow.** Agreed.
- **The demo PRD is real:** *[WEB] Add block to Neighborhood section* — a
  schools block on the classified detail page, behind an A/B test. A designer
  already designed it in Figma. It still needs building on web and Android.
- **The idea:** run it through the design skill, then compare with the
  designer's work.
- **Confluence is out of reach for now.** This Claude account is personal; only
  the work account can read it. Gabriel pastes PRDs by hand.
- **Proved from the iOS code:** the classified page has a location section,
  `aviv_seeker_classified_ios/Sources/AvivClassifiedLocation/View/LocationSectionView.swift`.
  **Guessing** it is the app's version of the web's Neighborhood section.
- **Checked 25 Sep: neither app has the schools block.** Proved by searching
  `origin/develop` of both repos, fetched that day — code, strings and branch
  names — for school, scolaire and education. Nothing on the classified page.
- **Both apps already have the fallback button.** Android: `Request precise
  address`, in the classified-detail strings. iOS: `Demander l'adresse exacte`.
- **So the demo builds on iOS**, the one platform with a working build path.
- **Local repos are behind.** `gsl-ios` sits 333 commits behind `develop`, on a
  prototype branch. `gsl-android` sits 10 behind, on a product-coding branch.

| Task | Status |
| --- | --- |
| **Have an iOS engineer confirm the five `guessing` rows** in `components-ios-map.md` — `Floating Button Group`, `Loading State`, `Navigation Bar (App)`, `Date Picker`, `Top Bar` | proposed · needs a person on the iOS team |
| ~~**Give the design skill the colour guidance it needs to pick a token**~~ — **Done 24 Sep 2026.** Five colour family pages in `references/`, cut before their value tables | done |
| **Should a generating agent in this repo also read the colour family pages?** `CLAUDE.md` says a generating agent reads `-rules-ai` files and never the token pages. But `color-rules-ai.md` picks only the family, and the token inside it is chosen on `content.md`, `surface.md` and the rest. The design skill now reads them. **Recommendation:** say in `CLAUDE.md` that the five family pages count as rules for this purpose, and `-tokens` pages still do not. Your sentence. Found 24 Sep | question · needs your decision |
| **Shadow tokens are not in slash form** — `spec-rules-ai.md` says every token is written in slash form, but `shadow-rules-ai.md` names its tiers `none`, `4`, `8`, `16`. A spec agent must guess the form. **Fix:** one line in `spec-rules-ai.md` saying shadow is written as its tier. Found 24 Sep | proposed · minutes |
| ~~**Run `/design` for real, once**~~ — **Done 24 Sep 2026, by Gabriel: `spec-002`, a welcome banner.** The design side held; the build did not. Findings in the three rows below | done |
| **Should the design skill get a proposal step, where the person validates the flow and settles ambiguous words?** Raised by Gabriel 24 Sep, after `spec-002`. **Proved:** `components-rules-ai.md` turns an intent into a component; nothing turns the person's words into an intent. In `spec-002`, *banner* was read silently as *not blocking*, and the rules then correctly gave `Card`. **Proposal on the table, not decided:** step 1 problem (as now) → step 2 proposal: their words → the need → the component, with one plain question per ambiguous word, drawn from the rules' *Otherwise* columns (*does it block the screen?*), then the person validates the flow → step 3 build. **Paused by Gabriel: too fast to follow.** Take it slowly, one step at a time | question · paused · Gabriel, 24 Sep |
| **The design skill built a card where a component exists** — Gabriel, first real run, 24 Sep. `spec-002` placed a `Card` and declared its content as an invention. **Proved from the spec:** it ruled out `Pop-up`, `Modal Bottom Sheet` and `Alert` because *the source asks for a banner* — the person's word, not the need. The need is a short greeting with an illustration and a close control, on arrival; **Overlaying content** in `components-rules-ai.md` sends that to `Pop-up` (apps only) or `Modal bottom sheet`. **Guessing:** `Modal bottom sheet` is the rules' answer, because `Pop-up` has no doc yet. **The background** was `Card` · `Color: Primary light`, chosen *to feel warm* — an open assumption, not a rule. **Fix:** in `SKILL.md`, a solution word in the source — banner, pop-up, dropdown — is a wish and never a reason to rule a component out; before any `composed` element, name the component the rules give for the need, and rule it out by the need only | **fixed in `SKILL.md` 24 Sep** · not tested until the next real run |
| **The build put the banner over the whole app, not on the home screen** — found in the first real run, 24 Sep. **Proved from the code:** `/prototype` added it as an overlay on the app's root view in `SeLogerApp.swift`, so it floats over the splash screen and the cookie consent, and is greyed by the consent's dimming. **Proved from the spec:** it says *top of the home screen, above its existing content*. **Fix, our side:** the design skill's hand-off tells `/prototype` to place each block inside the screen the spec names, never over the whole app. **Fix, Aviv's side:** the row *Teach `/prototype` to take a spec*. **Not tested:** how the banner looks on the home screen — no screenshot reached it | **fixed in `SKILL.md` 24 Sep** · not tested until the next real run |
| **A spec cannot say where a block goes on an existing screen** — `spec-002` could only write it as a loose sentence under the block heading. `spec-rules-ai.md` has no field for it, so a build agent reads it as a hint. **Fix:** a *Placement* item on each block's line — the screen, and what it sits above or below. Found 24 Sep | proposed |
| **The design system's illustrations are not documented** — the spec had no illustration to name, so it left a placeholder; `/prototype` then picked an iOS illustration that rendered as a broken-image cross. **Proved:** `references/` holds no illustration inventory, and the iOS code imports an illustration library. **Needs:** an illustration index and rules, as icons have. Found 24 Sep | proposed · needs a source for the illustrations |
| ~~**Write the design skill**~~ — **Done 24 Sep 2026.** `skills/design/`, linked into `~/.claude/skills/` | done |
| **Teach `/prototype` to take a spec**, reading `components-ios-map.md` for names. It lives in `~/.claude/skills/ios-prototyping/`, outside this repo, owned by Aviv | proposed · unblocked 24 Sep |
| **iOS has two values GSL does not, and lacks eleven GSL has** — found 24 Sep, building `tokens-ios-map.md`. iOS-only: spacing `148`, radius `2`. GSL-only: `Spacing/64`, `128`, `256`, `Radius/Rounded`, the three border widths, the eight underlined text styles, `Border/Focus`. **Not checked:** whether iOS components hold these values themselves. A spec naming one of them cannot be built by name on iOS | proposed · needs an iOS engineer to say which side is right |
| **Say that `tokens/scripts/` now reads two code repos** — `tokens/README.md` says *four Python scripts* that read *the design-system code repo*; `.claude/rules/token-docs.md` says *four read-only extractors* reading `gsl-core-web-design-system`. There are five, and the new one reads `gsl-ios`. Sentences you own. Found 24 Sep | proposed · minutes |
| **Pick one colour notation in `color-rules-ai.md`** — it writes `content.light.default` and `Content/Light/Default` side by side. The spec format now requires the slash form. Found 24 Sep | proposed |
| **Document `Donut chart`'s variants** — its *Variants & Modifiers* says `Not documented`, so a spec cannot name its legend or its segment count. Found 24 Sep, writing `spec-001` | proposed |
| **Fix `card.md`'s radius column `8ps` to `8px`** — a spec copies variant values from that column. Found 24 Sep | proposed · minutes |
| ~~**Tell the people pages the design skill exists**~~ — **Done 24 Sep 2026.** `skills/README.md`, `README.md`, `scripts/README.md`, `CLAUDE.md`, and the prototype page rewritten | done |
| **Answered 24 Sep: specs are saved in `~/gsl-specs/`, outside every repo, so a spec outlives its prototype and one spec builds on every platform.** Gabriel chose it over a folder per platform repo, which `/prototype`'s cleanup would delete with the branch. `spec-rules-ai.md`'s *Where specs live* and the `spec-001` numbering clash still need fixing — the sub-task above | answered · Gabriel, 24 Sep |
| **Rewrite the links in the design skill's `references/`, rather than cutting them** — the generator keeps a link's text and drops its target when the target is not copied. An agent loses the pointer, for example from a ruleset to a colour family page. Found 24 Sep | proposed |
| **Publish the design skill to the company skill marketplace** — **not tested** whether it accepts a skill with a `references/` folder. Gabriel, 24 Sep | proposed · after a first real run |
| **A build skill that takes a spec, for Android, web and Figma** — `/design` stops at the spec on all three. Each needs a build skill and a name map, as iOS has. Figma may need the rules bundled in one file, as `skills/gsl-color-rules/` was. Gabriel, 24 Sep | proposed · Android waits, per 24 Sep |
| ~~**Find real PRD examples**~~ — **Found 25 Sep:** *[WEB] Add block to Neighborhood section*, pasted by Gabriel. No format work: PRDs are not unified | done |
| **Test 1, 25 Sep: the schools PRD, built on iOS from `~/gsl-specs/spec-004.md`.** Compared with a designer's version made from the same PRD. **Same structure:** a title, a tag, a paragraph. **Different content:** the designer had facts the PRD lacks — "Écoles publiques de secteur", a `Beta` tag, and the rollout status. **Different container:** the designer placed it flat, like the section's other rows; the AI put it in a card. Proved from the two screenshots | logged |
| **Content belongs in the PRD** — Gabriel, 25 Sep. The designer knew things the PRD never said. A PRD should carry the content: what the block says and why. It should not name icons, colours or layout — those are design-agent decisions. **Gabriel, 25 Sep, on Task B:** nor the component or container either — not "one row per school", not "a modal". Name what each item holds and what a control is for, such as "a CTA to see more schools". This is the "better input" work, before the brief | proposed · input |
| ~~**The demo prompt should not mention icons or tokens**~~ — **Done 25 Sep 2026, in `~/gsl-specs/prd-schools.md`.** — Gabriel, 25 Sep. Test 1's prompt asked for "design-system icons and tokens". The design agent decides those alone | done |
| **A new block in an existing section should follow that section's pattern** — Test 1 used a card; the designer followed the flat rows around it. **Guessing:** no rule tells the design skill to read the surrounding section first | proposed |
| **The build shows a white box inside a grey card, and "En savoir plus" falls outside it** — Test 1. The spec put `Cell Content` inside a `Light` card, and named no background for it. **Proved from the screenshot. Not tested** whether the spec or the build is at fault | proposed |
| ~~**The design skill cannot name an icon**~~ — **Closed by Task A, 25 Sep 2026.** — the icon index is not in `skills/design/references/`, so `icons-rules-ai.md` forbids every name. Proved from `spec-004`'s assumptions | done |
| ~~**No rule says which tag means "new" or "beta"**~~ — **Closed by Task A, 25 Sep 2026.** — Test 1 picked the default tag with "Nouveau"; the designer used a dark `Beta` tag. Proved from `spec-004` and the screenshot | done |
| ~~**iOS icon names follow one rule, with a short list of exceptions**~~ — **Closed by Task A, 25 Sep 2026.** — checked 25 Sep against `develop`'s `AssetIcons.swift`. `name_<icon>_filled_on/off` turned into kebab-case matches **416** index names. About 45 differ either way; some are parsing noise, and real ones include `dishwasher`, `RDC`, `file-cdd` and the social icons. **Proposed:** the naming rule plus an exception list, not a full map. Android not checked | done |
| ~~**The tag's variant flow chooses emphasis by surface only**~~ — **Closed by Task A, 25 Sep 2026.** — Gabriel, 25 Sep: meaning works for status tags, but Dark versus Light versus Primary has no rule beyond the surface. **Proposed:** choose by what the label says (its role), then let the surface only swap for contrast. Roles and variants need his ruling | done |
| ~~**Task A fix — two defects in `tag.md`**~~ — **Done 25 Sep 2026.** The Variants pointer now names both places the role table lives, and **Hierarchy** sits below the style DO captions | done |
| ~~**Task A — fix the rules the schools test exposed**~~ — **Done 25 Sep 2026.** `icons-index.md` and a new `icons/icons-ios-map.md` are in the design skill's `references/`. 374 of 401 iOS icon paths follow one rule; the rest are listed. `tag.md` and `components-rules-ai.md` choose a tag's style by role. `check-all.py`: six pass, and the seventh fails only on the five logged disagreements | done |
| **Task B — the schools PRD with content, then Test 2.** Add the designer's title, `Beta` tag label, explanation and the full disclaimer, both sentences, to the PRD. No icons, colours or layout. Drop "icons and tokens" from the prompt. Gabriel runs it in `~/gsl-ios`. **Gabriel, 25 Sep: the school list goes in the PRD for Test 2**, with its rows' content — name, type, distance, travel times — and the modal's three paragraphs, from his two screenshots. **PRD written 25 Sep: `~/gsl-specs/prd-schools.md`.** Private schools now in scope; no component, container or layout named | PRD written · **Test 2 is next** · Gabriel runs it |
| **The PRD's four travel modes are read from icons, not from text** — walking, cycling, car, public transport. The designer's screens show icons only. **Guessing:** the last icon, a bus or tram, means public transport. Found during Task B, 25 Sep | note · confirm with the designer if Test 2 differs |
| **Five iOS icon paths draw the wrong picture** — found during Task A, 25 Sep, on `gsl-ios` `origin/develop` `ccb15356ed`. `location-arrow` draws `user`. `grip-dots-vertical` draws `grid`. `bicycle` and `biking` both draw `route`. `pen` filled draws `pen-line`. **Proved** by reading each line in `IconsDescription+<Category>.swift`. **Wrong screen, nothing tells you.** `icons/icons-ios-map.md` now tells an iOS agent to treat them as missing. The fix is in the iOS code, for the iOS team. **Also:** 58 index names have no public iOS path, listed in the same file | for the iOS team |
| **One DO caption in `tag.md` contradicts the new role rule** — found during Task A, 25 Sep. It says *"Use tags with different emphasis to indicate the level of importance."* The role rule says style comes from role, never from loudness. It is your sentence, from the imported doc. **Recommendation:** cut it, or rewrite it to point at **Style by role** | your call |
| **`icons-rules-ai.md` still says fifteen names break the convention and three carry a trailing space** — found by `/task-check`, 25 Sep. `icons-index.md` says twelve, and says the three trailing spaces were fixed on 14 Sep. Both files now ship together in the design skill, so an agent reads the contradiction. It also names `assistance ` with a trailing space, which no longer exists. **Proposed:** correct the ruleset to twelve and drop the trailing-space sentence | proposed · minutes |
| **How the apps use tags today — checked 25 Sep on `origin/develop` of both repos.** Same nine styles on iOS (`DSTag.swift`) and Android (`Tags.kt`). **New listing:** `Primary` strong with a fire icon, on both apps. **Beta feature (AI search):** `Information` weak, iOS — the designer's schools screen uses `Dark` instead. **Gabriel, 25 Sep: "Beta" uses `Information` weak.** **Seen or contacted listing:** `Light` with an icon, Android. **Neutral attributes** (household info in chat): `Subdued`, both. **System state** (`Epuisé`, expiry, statistics): status colours. **Listing tags from the server:** the backend picks the style; Android falls back to `Dark`. **Not found:** a commercial highlight such as "Exclusivité" set in app code. Found by reading code, not by opening the apps | logged · feeds the tag rule |
| **The designer's legal disclaimer, for the PRD** — from Gabriel's screenshot, 25 Sep: "Informations fournies à titre purement indicatif. L'établissement public de secteur est affiché en priorité, suivi des établissements les plus proches." Then an inline "Learn More" link, and "Source : data.education.gouv.fr" with an external-link icon. Its second sentence describes a school list, which is out of scope. **Gabriel, 25 Sep: keep it in the PRD** — **done 25 Sep, in `~/gsl-specs/prd-schools.md`** | done |
| **Make the design skill take a PRD as its input, with no questions** — Gabriel, 25 Sep: with a PRD as the brief, questions add nothing. Each gap becomes a written assumption in the spec instead. If questions come back, they are a later improvement to one step of the process. Replaces: its questions shrink to the gaps the PRD leaves. Waits on real PRD examples. **Known gap, proved from `project/decisions.md`:** a PRD does not list content, so the skill still chooses it with no rules | proposed · after the examples |
| **Define the PRD format, and write the PM skill** — moved down 24 Sep: the design skill asks the problem questions and writes them into the spec, so no PRD is needed for the demo. **Comes back only if** those questions prove too heavy, or a PM wants a problem document with no spec | deferred · Gabriel, 24 Sep |

### How a screen gets designed

Designing a screen is a sequence, and each step needs the ones before it. So a
task's worth is how early the step it repairs sits: repair an early step and
everything after it becomes possible, repair the last one and nothing else
changes.

|     | Step                                                         |
| --- | ------------------------------------------------------------ |
|     | Define the content — from the user need                      |
|     | Define the components to use, and which variant, in one move |
|     | Define what needs to be built                                |
|     | Choose the tokens — colour, text style, spacing              |
|     | Put them on the screen                                       |
|     | Place them according to the design guidance                  |
|     | Check the content                                            |

**The steps are the same for anyone.** A person and an agent take them in
different orders, and an agent may take several at once, but neither gets a
sequence of its own. What differs is the *answer* at a step, never the step.

**The sequence is unfinished, on purpose.** A flag seen three times and ruled a
ruleset gap is how a missing step gets found, so adding one is expected rather
than a correction.

**A row names the step that fails, not the kind of file being fixed.** Grid
lives in `tokens/`, so a grid task looks like it belongs under *choose the
tokens*. What breaks without it is knowing your margins, which is *place them
according to the design guidance*. Sort by the symptom, not by the folder —
without that, every token task drifts into the token step and the order stops
meaning anything.

**Due by September** decides between two rows at the same step. An agent
designing in Figma can place any component in the libraries, and whether that
component is also built on web, iOS or Android changes nothing about the screen
it draws. Rows that are not due are real and they stay; they sit below the ones
that are.

Then two tie-breakers, and neither is ever the sort:

- **How bad it is when it goes wrong.** Worst is a wrong screen with nothing to
  tell you — the agent obeyed every rule we wrote and the output still isn't
  compliant, so the only way you find out is opening the file and looking.
  Better is the agent stopping to ask you: you had to step in, but nothing wrong
  shipped.
- **How often it happens.** A failure on every listing beats one on a rare
  screen.

**Fear the silent ones.** An agent that stops tells you it stopped. One that is
confidently wrong doesn't.

**Effort is never a reason.** A cheap tidy-up is not a task however cheap it is,
and a hard task is not demoted for being hard.

**Nothing here is started without your go**, and being top of the list is never
authorisation to start.

### What's broken
| **Decide two `energy-tag` hero images** — the source replaced the picture under the summary rather than adding one. Swap, or keep what is there. Minutes, but it is a judgement |
| **Add the colour-accessibility rule to `kpi`, or decide not to** — the Zeroheight page carries a do/don't saying *"Don't rely on colour alone to give meaning"*, with two images. **None of that text is in `kpi.md`.** It is a content gap, not a missing image |

The next task is the **earliest step's topmost row that is due by September and
has an empty *Blocked by***. If no step has one, look in *Scoring a finished
screen*, then *Keeping the repo usable*.

Two steps have no rows at all. That is not evidence they are sound — it is that
nothing has ever looked at them. Their headings stay so the gap stays visible.

#### Define the content

**Nothing yet.** The scorecard cannot see copy or tone — its own blind-spot
table says so — and `Content & UX Writing` is empty in 15 of 57 component docs.
Nothing is broken here because nothing has been built here.

**One row now exists, and it is not due by September.** It came out of the input-agent
assessment on 16 Sep, in the note at the bottom of this file.

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Build the input agent — it interviews the user, then writes the spec | no | **The agent stops and asks you — or rather, nobody asks anything.** A run starts from a brief a person wrote by hand, so the step *Define the content* has no procedure and no record of how the content was decided. `brief-001.md` is one person's judgement, written once, unrepeatable. **What it would be:** an agent that asks a bounded set of questions, five to eight, then writes the spec the two rows in *Scoring a finished screen* define. The questions are already drafted — `project/design-process-map.md` holds 36 of them across Discovery, Definition and Solutioning. **Why it waits:** the interview adds a front door, never accuracy, and it adds human variance exactly where measurement wants none. **For a measured run there is no interview** — a fixed spec is fed in, and the interview mode is for real design work. **Do the spec and the prompt template first.** An interview with no spec format to write into has nowhere to put its answers. Raised by Gabriel 16 Sep **Comes up:** Every real design, once designers use this rather than only runs | ~~The spec format existing~~ — exists since 24 Sep. Overlaps the design skill, which asks up to nine problem questions since 24 Sep | Unknown |

#### Define the components to use

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| **Two eval rows failed because the obligation sits beside the row, not in it** — *found by eval run 4, 23 Sep 2026; corrected 24 Sep* | yes | **Wrong screen, nothing tells you.** Rows 41 and 42 require more than a component name — `Feedback thumb buttons` and `Score tag` must be **reported as selected**. **The cold agent named the right component in both and stopped.** The obligation is in the ruleset — lines 312, 367 and 368, *'Select it. Report that you did'* — but in the separate *Built outside the design system* section, never in the row's own cells. An agent reading **Which component** as the flat decision table the ruleset says it is gets the component and misses the duty. **Recommendation: put the obligation in the row** — the same words, in the `Choose` cell of both. **Row 29 was filed here on 23 Sep and does not belong.** It is a different fault: *'Figma-ready, web in progress'* — the answer it expects — appears **nowhere** in the ruleset, so the eval asked for a fact the file cannot supply. **Gabriel's decision of 24 Sep settles where that fact should live** — in each component doc's readiness table, not the ruleset — which means a run that reads only the ruleset **cannot** be asked about availability at all. Row 29 has to be rewritten or moved out of this eval; see the readiness row below **Comes up:** Every run that selects an externally-built component |  | An hour |
| **`Info state` and `Loading state` both claim full-area loading, and nothing decides between them** — *found by eval run 4, 23 Sep 2026* | yes | **Wrong screen, nothing tells you.** The cold agent found this unprompted and quoted both sides. `Info state`'s own row lists its cases as *'Full-area states — empty, error, success, **loading**'*. `Loading state`'s Otherwise says *'The area is empty, failed or succeeded **rather than waiting** → Info state'*. **So `Info state` claims loading, and `Loading state` exists for exactly that case.** The file gives a pointer in one direction only — Loading state to Info state when *not* waiting — and **nothing resolves which one owns full-area waiting.** **This is the fault class the eval exists to find and no script can see:** `check-rules-docs.py` compares a doc to a rule, never a rule to another rule, and `check-rules-reach.py` only asks whether a rule exists. Both rows exist and both are reachable. **Recommendation: `Loading state` owns waiting, `Info state` loses *loading* from its list.** A dedicated component beats a slot in a list of four, and `Loading state`'s description — *'a spinner with an optional title and description'* — is the more specific of the two. **Your call, because it changes what an agent places** **Comes up:** Every screen that fetches anything |  | An hour, once you have ruled |
| **`Pop-up` and `Alert` both say they are for 'a confirmation', and eval row 54 cannot tell them apart** — *found while scoring eval run 4, 23 Sep 2026* | yes | **Wrong screen, nothing tells you.** `Pop-up`'s row: *'A **small** amount of blocking content — a short confirmation, a brief message'*. `Alert`'s row: *'**Alerting the user and expecting a decision back** — an answer, a confirmation, a choice'*. **Both claim confirmations, and both are available in apps.** **How it surfaced:** eval row 54 asks for *'Confirm before deleting a listing, blocking, in the iOS app'* and expects `Pop-up`. The cold agent answered **`Alert`**, quoting Alert's own row. **The agent is defensible and the intent is the defective part** — 'confirm before deleting' is word-for-word Alert's stated case, so the intent never isolated `Pop-up` at all. Scored as a miss by the letter, and it is not the agent's fault. **Two things need fixing, and they are separate.** The ruleset needs a rule separating the two — size is the only candidate the file offers, and it is not stated as the test. **Row 54 needs rewriting** to an intent only `Pop-up` answers, which cannot be written until the rule exists. **This is the second contradiction run 4 produced**, and neither was catchable by any script **Comes up:** Every blocking overlay in an app | The rule separating them, which is your decision | An hour for the rule, ten minutes for the intent |
| Write the component descriptions into Figma, the way the colour ones were done | yes | **Wrong screen, nothing tells you.** The 227 colour variables carry descriptions since 15 Sep; **no component does.** So an agent placing a component in Figma has the library's structure and nothing else — no *when to use*, no *don't use for*, no variant guidance. **The source already exists:** `components/<name>/<name>.md` holds a usage doc for 75 components, 22 Sep 2026, and `components-rules-ai.md` holds the selection rules. This is a mapping job like the colour one, not a writing job. **Proved 15 Sep: `setComponentDescription` exists in `~/figma-cli/src/figma-client.js:5026` and supports Markdown**, so the write path is the same. **Read `skills/README.md` first** — the four traps that cost a day on colour all apply, especially writing by id rather than by name. **One reason to hesitate, recorded rather than hidden:** the 16 Sep test showed Figma's agent choosing components correctly **without** any description, so this may buy less than the colour work did. Worth scoping to the components that went wrong in a real run rather than all 75 **Comes up:** Every screen an agent builds in Figma |  | Two days, guessing |
| Work out what the icon variant axes are for, from where they are used | yes | **The agent stops and asks you — and this time you cannot answer it either.** Every icon carries `Filled`, and 55 carry `Circle` and 54 carry `Square`. **All three default to `Off`**, so an agent that places an icon and sets nothing gets the bare glyph — which is exactly what run-002 did, where the circled `info` was wanted. **Gabriel, 14 Sep: the axes were added over years to cover various cases, the reasoning is lost, and no variant is a bad choice by any rule that exists today.** So this is not a write-up — it is an investigation, and asking him to remember is not the method. **Derive it from real usage, the way spacing's *Start from real usage* section was built** — from actual bindings rather than stated intent. Two sources are already reachable: the four Figma libraries, and the web code repo that `tokens/scripts/` already reads. Anything with no consumer gets written down as having none, never guessed at. `Circle` and `Square` are the narrow ones and are where run-002's answer lives | | Two days, guessing |
| Fix thirteen icon names in Figma | no | **Only our own files change, until an agent misses a match.** Measured 14 Sep across all 454 names. **The three dangerous ones are already done** — you renamed `assistance `, `virtual-staging ` and `two-three-dimensional-draw ` by hand the same day. A trailing space is invisible in every rendering, so an agent matching `assistance` failed while appearing to have looked correctly. **Thirteen are left. Five use spaces instead of hyphens** — `eye slash`, `smart fill`, `smart edit`, `smart search`, `image ai`. **Seven use capitals** — `Save`, `One`, `Two`, `Three`, `RDC`, `RER-paris`, `location-Xmark`. **One is a typo** — `maginifying-glass-spark`, for "magnifying". **`light-bulb` and `lightbulb` are no longer on this list** — they were filed here as one icon under two spellings and they are two different icons, corrected 14 Sep. Retiring one is a separate question for you. **Both halves or neither:** rename in Figma, then re-run `figma-sync-icons` | **The Figma edit being made** | Twenty minutes for the sync, once it is |
| Put the variant property definitions into the four Figma registries — *parked by Gabriel, 23 Sep 2026* | no | **Wrong screen, nothing tells you.** The registries record a component's identity and a *count* of its variants — never what the variants are **called**. Measured 14 Sep: **0 of 98 entries** across components, patterns, experiences and foundations carry top-level property definitions. `Avatar` says `variantCount: 103` and nothing else; `Badge` says 28. An agent placing either has no local answer for *which property, which option*, so it must reach Figma or guess — and guessing produces a placed component with wrong variants and no error anywhere. **This is a gap against the skill's own spec, not a design choice:** `figma-sync-component-sets/SKILL.md` step 3 says capture *"All `componentPropertyDefinitions`, preserving exact string literals/typos"* and step 4 says write *"property definitions"* into the registry. **The inversion is the odd part:** all 33 Pattern 2 entries **do** carry `exposedSubComponents[].properties`, in exactly the right shape — `{"Type": {"type": "VARIANT", "options": ["Floating Button", …]}}`. So the registry can tell an agent how to configure `Action Menu`'s exposed `Button` slot and not how to configure `Button` itself. **Why it matters beyond tokens:** a session running off `~/.claude-personal/` has no Figma tools at all, so local data is the difference between answering and not answering. Raised by Gabriel 14 Sep after a test run's agent said it would read the registries *"rather than burn searches — they carry variant properties too"*, which is not true of any top-level entry. **Still 0 of 98 after the 16 Sep `Cell Content` sync** — that entry's properties went into its `status` prose, matching `Chip` and the other 60 entries, rather than into the structured field the skill's step 4 asks for.  **Re-measured 23 Sep 2026: still 0 of 98**, and the count has not moved since 14 Sep. **One further consequence, not recorded before:** nothing can check whether a doc's `Variant Selection Flow` covers every property the component actually has. The flows stand at 75 of 75 filled, and *filled* is the only thing known about them. **Gabriel, 23 Sep 2026: not chased before September.** The row stands and is no longer due. His reason: a model placing a component has the options in front of it, so matching *"a button with a left icon"* to the property that does it is work the model already does, and the mapping would be per-platform — which the tool-neutral rule keeps out of the usage docs anyway. **Not tested** that a model does this reliably; nobody has run it. **What is not parked** is the part no property name carries: the choices a brief never names, and limits such as one Primary per section. Those live in each doc's `Variant Selection Flow`, which is written and 75 of 75 filled. **The consequence he accepted:** nothing can check a flow against the properties a component actually has, so *filled* stays the only thing known about the flows until this row is picked up | **A session running off `~/.claude/`, with Figma Desktop open** | Unknown until one entry is re-synced and measured |
| Re-run the exposure walk on the 50 Components entries never verified against the live file | yes | **Wrong screen, nothing tells you.** Measured 16 Sep as **50 of 61**; **re-measured 18 Sep and the number and the definition disagreed.** `figma-components-registry.json` has **32 of 61** entries carrying neither `nodeId` nor `auditedDate`, **30 of them Pattern 1**. The 50 matches a different count — **49 entries lack `auditedDate`**, and 33 lack `nodeId`. The registry has not changed since `c62a7ae`, so this is a measuring difference, not work that happened. **`Button Bar` is one of the 32**, which is why its new doc, written 18 Sep, has no Variants section. The other three registries are clean — patterns 0, experiences 0, foundations 2. **This is not a theory.** `Cell Content` was one of them and its Pattern 1 was wrong: a walk on 16 Sep found `.placeholder left` exposed on all 33 variants, and Gabriel confirmed the old entry was simply wrong rather than out of date. **`Text Field` carries the identical sentence** — *"Reclassified (was Pattern 2) — live isExposedInstance trace found no nested exposed instances"* — written in the same sweep that produced the wrong one. **What a wrong Pattern 1 costs an agent:** it says the component has no configurable inner slot, so the agent never looks for one and ships the slot's default without knowing. On `Cell Content` that would have meant never choosing an image over an icon. **Why it is cheap:** one walk per entry, and the walk is already written | **A session running off `~/.claude/`, with Figma Desktop open and FigCli running** | Half a day |
| Run the component eval a fourth time, aimed at the count-the-parts test | yes | **Tells us whether what we wrote actually works.** The test that decides between **Highest tier first** and **Which component** — *two or more of the higher-tier component's capabilities means take the higher tier* — was **written at the end of eval run 3 as a fix, and has never been run against.** Run 3's 10/10 confirmed the *previous* fix, the precedence adjudication, not this one. **The specific doubt:** the threshold is exact and the thing counted is not. Nothing anywhere defines what one capability is, and the four worked examples cover only `Filter bar` and `Wizard`. Is `Phone Number Field` one capability, a phone input with a prefix, or two, text entry plus country selection? Two agents could split it differently and both quote the rule correctly. **And the test fires where it is weakest:** three collisions are named with their own stated reasons, so the count only runs on collisions nobody has named — exactly the ones with no worked example. **Run it on Sonnet, not Opus**, for the reason the other eval row gives: a stronger reader silently repairs ambiguity, which is the weakest version of a test meant to find it **Comes up:** Any time two rules match one build |  | Half a day |
| Check `Energy Tag`'s 48 Figma variants against the corrected energy mapping | yes | **Wrong screen, nothing tells you.** The French DPE class-to-colour mapping now in the colour ruleset is verified against web source code and has never been checked against Figma. `Energy Tag` is recorded with 48 variants and no property names, so nothing local can answer it. If Figma assigns classes differently from web, one of the two is wrong and a generating agent is following whichever we wrote down. Hits every French listing | **A session running off `~/.claude/`, with Figma Desktop open.** This one has no Figma tools | Half a day |
| Check whether `.Legend` is used loose anywhere in the Figma files | yes | **Wrong screen, nothing tells you.** The ruleset now forbids placing a dot-prefixed part. If a designer has already dropped `.Legend` beside a chart somewhere, that file disagrees with the rule, and an agent copying an existing screen would inherit the mistake. Unknown either way today | **A session running off `~/.claude/`, with Figma Desktop open.** This one has no Figma tools | An hour |
| Add a legend alignment property to `Donut chart`, and make its gap adjustable | yes | **The agent stops and asks you.** **Ruled a `library defect` by Gabriel on 14 Sep, from run-002.** The component lays its graph frame out horizontally, so the legend always sits beside the chart. The *below* placement its own documentation allows simply does not exist: `components/charts/legend.md` says *"Position the legend below or to the left of the graph"*, and the library can produce only one of the two. **This row used to ask for the opposite** — it wanted the side option *added*, on the grounds it was missing. It is not missing; it is the only one there. Corrected 14 Sep. **Two things are needed, not one.** An alignment property, so the legend can sit below. And an adjustable gap, because the fixed 56 is what forced run-002's builder into instance surgery at mobile width. Gabriel's range, 14 Sep: `Spacing/16` minimum so the legend never reads as sitting on the graph, `Spacing/48` maximum so it always reads as belonging to it, chosen by context within that. **Two halves:** the Figma edit is yours or the library owner's, then `figma-sync-component-sets` is re-run on the Patterns library so the new properties are recorded and the ruleset's *no alignment property* line comes out. Skipping the second half is how a Figma fix goes undocumented | **The Figma edit being made** | Half an hour for the sync, once it is |
| `Donut chart`'s `Data 4`, `Data 5`, `Label` and `Value` properties hide nothing | yes | **Wrong screen, nothing tells you — proved, 14 Sep.** Switching all four off on a live instance leaves all five arcs drawn and the centre `Total Value / 9,999.99 €` showing. The instance records the properties as `false`; the layers stay visible. So an agent asked for a three-part breakdown gets a five-part ring plus a total it never asked for, and the property panel tells it everything worked. Worked around by hand in the `Claude-to-Figma — SL` playground file — hiding the two unwanted arcs directly, then rewriting the remaining three arcs' `arcData` to close the ring — which is exactly the instance surgery a generating agent must never be doing. **Two halves, same as the legend-position row:** the Figma fix is yours or the library owner's, then `figma-sync-component-sets` is re-run on the Patterns library. **Seen a second time in run-002, 14 Sep**, by a different agent building the same two blocks — same silent failure, same hand-hiding of arcs to work around it | **The Figma fix being made** | Half an hour for the sync, once it is |
| Give GES its own class tag, or a CO2 scale option on `Energy Tag` | yes | **Wrong screen, nothing tells you.** A French listing carries two ratings — DPE on the energy ladder and GES on the CO2 ladder. `Energy Tag` covers only the first: its `Country` options are Germany, Austria, France and three Belgian regions, and all 48 variants are coloured from `Color/Scales/Energy/*`. The seven `Color/Scales/CO2/Blue100`–`Blue700` tokens exist and **no component consumes them** — the same shape of gap as the energy filter slider. An agent building a French listing detail page today either colours GES as though it were DPE, or invents its own; both are wrong and neither is flagged. Built by hand on 14 Sep to learn what it needs: seven steps, the same 33×24 arrow silhouette as `Energy Tag`, fill from the CO2 ladder, and the class letter flipping from white to black above `Blue300` or the light steps are unreadable. **Seen again in run-002, 14 Sep:** the building agent hand-built the seven-step CO2 ladder and declared it, and the scoring agent separately flagged `Energy tag` being used for the GES rating, which **Which component** restricts to energy-efficiency ratings | **The Figma build being made** | Half an hour for the sync, once it is |
| Generate the web availability list from the web code repo | no | **Wrong screen, nothing tells you — proven, not theorised.** The list built from the component docs was wrong for five of thirteen. `~/gsl-core-web-design-system` is ground truth: `libraries/ui/src/` and `libraries/patterns/`. The work is not the lookup but the name matching — Figma calls it `Bar graph`, the code calls it `barchart`; `Modal Bottom Sheet` maps to `Modal`; `Energy Tag` maps to neither `tag` nor `energyclassslider` cleanly. That matching is judgement, roughly 98 Figma names against 60 code directories **Comes up:** Every web screen |  | Half a day |
| Add the missing platform-readiness rows to three component docs | no | **The agent stops and asks you.** `date-picker`, `phone-number-field` and `tabs` have no `Figma \| Web \| iOS \| Android` row at all, and the other 52 do. **Less urgent than it was:** the ruleset no longer singles them out — it now says web, iOS and Android are unestablished for *every* component, so these three are no longer a special case. It matters again the moment web availability is answered properly, and all three are in fact live on web **Comes up:** When web availability is next attempted |  | An hour, once the real statuses are known |
| Close the five disagreements `scripts/check-rules-docs.py` reports — *proposed, count corrected 22 Sep 2026* | no | **Wrong screen, nothing tells you.** The new check compares each doc's `When NOT to use` against its ruleset row's `Otherwise`, as sets of component names. **It reported 12 on the day it was written; five were created the same afternoon by new ruleset rows and were closed immediately.** Seven predated it; **five are left, proved by running the check on 22 Sep 2026.** **`alert` and `navigation-bar` have since dropped off it** — Alert's row was reworked when `Pop-up` was parked, and the navigation fold was fixed on 21 Sep. **`donut-chart` and `line-chart` say *Bar chart* where the ruleset and Figma both say *Bar graph*** — the same class of naming defect as `Feedback Messages`, and the file is `bar-chart.md`. **`energy-tag`** names `Chip`, `Score tag` and `Tag` and its row names none. **`toggle-group`** does not name `Cell content`. **`link` bolds more than the component name** — *Button tertiary* — which reads fine to a person and defeats an exact match. **Each needs deciding one at a time**, because either side can be the wrong one **Comes up:** Any screen using one of the five | | Two hours |
| ~~Take the named-and-dated attributions out of the component docs~~ — **Decided 21 Sep 2026: the convention stops applying to component docs.** `CLAUDE.md` says so now, and `phone-number-field`'s note is cut. Done | no | **Nothing breaks; a person reads the wrong kind of page.** A component doc is read by people as well as agents, and **an attribution is repo bookkeeping rather than documentation** — who said a thing and when belongs in the ruleset and the decision log, which is where the rules for an agent are enforced. Gabriel's words, 18 Sep. **The sweep is one page.** Measured the same day across all component docs: `badge` carried two and they were cut on the spot; **`phone-number-field` line 179 is the only one left**, reading *“Corrected 18 September 2026… Gabriel confirmed it was inverted.”* **There is a rule to settle first, and it is why this is a row rather than a two-minute edit.** `CLAUDE.md` says that once a correction is approved you *“say what changed and when, inside the file”* — which is exactly what that sentence is doing. **Either that convention stops applying to component docs, or the sentence stays.** Gabriel decides which, and `CLAUDE.md` changes with it **Comes up:** Every correction made to a component doc from now on | | Twenty minutes, once the convention is settled |
| ~~Settle whether `Image Ratio` is a component an agent may be sent to~~ — **Decided 21 Sep 2026: it joins Never select.** Gabriel: a Figma-internal helper for designers, never a component in a product screen. Both inventory entries marked, the `Image slider` pointer removed, `image-slider.md` updated. Done | no | **The agent stops and asks you — or follows a pointer to nothing.** **Gabriel, 18 Sep: it is an internal Figma component, built to save designers handling image ratios by hand, and he believes Figma now has the feature natively.** **What the ruleset does with it today contradicts that.** The `Image slider` row sends *“A single image held to a fixed ratio”* to **Image ratio**, and that is **the only place in the whole ruleset that names it as a destination**. It has **no `Choose` row of its own**, so an agent that follows the pointer finds no rule telling it when or how to use what it arrived at. It has **no doc**. And it is **one of the two names that exist in two libraries under different keys** — already an open question of its own, which this answers half of. **Three outcomes, and they need different work.** Native in Figma now → the pointer comes out and the component is retired. Still needed by designers but never by an agent → it joins **Never select** with the reason written down, as `Filter button` did. A real component → it needs a `Choose` row and a doc like any other. **What settles it:** a look at the live Figma file, and whether anything in the four libraries still places it. **The pointer and the `image-slider` doc agree today**, so `check-rules-docs.py` is clean either way — **changing one without the other is what would break it** **Comes up:** Any screen with a single fixed-ratio image | **A session with Figma open** | Half a day |
| Rework `Button`'s horizontal padding, so a tertiary button aligns under a block of text — *proposed, Gabriel 21 Sep 2026* | no | **Wrong screen, nothing tells you — and a whole component exists to work around it.** `Button` carries horizontal padding on every variant. A tertiary button placed below a block of text and aligned left therefore **does not line up with the text above it**, because the padding sits between the container's edge and the label. **`Text button` was built for that reason**, and Gabriel's words on 21 Sep were that `Button` should be reworked one day. **Why it is not urgent:** the workaround exists, is a real component, is documented as of 21 Sep, and produces the correct result. **Why it is real:** a design system carrying two components because one of them cannot align is carrying a defect with a name. **Not scoped here** — whether the fix is a padding option, an alignment property, or something else is the first question, and it is a library change rather than a documentation one **Comes up:** Any tertiary button under a text block | **A library decision, and whoever owns `Button`** | Unknown until the approach is chosen |
| Bring the other two Badge descriptions in line with the corrected rule row — *proposed* | no | **Wrong screen, nothing tells you.** The `Badge` **Choose** row in `components-rules-ai.md` was corrected on 18 Sep: a badge is normally anchored to a host and **may also stand alone**. **Two other places still say host-only.** The `Tag` row's pointer reads *“The marker is anchored to another component's geometry → **Badge**”*, which is true but no longer the whole rule. The inventory entry reads *“Attention marker attached to a host component.”* **Neither contradicts the corrected row outright** — both are simply narrower than it, which is why this is a row rather than a same-task fix. An agent reading either one alone would not know the standalone case exists. Two one-line edits, and both are Gabriel's call because they change what a page means **Comes up:** Any screen wanting a count that is not pinned to a control | | Ten minutes |
| Give six docs' `When NOT to use` the two-column table the template asks for — *proposed* | no | **Wrong screen, nothing tells you.** `accordion`, `autocomplete`, `avatar`, `legend`, `divider` and `rating` write that section as prose or a bullet list. **They are not wrong to a reader** — but `scripts/check-rules-docs.py` cannot compare them to the ruleset, so those six sit outside the only check that would catch them drifting. **The fix is layout, not meaning:** the same sentences in the `Instead, when… | Use` shape the other 51 use **Comes up:** Silently, whenever one of the six drifts | | An hour |
| Write the selectable components that still have no doc — *one left, 22 Sep 2026* | no | **The agent stops and asks you — or worse, it does not.** All sixteen are reachable by a rule as of 18 Sep, so an agent **is** sent to them and finds nothing. `Badge`, `Badge Store`, `Button Bar`, `Date Field`, `Estimation card`, `Feedback Bar`, `Feedback Thumb Buttons`, `Image Slider`, `Loading State`, `Mega menus`, `Navigation Bar (App)`, `Pop-up`, `Score Tag`, `State Message`, `Text Button`, `Tooltip`. **The six with web code are done.** `Button Bar` on 18 Sep, then `Badge`, `Image Slider`, `Loading State`, `Text Button` and `Tooltip` on 21 Sep, each from its rule row plus its web source. They fill 8 to 12 of the 17 template sections. **Twelve are left, not ten — this row's own list was wrong.** It named sixteen; the registry holds eighteen. **`Burger menu` and `Map template` were missing from it** and have been since it was written, so every figure quoted from this row has been two short. `components/coverage.py` counts from the registry rather than from a hand-kept list, and had it right throughout — the same lesson as the inventory going stale on 18 Sep. **Three more were written from Figma on 21 Sep** — `State Message`, `Navigation Bar (App)` and `Score Tag`, the first docs in this repo built from a live Figma read rather than from web code or Zeroheight. **All nine that were left are now written, except the one deliberately held.** `Date Field` and `Feedback Bar` on 21 Sep; `Burger menu` and `Mega menus` on 22 Sep with the navigation four; `Badge Store`, `Feedback Thumb Buttons`, `Estimation card` and `Map template` on 22 Sep from a live library read. **One is left and it is `Pop-up`**, held until its overlap with `Modal bottom sheet` is settled — which is a question in this file, not a doc waiting to be typed. **So this row is finished as a task.** `components/coverage.py` is the count that matters and reads **75 documented, 1 selectable gap**. **The other ten have a rule row and nothing else**, which fills two of sixteen sections and produces a stub. **Figma is what the other fourteen sections need**, and no session has had it during this work. **The method is proved and Gabriel has read six of them.** His reading caught five things no check could — on `Text Button` alone, why the component exists, what `danger` and `inverted` are for, the mandatory external icon, and that a form's submit is a **primary** `Button`. **Comes up:** Every one of the sixteen | **Nothing. The one left waits on a decision, not on access** | Done, bar `Pop-up` |
| Give asking-the-user its own section in the ruleset, separate from telling them — *proposed, Gabriel's idea 18 Sep* | no | **Wrong screen, nothing tells you.** *Providing feedback and status* now holds nine rows and **two opposite directions**. Seven are the system telling the user something — `Snackbar`, `Feedback message`, `Info state`, `Tag`, `Score tag`, `Badge`, `Tooltip`, `Coach mark`, `Loading state`. Two are the system **asking** the user — `Feedback bar` and `Feedback thumb buttons`, both added 18 Sep. **Gabriel's words: they should be part of the same tree of feedbacks to the user.** He flagged it as an idea, not an instruction, which is why this is a row rather than a change. **What it costs:** one new `###` heading and two rows moved. **What it buys:** an agent scanning for *how do I ask the user what they think* finds a heading that says so, instead of reading nine rows about the opposite thing **Comes up:** Any screen that collects an opinion | | Half an hour |
| Settle when to use `Feedback bar` against `Feedback thumb buttons` — *proposed* | no | **The agent stops and asks you.** Both ask the user's opinion, and **Gabriel confirmed 18 Sep they are separate components — the thumbs are not inside the bar.** What is missing is the condition for choosing. **The rows written on 18 Sep split them by how much the block carries** — a bare thumbs up/down against a laid-out block with an illustration, a pre-title and its own buttons — and **that split is read from their Figma properties, not from any written rule.** It is a reasonable guess and it is labelled as one inside the ruleset. **Neither is built on web**, so the answer is a Figma look or a product decision, not a code read **Comes up:** Any screen that collects an opinion | | An hour, once a source is open |
| **Clarify what `Menus` is actually used for** — *Gabriel is investigating, 21 Sep 2026* | no | **Wrong screen, nothing tells you — in the other direction.** `Menus` went onto **Never select** on 18 Sep as a **provisional** decision, not a settled one: Gabriel's words were that it was built to cover international content needs, that nothing requires it to build anything today, and that it should evolve when confronted with real product. **So the risk this row exists for is the opposite of the usual one** — not an agent placing something forbidden, but a real need going unmet because the rules say no. **What would settle it:** a screen that actually needs a profile or language menu standing on its own, rather than as one of `Navigation bar`'s controls. **Until then the entry stands and is marked provisional inside the ruleset**, so nobody reads it as a closed question. **It is used, and that is new — Gabriel, 21 Sep 2026.** `Menus` is used by **one brand, in one file**. **What it is used *for* is not clear to him either**, and he is investigating. **So this row is no longer waiting for a need to appear — it is waiting for an answer about a use that already exists.** Nothing changes in the ruleset until that answer lands: the entry stays **Never select**, provisional, because one unexplained use in one file is not yet a reason to open it to every agent. **What would settle it:** what that brand's file does with `Menus` — whether it is a profile or language menu standing on its own, something `Navigation bar`'s own controls already cover, or a one-off nobody should copy. **Comes up:** Any internationalised navigation | **Gabriel's investigation** | An hour, once he has the answer |
| Find the variant axes for `counter-field` and `donut-chart`, the two flows with nothing to mine — *proposed* | no | **The agent stops and asks you.** Narrowed 18 Sep from a four-component row, after checking each doc: **`filter-bar`, `media-upload`, `legend`, `chip-group` and `phone-number-field` all had unused material sitting in their own pages**, and their flows were extended from it the same day. These two did not. **`counter-field`'s whole `Variants & Modifiers` section is the shared form header** — the same paragraph seven other docs carry, with nothing of its own. **`donut-chart` is not built**: its readiness row reads `To Do 🚧` on all four platforms, so *No further variant axes are documented* is not a gap, it is the truth, and this row should be closed rather than worked if that is still so. **Where the answers would come from:** the live Zeroheight page and the Figma variant properties, not this repo **Comes up:** Any numeric stepper, any part-of-whole chart | | Two hours, once a source is open |
| Seven conditions are undocumented inside flows that otherwise work — *proposed* | no | **The agent stops and asks you.** Written down 18 Sep while extending five flows, each as a visible `not documented` line rather than an invented condition. **`filter-bar`** — the all-filters button and the primary button can each be shown or hidden, and nothing says when. **`chip-group`** — whether one group may mix chip types, and how many chips a group may hold. **`legend`** — below the graph or to its left, with no rule for choosing; the doc's own note says no position tested better than the others. **Why they were left visible:** an agent reading *not documented* asks or declares; an agent reading a plausible invented condition follows it and nobody ever finds out. **What closes them:** user testing for the legend, which its own page asks for, and the source pages for the rest **Comes up:** Any filter bar, chip group or chart | | Unknown until a source is open |
| ~~**Make every usage doc tool agnostic**~~ — **Done 22 Sep 2026.** 58 lines across 22 docs; `check-tool-neutral.py` now reports clean. Ten `-figma.md` sub-pages created, only where there was content. |
| Fill the accessibility and breakpoint sections in the component docs | no | **The agent stops and asks you — 85 times.** Measured 16 Sep by `components/coverage.py`, across all 55 documented registry entries. **`Accessibility (a11y)` is the worst section in the repo: 49 of 55 pages have the heading and write `Not documented` under it.** The six that say anything are `Carousel`, `Checkbox`, `Checkbox Group`, `Coachmark`, `Phone Number Field` and `Snackbar`. **`Breakpoints & Platform Adaptations` is second: 36 of 55 say nothing.** Between them, 85 pages looked at the section and wrote nothing. **Why it is not urgent:** the September scorecard asks no accessibility question, so a run cannot fail on it today. **Why it will be:** contrast, touch target and focus order are where a compliant-looking screen is actually wrong, and nothing in this repo would catch it. **Do not write these from guesswork** — a11y answers come from the web code repo and the live components, the way the energy mapping did. Regenerate the ledger after any batch to see the number move **Comes up:** Every screen, invisibly | | Unknown until one page is done and timed |
| ~~**Settle the whole navigation family in one go**~~ — **Done 22 Sep 2026.** Four docs written, the web navigation recorded as one system in the ruleset, every name left alone as Gabriel asked. **`Navigation Bar (App)` stays out** until the app is investigated. |
| ~~**Stop `check-rules-docs.py` folding `Navigation Bar (App)` into `Navigation bar`**~~ — **Done 21 Sep 2026.** Disagreements 6 → 5, and the false line about `navigation-bar.md` is gone. |
| Scale the availability table to iOS and Android | no | **The agent stops and asks you.** Only **Figma** is answered completely. Web was attempted, found wrong, and reverted — it now says "not established, ask", the same as iOS and Android. The component docs record 22 components unavailable on iOS and 31 on Android, and none of it is in the ruleset. **Do not copy those rows in.** The web pass proved the docs' readiness rows are stale; there is no reason to think the iOS and Android columns are any better, and neither native code repo is on this machine to check against **Comes up:** Every iOS or Android screen |  | Unknown until a source is found |
| Settle `Pop-up` against `Modal bottom sheet` — *flagged for later, Gabriel 21 Sep 2026; investigation first* | no | **Wrong screen, nothing tells you.** Read live from the Components library 21 Sep. `Modal Bottom Sheet` carries a **`Type` axis whose options are `Modal` and `Bottom Sheet`**, a `Platform` axis covering `Web`, `iOS` and `Android`, and `Size` of `Default (Hug content)`, `Full-Height` or `Full-Screen` — 21 variants. `Pop-up` has **no `Platform` axis at all** — 12 variants, and only a page annotation saying *"Pop-up (iOS/Android)"* and *"⚠️ Only for iOS and Android"*. **Three contradictions fall out.** One: the ruleset sends **web** to `Pop-up` for a short confirmation, under `Alert`; Figma says `Pop-up` is not on web. Two: the ruleset calls `Pop-up` the **small-content** alternative, and `Pop-up` has a `Full-height` type. Three: `Modal Bottom Sheet` with `Type=Modal` on iOS and Android already covers what `Pop-up` claims, so two components answer one question and nothing chooses between them. **What actually differs in the props:** `Pop-up` alone has `Padding content` ON/OFF and a `Content` instance swap; `Modal Bottom Sheet` alone has `Drag Handle`, three header sub-booleans and `Size=Full-Height`. Both carry `Header`, `Button Bar` and a `Content placeholder` slot. **Not tested:** whether these are two real components or one is a leftover. Gabriel said the pair confuses him too, which is the reason this is a row and not a guess | Your answer, after the investigation | Half a day to investigate, unknown to fix |
| ~~**Draft the missing eval intents, from the rules never tested**~~ — **Done 23 Sep 2026.** 33 rows added, taking the file from 31 intents to **64**. Row 17 corrected in the same edit: it accepted `Pop-up` for a blocking confirmation **on web**, which the 21 September apps-only ruling forbids, so a wrong answer would have scored a pass. **Not yet run** — running it is the row below, and it must be a cold session | yes | Superseded by the done entry |  | Done |
| **Re-run the component selection eval — the file now holds 64 intents and 37 have never been run** — *unblocked 23 Sep 2026* | yes | **Wrong screen, nothing tells you.** `components/components-rules-ai-eval.md` is the only check that asks whether an agent given the ruleset alone still picks the right component. **Proved 22 Sep 2026: its last run was 8 September**, and it scored **27 intents of the 64 the file now holds** — **rows 28–64 have never been run at all**, 33 of them drafted on 23 September for exactly this run. Since that date the ruleset has grown to **71 rows**, with roughly twenty written on 21 and 22 September, plus the never-select rulings, the navigation family and the built-outside-the-system flag. **None of that has ever been tested.** Its own scoring band says *below 56, do not point agents at it*, so today the honest answer about the ruleset is that nobody knows. **It is also the repo's only check on the ruleset contradicting itself** — `check-rules-docs.py` compares a doc against a rule, never a rule against another rule, and run 2 found exactly that kind of fault. **Run it cold, in a fresh session, with only `components-rules-ai.md` in context** — the skill's own instruction, and the reason it cannot be folded into another task **Comes up:** Every time an agent chooses a component |  | Half a day, and it must be a fresh session |
| **Seven of the eight rulesets have never been tested by anything** — *found 23 Sep 2026, while renaming the eval* | no | **Wrong screen, nothing tells you.** The repo holds **eight `-rules-ai.md` rulesets and one eval**: `components-rules-ai-eval.md`. `icons-rules-ai.md`, `color-rules-ai.md`, `spacing-rules-ai.md`, `typography-rules-ai.md`, `radius-rules-ai.md`, `shadow-rules-ai.md` and `border-width-rules-ai.md` have **no eval at all**, so nobody has ever asked whether an agent reading one of them reaches the right answer. **The naming rule of 23 Sep is what made this visible** — under the old name the gap did not show in a directory listing; under `<ruleset>-eval.md` it does. **What an eval would catch that the six checks cannot:** two rules that contradict each other, and a rule so vague two agents split it differently. `check-rules-reach.py`, added 23 Sep, now catches the third case for components only — a component no rule names — and there is no equivalent for any other ruleset. The rest compare a doc to a rule, never a rule to another rule. **The shape an intent takes is now settled** — the 23 September drafting took the component eval to 64 intents, and its rows are the model to copy. **Do not do all seven.** The component eval took three runs and twelve ruleset fixes to become useful; start with **colour**, which has the most rules and the most downstream effect, and decide from what it finds whether the rest earn one **Comes up:** Every time an agent reads a ruleset that is not the component one |  | Half a day for the colour one alone |
| **Check what a readiness table claims, not just that it exists** — *proposed 22 Sep 2026; made the path by Gabriel's decision, 24 Sep; **Figma column done 24 Sep*** | no | **Figma column: done 24 Sep 2026.** `scripts/check-readiness-figma.py` runs in `check-all.py`; six cells fixed, 75 of 75 agree. **Web, iOS and Android remain**, and `components-rules-ai.md:452` stays until all three are verified. **Decided 24 Sep 2026: the readiness table in each component doc is the one place availability lives.** Gabriel's process, in his words: a component in Figma can be used by a designer; if it is not on web, the design system team builds it; if it is not in the app, the needing team builds it. **The agent knows or asks which platform it designs for — that is not documentation.** The only real conflict is designing an app screen in Figma with a component that exists only on web; in a platform's own codebase it cannot happen. **What this unblocks, once the tables are verified:** `components-rules-ai.md:452`, *'Do not read a component doc's readiness row, on any platform'*, is removed, and the *Platform limits* section loses its availability table. What stays there is only what is not availability — `Pagination`'s *use infinite scroll on mobile*, and the width constraints on `Action Menu` and `Modal Bottom Sheet Menu`, which are design rules filed under the wrong heading. **Not before.** The table is known wrong in places: on 10 Sep five of thirteen components the docs called unavailable on web were shipping. Pointing an agent at it before it is fixed trades a stall for a confident wrong answer. **Wrong screen, nothing tells you.** Every doc carries a `Figma \| Web \| iOS \| Android` row and **nothing reads a single cell of it.** **Proved 22 Sep 2026:** `template-drift.py` matches the header with one regex and stops there. **The failure already happened once** — `navigation-bar.md` claimed **Ready** on web while its own next sentence said no web component exists, and **a person caught it, not a check.** **Why this row is worth more than it looks:** the readiness table is the only claim in the whole repo already written in a fixed shape, so it is the one place a script could compare documentation against reality. Everything else is prose. **Do it one column at a time.** Figma is answerable today from the four registries. Android and iOS become answerable the day the app monorepo is read. Web is the hard one and stays last — `libraries/ui` is not the whole story, which is how `Score tag` was recorded as not built when it is **Comes up:** Every time an agent is asked to build for a platform | | Half a day for the Figma column alone |
| **Compare the documentation against real implementation, not only against itself** — *Gabriel's direction, 22 Sep 2026* | no | **Wrong screen, nothing tells you — and this is the gap under every other row here.** **Proved 22 Sep 2026, and still true of the six there are now:** every standing check reads this repo against this repo. None opens Figma, the web code or the app, so **not one of them can tell whether a sentence is true.** `map-template.md` fills 13 of its 16 sections, passes every check, and every word came off a component library with nobody who knows the product having read it. **Gabriel's framing:** a documentation will never be fully trustworthy, because context is always missing from some product — so the target is not certainty, it is comparison against the three things that are real. **The three differ enormously in cost.** **Figma** is reachable today and partly structured, in the four registries. **The app is one monorepo** — one read, the same shape of job the four token scripts already do against the web repo, and nothing here has ever opened it. **The web is many repositories and stays last:** the repo reads `libraries/ui` only, and that miss is what recorded `Score tag` as unbuilt when it lives in the patterns package. **The blocker is shape, not access.** A script can compare a table to a repo; it cannot compare *"the calendar button may be decorative"* to anything. **So the readiness-table row above is the first concrete step of this one**, not a separate idea **Comes up:** Every time a page is read as finished |  | Weeks, and it is a direction rather than a task |

#### Define what needs to be built

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Extend the "when nothing fits" rule to cover *exists, is correct, and doesn't physically fit* | yes | **Wrong screen, nothing tells you.** The donut chart is 373px wide inside a 360px frame. The agent switched off a mandatory part of it and broke no rule doing so. **Parked by Gabriel, 10 Sep** — it tangles three questions and he wants them separated first: is the Figma component missing a stacked variant, is the documentation wrong, or does the component itself need fixing? Don't pick this up until that is settled | **Your three-way decision** — is the Figma component missing a stacked variant, is the documentation wrong, or does the component itself need fixing | Half a day once it is |

**The "Legend" task is done, and it did not settle the chart-sizing one.** They
looked like one fix — both "the component exists and is right, but I can't use
it in the form I need". They aren't. The legend was never placeable at all, so
the answer was to stop calling it a component. The donut is placeable and simply
too wide, which is still an open question about the component itself.

**The routing-entry task is done, and it did not settle it either.** That task
sends a simulator to compose-and-declare because no Pattern or Experience is
that block. The donut chart is the opposite case — the component exists and is
right, and simply does not fit the width. Compose-and-declare has no answer for
that, which is why the chart-sizing row is still waiting on your three-way
decision.

#### Choose the tokens

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Settle whether `Spacing/56` is a page-rhythm token or a forbidden one | yes | **Wrong screen, and the scorecard fails the run.** The spacing ruleset says both, 83 lines apart. **Component spacing stops at 32** lists `Spacing/56` among the page-rhythm tokens at `spacing-rules-ai.md:60`; **Do not use** forbids it at `:143`, on the grounds that it appears *"not in any usage note, layer, or rhythm table"* — which the rhythm table in the same file falsifies. An agent reaching for a section gap reads the first, emits `Spacing/56`, and the scorecard's spacing deny-list fails the run for obeying the ruleset. **`Spacing/40` is loose in the same way:** it is called *the boundary* and *page-level*, and appears in neither the component range nor the page-rhythm list. Needs the audit read, not a guess **Comes up:** Any screen with section separation |  | Twenty minutes |
| Finish the colour ruleset's restricted list | yes | **The agent stops and asks you.** The pointer into the audit is **already gone** — removed while freeing the energy colours, so this is no longer a dead end. What remains: the list still says "and 28 others" and enumerates only about ten of them, so an agent asking about an unusual token gets no answer. The audit's own section is grouped by family too, so the per-token reasons have to be written, not copied **Comes up:** Any screen using an unusual colour token |  | Half an hour |
| Decide whether `headline/20/bold` belongs in the typography ruleset's used-styles table | no | **The agent stops and asks you.** `typography-rules-ai.md` heads that table *"the styles the design system exercises in real components"* and then lists `headline/20/bold` with a component count of **0** and a *Bound by* reading `direct: unsafe`. Either the style does not belong in a table of what components use, or the heading overclaims and the table is really *the eleven worth preferring*. The count of eleven depends on which answer is right. Low harm — an agent picking it gets a real style — but it is the one row in six rulesets that contradicts its own heading **Comes up:** Rarely, and only for a 20px heading |  | Twenty minutes |
| Give opacity, motion and z-index a ruleset, or say plainly they are out of scope | no | **The agent stops and asks you.** The other half of the row above. All three are code-only — `tokens-index.md` records motion and z-index as having no Figma property at all, and says an alpha colour token is preferred over an opacity. So a Figma agent never needs them and a web agent has nothing it may read. Either answer closes it; leaving it unanswered is what doesn't **Comes up:** Every web screen that animates or stacks |  | Half a day |

#### Put them on the screen

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Record the Figma text-field names for each component | yes | **Wrong screen, nothing tells you.** An agent can place a component but has no way to put words into it, so it ships with the default placeholder text still in place — which breaks nothing. Every string in the test came from reading Figma live, not from this repo | **A session running off `~/.claude/`, plus Figma Desktop open.** The Figma plugin is installed in that config only; a session on `~/.claude-personal/` has no Figma tools at all. Check the bridge responds before starting, not after | A day |
| Make `Donut chart`'s data switches work through the plugin API | yes | **Wrong screen, nothing tells you.** The component exposes `Data 3/4/5`, `Label` and `Value` on its nested `.Donut chart` part. Set through the plugin API they report as applied and change nothing — the arcs carry no property reference, so **proved** by run-003, which set them, read them back as set, and saw five segments still. Its builder got three segments by hiding two arcs and closing a third arc's sweep by hand, and the scorer raised that as a decision you now have to rule on. **The ruling is about the agent; this row is about the component.** Until it is fixed, every run needing fewer than five segments reaches inside the instance, and every one of those becomes another decision for you. Found by run-003, 14 Sep **Comes up:** Any screen with a donut of fewer than five parts |  | Unknown — it is a Figma library fix, not a repo one |
| Let `Donut chart`'s legend fit a mobile width | yes | **Wrong screen, nothing tells you.** **Proved** on run-003 at 375pt: the chart is 176 wide and its internal chart-to-legend gap is fixed at 56, leaving the legend column at most 111pt inside a 343 content width. Full amount names do not fit. The builder shortened the legend labels to "Borrowed", "Notary fees", "Interest" and put the full names in the rows beneath — a workaround it chose alone, which no rule sanctions and no scorecard question catches. **This is the same 56 already on your plate** in `status.md`, seen from the other side: there it is a number above the 48 ceiling, here it is the reason a mobile legend cannot hold a real word. Found by run-003, 14 Sep **Comes up:** Any mobile screen using the chart's legend |  | Unknown — Figma library fix |
| Add an icon-only `Text Button` below 16px, or say 16 is the floor | no | **The agent stops and asks you.** `Icon=Icon only` exists only at `Size=16px`. An agent wanting a small inline explain-on-demand trigger has one size and no rule saying whether that is deliberate. Run-003 used the 16px one and moved on, so nothing broke — it is the absence of an answer that is the finding. Found by run-003, 14 Sep **Comes up:** Any row of values with a per-row explainer |  | Twenty minutes, once you answer |

#### Place them according to the design guidance

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Give grid and breakpoint a ruleset an agent is allowed to read | yes | **Finishes one of the four things the agent must know — how the page is laid out.** Twelve token categories exist; six carry a `-rules-ai` ruleset. A generating agent is told to read `-rules-ai` files and nothing else, so for the other five it has **no authorised source at all** — it either guesses or stops. Two of the five reach a Figma canvas: `grid`, which `tokens-index.md` records as *Figma and product pages only*, and `breakpoint`. Page layout is the one ❌ in `status.md`, and grid is what it is made of. The other three — opacity, motion, z-index — are code-only and sit in the block below **Comes up:** Every screen that needs a margin or a column width |  | Half a day |
| Check the page-layout rules against real screens | yes | **Finishes one of the four things the agent must know — how the page is laid out.** Margins and rhythm are written but have never been checked against a real screen, so whether they hold is unknown, and that is the problem | **You naming 3–5 Figma screens** to check them against | Unknown |

#### Check the content

**Nothing yet**, for the same reason as the first step: there is no rule here to break.

### Scoring a finished screen

These break no design step. They break finding out **which** step broke, which
is what the scorecard is for.

| What | Due by September | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- | --- |
| Give a session a way to know it is inside a run | yes | **Wrong screen, nothing tells you — and it has now happened twice.** A test run on 14 Sep built the energy and finance blocks successfully and wrote no report, no folder, no declarations. The cause was not the missing generating skill: the session **never knew it was doing a run**. The brief arrived as pasted chat text with a Figma link, so nothing matched it to `prompt-run-001.md` and nothing put it in the pipeline. `how-a-run-is-reported.md` was written about exactly this — *"a real run happened, was looked at, and the judgement evaporated with the conversation it happened in"* — and it has now recurred for a **different** reason than the first time. The first was that the rules called a report something you look at; this one is that a run has no entry gate. **What would fix it:** a run starts by creating its folder and saving the brief into it, so a session's first act names the run it is in — the same shape as `/task-next` opening `status.md`. Whether that is a skill, a line in `CLAUDE.md`, or both is open. Raised by Gabriel 14 Sep **Comes up:** Every run started from a pasted brief |  | Half a day |
| Give a run report its title and metadata table an owner | yes | **The agent stops and asks you — and in run-002 it did exactly that.** `compliance-scorecard.md`'s report template opens with `# Run NNN — …` and a Date / Platform / Wireframe / Brief / Output table. Neither was written, because nothing owns the top of the file: the building agent is told to write **only** `## Declarations`, and the scoring agent is told to append **below** everything already there and never touch it. `report-run-002.md` opens straight at `## Declarations`. The scorer put the metadata inside its own section instead and reported the gap rather than fixing it, which is the right behaviour and is why this is visible at all. **The fix is one step in `generate-and-score`:** the orchestrating session writes the title and the metadata table at step one, alongside the brief, before either agent exists — **Smaller since 15 Sep**: the metadata table lost its `Wireframe` row, because a run now sits inside its brief folder. What is left is the title, the date, the platform, the brief link and the destination **Comes up:** Every run |  | Twenty minutes |
| Wire a finding into the backlog on its first sighting | yes | **The agent stops and asks you — or rather, nothing does.** Your own description of the loop has a step the files don't: measure, then work out the root cause, then put the root cause in the backlog. The scorecard now measures and stops. Failures are written into the run's report and go nowhere else, and a flag waits to be seen **three times** before anyone looks at it — which, on a project with one run ever, means never. **Two halves:** every question answered `yes` becomes a backlog row, and the three-sightings threshold comes out of the flag ledger. **The cost to accept:** at fifty runs this floods the list, so it wants a line saying to revisit if it does. Agreed with you 12 Sep as the task after the scorecard rewrite **Comes up:** Every run |  | Half a day |
| Have a generating agent build from the three new rulesets and nothing else | yes | **Tells us whether what we wrote actually works.** It finds problems rather than fixing any, which is the point. The same method found twelve defects in component selection. Run it *after* the fixes above, or we test rulesets we're about to rewrite. **Run it on Sonnet, not Opus** — not to save money, but because a stronger reader silently repairs ambiguity, so testing a spec meant to be unambiguous on the strongest available reader is the weakest version of the test |  | Half a day |
| Check a declaration against the facts file, so a late rewording is caught | no | **Wrong screen, nothing tells you.** The run report has two authors: the generating agent writes `## Declarations` before scoring, the checking agent writes the verdict after and must never touch that section. Both rules are stated in three files and all three are instructions, not mechanisms. The failure to fear is not a dishonest agent but a careless one — a checker that finds no declaration, helpfully writes one so the report looks complete, and reports a pass. **The fix, decided 12 Sep:** the adapter copies declarations into `facts-run-NNN.json` at translate time, so the same text exists twice, written at two different moments. They must agree; divergence means the report was edited after the fact. **Gabriel chose this over committing each run twice** — two commits would work today but add a step mid-run that a person can forget, which is the same class of failure as the missing report that caused all of this | **The adapter, which was deleted on 14 Sep 2026.** There is no facts file and no second copy to compare against, so this row's whole mechanism is gone. **Re-open it only with a different mechanism** — git history already shows any edit to a report after its commit | Half a day, once the adapter exists |
| Score `run-001` and write the report it never got | yes | **Tells us whether what we wrote actually works** — and it would be the first time the scoring sheet is used on real output. `compliance/runs/run-001/` holds the brief and two screenshots and nothing else. The scorecard has never been run against anything, so its six checks, its thresholds and its adapter contract are all reasoning that has never met a real screen. `compliance-scorecard-audit.md` says this plainly: *being implementable is not the same as being correct* | **The Figma file that run produced still existing, plus a session with Figma tools.** Screenshots alone cannot answer whether a colour was token-bound, which is most of what the scorecard asks | Half a day, once both are true |
| Correct `generate-and-score`'s Dev Mode MCP precondition | yes | **The agent stops and asks you.** The skill requires the scoring agent to read through the Figma Dev Mode MCP, so that it is not trusting the channel that drew the screen. **Proved on 14 Sep:** the server answers on `127.0.0.1:3845/mcp`, but it is configured for the `/Users/gabriel.saintmartin` root only, so a session in this repo — and every subagent it spawns — has no such tools. Only raw `curl` reaches it. Run-003 scored through `figma-cli` instead, on your ruling. **The argument for making that permanent:** the independence that matters is between the two *agents*, not between two transports — both read the same live document — and the MCP returns generated React and Tailwind, a translation step `compliance-scorecard.md` explicitly says it does not want. Either wire the MCP into this repo or rewrite the precondition to name `figma-cli` and say why. Raised by run-003, 14 Sep **Comes up:** Every run |  | Twenty minutes |
| Add two verdicts to the flag ledger — one for the prompt or spec, one for a tooling limit | yes | **Tells us whether what we wrote actually works — and today it cannot.** The ledger has four verdicts: `agent error`, `ruleset gap`, `library defect`, `accepted`. A bad screen has more causes than that. It can also be a badly written prompt, or a platform whose agent simply cannot do the thing asked. **Neither has anywhere to go**, so both land as `ruleset gap` or `agent error` — and a documentation page gets rewritten to fix a failure the documentation never caused. That is the most expensive mistake this project can make, because the rewrite looks like progress. **Two new verdicts:** one saying the prompt or the spec did not carry what the agent needed, one saying the platform could not do it however well it was asked. **Do this before anything else in this section.** It measures the confusion before we build anything against it. Raised by Gabriel 16 Sep, from the Figma-agent test **Comes up:** Every run, from now on |  | An hour |
| ~~Fix a spec format, and make it the only thing a run is built from~~ — **Done 24 Sep 2026: `specs/spec-rules-ai.md`.** Making it the only thing a *scored* run is built from is not done yet | yes | **Tells us whether what we wrote actually works.** Two runs of the same brief are only comparable if the words handed to the agent were the same words. Today the brief is prose, written once, and each run's session re-reads it in its own way. So a score moves and nobody can say whether the documentation changed, the agent changed, or the reading changed. **The shape already exists:** `compliance/briefs/brief-001/brief-001.md` is objective, content, constraints. **The work is naming the fields and freezing them**, then saying that the building agent gets the spec and the rulesets and nothing else. **Platform-neutral, always** — a spec that mentions Figma is a prompt wearing a spec's name. Raised by Gabriel 16 Sep **Comes up:** Every run |  | Half a day |
| Write one prompt template per platform, wrapping the spec unchanged | yes | **Tells us whether what we wrote actually works.** The Figma test on 16 Sep showed each platform's agent has its own habits, and adjusting the wording agent by agent means the wording is never the same twice. **What this does:** the spec stays untouched, and a per-platform template wraps it — the platform's own conventions, plus a stated note on what that platform's agent can and cannot do. **What it does not do, and the row should not pretend otherwise:** the per-platform difference does not disappear. It moves into one file instead of being re-invented inside every run. **Tooling stays a live variable** — Figma's own agent and `figma-cli` have different reach, and identical words still produce different failures, which is why the tooling verdict row above is the first of the three. **The first template is Figma's internal design agent**, on Gabriel's ruling of 16 Sep. Raised by Gabriel 16 Sep **Comes up:** Every run, and every platform added later | ~~The spec format being fixed~~ — fixed 24 Sep | Half a day per platform |

### Keeping the repo usable

Nothing here changes what an agent designs. It changes whether a person or a
session can find their way around — a stale filename, a missing introduction, a
pointer to a folder that no longer exists.

Real work, all of it. None is due by September, so the deadline column would say
"no" eight times and is left out.

| What | What goes wrong today | Blocked by | How long |
| --- | --- | --- | --- |
| **Rewrite what "done" means at end of September in `project/the-project.md`** — it still names the Figma milestone, which the iOS demo replaced on 24 Sep. A stakeholder reading it gets the old goal. Your words, so your call | **Only our own files change.** But it is the one page written for people outside the project | | Half an hour, with you |
| **Name `specs/` in the two sentences that count the contract** — `README.md` says *the seven `*-rules-ai.md` files*; with `spec-rules-ai.md` there are eight. `CLAUDE.md`'s *Where to look* has no `specs/` row. Both are sentences you own. Found 24 Sep | **Only our own files change.** A reader counting the contract files misses the spec format | | Minutes |
| **Six lines across four files say the repo has five checks; it has six** — *found 23 Sep 2026, needs Gabriel's go* | **Only our own files change.** `scripts/check-rules-reach.py` was added 23 Sep and wired into `check-all.py`, taking the standing checks from five to six. **Four files still say five:** `CLAUDE.md` lines 129 and 136, `README.md` line 10, and `.claude/skills/task-check/SKILL.md` lines 45, 69 and 78. **`scripts/README.md` is the fifth and needs two things** — its count corrected *and* a row for the new script, which its table of every script does not have. **No check can see any of it** — `check-links.py` asks whether a filename names a real file, never whether a sentence about it is true, and that is exactly the staleness this repo keeps rediscovering. **`project/decisions.md:70` says five and is deliberately left alone**, because the log records what was believed on a date and is never rewritten. **Why it waits for him:** a count is a claim about what a page contains, and the `task-check` skill is the file a future session obeys, so a wrong number there misroutes work rather than merely misinforming | | Fifteen minutes |
| ~~**`components-index.md` says the eval holds 27 intents**~~ — **Done 23 Sep 2026, on Gabriel's go.** Line 14 now reads 64, the number the drafting settled. | Superseded by the done entry | | Done |
| **`status.md` is 4,927 words and the rule says one screen** — *measured 23 Sep 2026* | **Only our own files change — but it is the one page he is told to open first.** `wc -w` says **4,927 words across 452 lines**. Three files state the one-screen rule: `CLAUDE.md`, `.claude/rules/project.md` and `status.md`'s own header. **The last time this was measured it was 966 words, on 11 Sep, and that was already treated as over** — the struck-through note is in the done list below. So it has grown roughly fivefold in twelve days. **The cause is structural, not carelessness:** `/task-check` adds a *Done, <date>* block every task and nothing has ever removed one, so the page is now a done log with the current task buried at line 44. **What it costs:** the page exists so he can see where the project is in one look, and that is no longer possible. **Recommendation: cut every *Done* block older than the last two tasks.** The backlog's done list already holds all of them in more detail, with dates — nothing is lost, and the two pages stop saying the same thing twice. **Then fix the loop that refills it**, or this row comes back in a fortnight | | An hour, plus a change to `task-check` |
| ~~**Record the 23 Sep naming rule in `project/decisions.md`**~~ — **Done 23 Sep 2026.** Logged with what was rejected — renaming `compliance-scorecard.md` to an `-eval` — and four costs, including the four dated records that now name files which no longer exist. It also states that it supersedes the `<pillar>-<suffix>` grammar decided 8 Sep, whose own cost line named the inconsistency this retires. |
| **Say on each page how it was written, so a page nobody has checked cannot read as finished** — *proposed, Gabriel's framing 22 Sep 2026* | **Wrong screen, nothing tells you.** Nothing on a component page says what it was built from, so a page assembled off a library alone looks exactly like one written from the published usage docs. **Proved 22 Sep 2026:** `map-template.md` fills **13 of its 16 sections**, which the coverage ledger reads as one of the better pages in the repo — and **every word of its guidance came off the component library, with nobody who knows the product having seen it.** **Coverage cannot ever see this**, and that is not a defect in it: it answers *is the page complete in shape*, never *is a filled section true*. The two do not substitute for each other and the row below says so where a reader meets the count. **Gabriel's framing, 22 Sep 2026, and it is the point of the row:** he does not want a quality score, he wants to know **which pages he needs to check.** **What it is:** one banner sentence at the top of the pages built without product knowledge — the same mechanism `menus.md` already uses to say the page is not permission to place the component. **One pass over all 75 pages, and then it maintains itself:** after the pass, no banner means checked, and a page written later carries its banner from whoever writes it. Without the single pass, an unbannered page only means nobody looked. **Two candidates today** — `map-template` and `estimation-card`. Both were written 22 Sep from a live library read with no product source and no code to check against. **The wording is Gabriel's to approve before anything lands on a page**, and **one fixed form is reused verbatim**, so 75 pages do not drift into 75 different warnings. **The banner count goes into the coverage ledger**, so both facts are met in one place rather than adding a second page to remember. **Rejected in the same conversation, and worth not re-proposing:** a two-mark scheme — supervision plus a next-check action — and a four-value action list. Gabriel: too much workflow to remember in a project that already carries a lot **Comes up:** Every time a page is read as finished |  | Half a day |
| Record every component's Zeroheight page address, once | **Only our own files change.** **All 21 addresses were found on 18 Sep and used**, but they live in a session scratchpad and are recorded nowhere in this repo. **Proved that day:** the styleguide index at `/626199550/p/43e0c4-index` lists **59** pages while the site navigation holds **127** — `chip-group`, `coach-mark` and `modal-bottom-sheet-menu` are on the navigation and not the index, so the index alone is not enough. Opening every collapsed nav group and reading its links gives the lot in one run. **Why it still matters:** a wrong address returns a page with zero images and no error, so a future re-merge has no way to tell a wrong slug from an empty page |  | An hour |
| Re-merge the three components merged before the merge script was fixed | **Only our own files change.** `select-card-group`, `dropdown` and `modal-bottom-sheet` were merged on 17 Sep, before **29 faults** were found in the two scripts on 18 Sep. **Proved on `modal-bottom-sheet`:** re-running it placed **2 pictures the old run had left behind**, lost no sentence and duplicated none. A third, `d51884675204ed59ebfe8d.png`, was unplaced and nobody has looked at why. The other two were not re-tested. **Their addresses are now known** — `44717f-select-card-group`, and `dropdown` from the navigation — so this is a short job |  | An hour for the three |
| Teach the link check to spot a page nothing points at | **Only our own files change.** `scripts/check-links.py` answers *does this link go somewhere*. It cannot answer *is this page reachable at all*, so a file that no table of contents lists is invisible to it — which is exactly how `project/how-a-run-is-reported.md` came to exist unlisted. The check skill now says to list a new file, but an instruction is a hope; a check that fails is a mechanism. Scope it narrowly: every `.md` under `project/` must be linked from at least one other file **Comes up:** Every time a page is added or renamed |  | An hour |
| ~~**Two pages still name `check-links.py` as the thing to run**~~ — **Done 22 Sep 2026.** Both now name `check-all.py`, and the sentence about what a clean run does not prove went in with it. Gabriel approved the wording change | **Only our own files change.** `README.md` line 8 and its `scripts/` row both tell a reader that the repo's one check is `check-links.py`, and `CLAUDE.md`'s *"three deliberate exceptions"* sentence enumerates the scripts the same way. **Both were already incomplete before 22 Sep** — neither mentions `check-rules-docs.py` or `check-tool-neutral.py`, which have existed since 18 and 22 September. `scripts/check-all.py` makes it worse by giving the five a single front door that neither page names. **Why it was not fixed in the same task:** these are sentences explaining what the repo is, not rows in a table of files, so they are Gabriel's to approve — `scripts/README.md` was updated because that one *is* a table of contents. **The fix is three sentences** and should say *run `check-all.py`*, with the five named beneath it in `scripts/README.md` where they already are **Comes up:** Every time someone new looks for how this repo checks itself |  | Twenty minutes |
| Decide whether the name check should read past `.md` files, and past `.md` names — *proposed, 22 Sep 2026* | **Only our own files change.** The name check added 22 Sep reads **`.md` files only, and only `.md` names inside them**. Two gaps follow, and both are deliberate rather than oversights. **One: a stale filename in a `.py` docstring or a `.mjs` comment is invisible**, because the walk is `rglob("*.md")` — and `scripts/` has five Python files whose docstrings name repo files freely. **Two: `.json` names are skipped outright**, so a `figma/*-registry.json` renamed tomorrow would break the four sync skills with nothing reporting it. **Why `.json` was skipped:** the web code repo's files are named here by the dozen — `scoreTag.json`, `shared/figma/grids.json`, `libraries/tokens/src/…` — and **proved 22 Sep**, including them put 30-odd names into the report that this repo can never resolve. **Recommendation:** do the first, skip the second. Reading `.py` and `.mjs` files costs one line and adds no noise — every script name in the repo already resolves. The `.json` half needs a way to say *this one is ours* first, and inventing one for a rename that has not happened is the wrong order **Comes up:** Whenever a script or a registry is renamed | | Twenty minutes for the `.py` half |
| Write the introduction this repo has never had — what the GSL Design System actually is | **Only our own files change.** Every human page assumes the reader already knows what a token is, what a component is, what the four Figma tiers are, and why *highest tier first* is the rule everything hangs off. Nothing anywhere explains any of it. You don't need it; anyone you hand this to does, and they hit the wall on the first page they open. It should also say how Figma, Confluence and the web code repo relate to each other, because a reader's first real question is *which one is true* **Comes up:** Every time someone who isn't you opens the repo |  | Half a day |
| Write the human documentation for this repo, as one job | **Only our own files change.** **Gabriel, 14 Sep: this is one task, not a row per pillar.** It replaces two rows that asked separately for a `figma/` page and a `components/` page. **Why one:** `tokens/` and `compliance/` each got a human page only because something forced one, `components/` and `figma/` never did, and `icons/` was created on 14 Sep without one. The coverage is accidental rather than decided, and fixing it a pillar at a time repeats the accident. **What it covers:** a page for a person at the entry to every pillar, plus the introduction this repo has never had — what the GSL Design System actually is. `components/` is the biggest pillar and its only entry point is a routing page written for a machine | | Two days |
| Write down how knowledge gets **into** this repo, and how it stays true | **Only our own files change.** `README.md` explains beautifully how a rule earns the right to exist — ledger, audit, ruleset, eval. It never explains how the raw material arrives: Zeroheight export to Confluence to `components/`, the four token scripts reading the web code repo, the four Figma syncs. Those exist only as one-line skill descriptions, so nobody can answer *what goes stale if nobody runs anything* — which is the question that decides whether this repo is trustworthy six months from now **Comes up:** Whenever the design system changes underneath us |  | Half a day |
| Rewrite the stakeholder page's *What has to be true* block around the five design-process steps | **Only our own files change.** **Gabriel, 15 Sep:** the process steps are the spine, not a UX/UI/Content decomposition of the deliverable — the steps change rarely, and a three-branch split is jargon a PM mis-hears. `project/design-process-map.md` now holds the full map; this task turns it into the short page. **What changes in `project/the-project.md`:** the goal sentence becomes *meets the user's need and the business's* — speed moves to the ROI column, because it is a property of the process and not of the interface; the seven design-system steps stop being the top-level table and sit inside *Solutioning*; a roadmap section is added, ordered from the map's *would change the step* rows; a line says what Gabriel owns, what he does not, and what he intends to take. **The September objective does not change** and stays true. **Start this in a fresh chat** — the session that wrote the map is the worst judge of whether its summary reads clearly **Comes up:** Every time the project is explained to someone new | **The process map being reviewed and settled.** Gabriel, 15 Sep: it is still a draft, and rewriting the stakeholder page from a draft means doing it twice | Half a day |
| Redefine the seven steps to cover the whole design process | **Only our own files change.** The seven steps describe designing a screen *inside the design system*. **Gabriel, 15 Sep: his role is enabling AI across the whole design process, not only the design system**, so the steps are a subset of the real picture and the stakeholder page says so out loud. Until they widen, `status.md`, `project/the-project.md` and this file's *What's broken* headings all organise work around a frame he has already outgrown. **Not a rewrite by me** — it starts as a conversation with him about what the wider steps are, then the three files follow. **The September objective does not depend on this** and stays true whatever the steps become **Comes up:** Every time the project is explained to someone new |  | Half a day, after a conversation |
| Write up the test run properly | Only our own files change. Findings are already safe in `project/archive/handoff.md` and in this file's notes **Comes up:** Nothing — you parked it |  | Half a day |
| **Two `-figma.md` pages are orphans — no usage doc links to them** — *proposed, 22 Sep 2026* | **Only our own files change.** **Proved 22 Sep 2026:** `estimation-card` and `map-template` each have a tool specification page that its own usage doc never links. The other ten are linked. **Nothing can see this**, and the reason is structural: `check-links.py` asks *does this link resolve*, which is the opposite question, and the four component checks match `<folder>/<folder>.md` by construction so a sub-page is invisible to all of them. **Two fixes, and they are separate decisions.** The immediate one is two link lines. The lasting one is a check that every `-figma.md` is linked from its own usage doc — about ten lines, and the natural home is `scripts/check-all.py`. **Related and already filed:** the row below about a page nothing points at is the same fault in a different folder **Comes up:** Whenever a tool specification page is written |  | Ten minutes for the links, half an hour for the check |
| **Move the "is this file actually a picture" check out of the merge script** — *proposed, 22 Sep 2026* | **Only our own files change.** The check that found the 36 Figma-JSON files saved under a `.png` name **lives inside `scripts/zeroheight-merge.py` and runs only during a merge.** Between merges nothing asks the question, and `check-links.py` cannot: it asks whether a file exists, never what it is. **Measured 22 Sep 2026 by an ad-hoc scan: 1,611 image references, 0 broken, 0 non-pictures — clean today.** That is a snapshot and not a mechanism; the last time the repo was clean on this, 36 bad files arrived from a migration and sat unnoticed. **The work is small** — the scan is a few lines reading each file's first bytes, and it belongs in `scripts/check-all.py` beside the other five **Comes up:** Silently, whenever an image is added by any route other than a merge |  | Half an hour |

**Making "every pillar has a page for a person" an actual rule is a rider, not a
task.** One line in `.claude/rules/project.md`, ten minutes, folded into
whichever of the four human pages above runs first. It is worth adding because
the pattern is visible: `tokens/` and `compliance/` each got a human page only
when something forced one, and `components/` and `figma/` never did. Nothing
says they should, so the coverage is accidental rather than decided.

**Removing the leftover rule numbers in the spacing ruleset is not on this
list.** The file still says "Rules 6 and 7 are unverified" and "Rules 3–5" after
the renaming, which is untidy, but an agent's output doesn't change. It's a
rider on whatever next opens that file, not a task.

## Questions — only you can answer these

**Sorted 23 September 2026 by one filter: does your answer change what an agent
builds, or how its output is scored?** Seven do and sit above the line. The rest
are real questions and not one of them stops a compliant screen being designed,
so they wait until September is past. Order inside each group is unchanged.

**The filter is not the same as importance.** A question below the line can
matter more to the design system than one above it. It is below because no agent
is currently blocked by not knowing the answer.

I'll bring them **one at a time**, with a recommendation.

### Answer these first — seven that change what an agent builds

| Question | Why it blocks a compliant screen |
| --- | --- |
| **The energy filter slider ships on web and was never added to the Figma libraries. Do we get it added, or tell agents to compose it from existing components?** | A design agent asked to build an energy filter has working web code it cannot use and no Figma component to place. Composing it by hand is the failure this project exists to prevent, so "compose it" needs to be a deliberate decision with a declaration rule, not a default |
| **May a generating agent read a token values page, or only the rulesets?** | Raised 12 Sep, sweeping the six token rulesets. `CLAUDE.md` says plainly: *a generating agent reads `*-rules-ai.md`, never the token pages* — the point being that token pages list everything that exists while rulesets list what is allowed. But every one of the six ruleset headers links straight to its values page, and the colour one links to six of them. `check-links.py` never flagged it because it only forbids `-audit`, `-eval` and `-ledger`. So either the rule has an unwritten exception or six rulesets have been handing an agent the forbidden door for weeks. **Recommendation: it is a real exception and should be written down.** A ruleset states which tokens are allowed but not what `Radius/8` resolves to, and an agent that cannot look up a value cannot use the rule. Say so in `CLAUDE.md` — values pages yes, audits never — and teach the checker the distinction. **The alternative is worse:** inlining every value into the rulesets duplicates the token pages and guarantees drift |
| What are the variant axes for? `Filled`, `Circle`, `Square` — the reasoning was not recorded | **The only question here that has already produced a wrong screen.** Every axis defaults to `Off`, so an agent that places an icon and sets nothing gets the bare glyph. That is how run-002 placed a bare `info` where the circled form was wanted. `Circle` and `Square` are the narrow ones and are where that answer lives |
| What exactly belongs to the `Decorative` colour family? Its boundary is too vague to apply | An agent choosing a colour reaches this family with no test for whether its case belongs in it. A boundary too vague to apply is a rule that cannot be followed, and colour is chosen on every screen |
| No rule separates the two middle shadow levels. Both are used by similar floating components | The same shape as the colour one, one layer down. Two levels, one situation, nothing deciding between them — so two agents split it differently and both quote the ruleset correctly |
| Is it still true that there's no live grid layout system? | **Grid and breakpoint have no ruleset an agent may read.** Until this is answered, nothing says how anything is placed on the page, which is the step after choosing every component and every token |
| **`Map template` is built entirely on components the library marks outdated — is this known, and does it need fixing?** Found 22 Sep 2026 while writing the doc | **It makes one ruleset line false today.** `components-rules-ai.md` says the pin sets are *"reached by placing `Map template`"*; placing it reaches the superseded set instead. **Proved by walking all 27 variants, not by sampling:** every variant carries `(❌ OUTDATED) mapPins`, **81 instances**, three per variant, plus `❌ .base_floating_button_group (outdated)`, **27 instances**. The current sets — `mapPinsV2_SL` and `mapPinsV2_IWT`, both registered — are used **zero** times. The doc warns to replace the pins after detaching, which is a warning and not a fix. **Gabriel, 22 Sep 2026: the map is one product's, already implemented, and not a priority.** It sits above the line because the false line is live, not because the map is urgent — move it down if you would rather leave it |

**The DPE colour question that sat here is answered.** It asked who owns the
energy colours and what an agent does when a legally required element has no
compliant component. The premise was wrong: a GSL component does consume those
colours, our own ruleset was the only thing forbidding them, and the French
class-to-colour mapping is now written and verified against web code. The one
loose end — whether Figma's `Energy Tag` agrees — is a task, not a question.

### Everything else — real, and none of it blocks September

### About what September means

| Question | Why it matters |
| --- | --- |
| **May a spec use an iOS component that has no name in the inventory?** `DSGauge`, `DSHighlightCard`, `DSIconButton` and about twenty more exist on iOS only, listed in `components-ios-map.md` | Raised 24 Sep. No rule covers them. Forbidding them means iOS rebuilds by hand what already exists — the failure *reuse before invention* stops. Allowing them puts iOS-only names into a neutral spec. **Recommendation: the spec stays neutral; the iOS agent may swap in one of these where the map says so.** Not tested |
| **Should `status.md` and `project/the-project.md` be rewritten for the iOS demo?** | Raised 24 Sep. Both still name the Figma milestone. `status.md` now says the priority changed; the rest of it is unchanged. `the-project.md` is yours and was not touched. **`status.md` is also 515 lines against a one-screen rule** — its done entries belong here |
| **`project/the-project.md` defines September as two unseen briefs, built and scored, with no rule broken. On 16 Sep you described something wider — document as much as possible, flag every missing piece, keep it platform-agnostic, and cover the process and the mapping. Which one is the objective?** | They are not the same test and they do not finish on the same day. The narrow one is passed by two good runs. The wider one is passed by coverage, and coverage has no end state unless you name one. **The two rank tasks differently too:** under the narrow objective a token ruleset that no run touches can wait; under the wider one it cannot. **I have not changed `the-project.md`** — the September objective is written down and it is yours to move. Raised 16 Sep |
| **Answered 24 Sep: the iOS demo replaces the Figma milestone.** ~~Priority changed 24 Sep: people want generated interfaces, via a design skill that hands a spec to the iOS and Android skills. Does the end-September Figma milestone still stand, or does the platform demo replace it?** | Raised 24 Sep. The two need different next tasks. The Figma milestone needs rule gaps closed. The platform demo needs a spec format and a name map per platform first. Six days remain, so doing both at once is **guessing** at best. Findings behind this are in *Notes*, 24 Sep |

### About how the rules are organised

| Question | Why it matters |
| --- | --- |
| **Is every generation a run, or can an agent be asked to design something outside one?** | Raised 12 Sep, writing the declaration rule. **When nothing fits** now sends a declaration to `compliance/runs/run-<NNN>/report-run-<NNN>.md`, which assumes a run folder and a run number exist. If someone can ask an agent to draw a screen without opening a run, step 4 of that rule is unexecutable and the record vanishes again — the exact failure the task just closed. **Recommendation: every generation is a run.** If no folder exists, the agent opens the next one and writes the brief into it before drawing. One destination, no second answer to maintain, and it costs a folder. The alternative — declaring in the reply when there is no run — puts the record back somewhere that disappears with the conversation |
| **Is a Confluence page written for you, or for an agent?** | Raised 11 Sep, writing *How to write*. That section splits every page into one of two readers and names where each lives — but it names only files in this repo, and the `component-web-ai-docs` skill publishes three pages per component to Confluence. The API spec is plainly for an agent and the decision tree is close behind, so those are covered by inference; the audit report is the one a person opens, and nothing says so. **Recommendation: the audit report is yours and follows the human rules, the other two are an agent's** — confirm it and I'll add one row to the reader table |
| **Should the scorecard also measure how well Figma itself was used — auto-layout, constraints, layer naming, instances rather than detached copies?** | Raised 12 Sep. **Part of it is already measured:** an instance versus a detached copy is `isLibraryComponent`, a bound variable is `isTokenBound`, an overridden internal is `isComponentInternal`. What is genuinely new is craft of the tool — auto-layout, constraints, layer naming, frame width — and none of it is platform-neutral, which is the one property the facts contract was built to have. A Figma-usage question inside the existing scorecard would report `unavailable` on web, iOS and Android forever. **Recommendation: yes, but as a second scorecard, in the same run folder — `report-run-NNN.md` beside `figma-report-run-NNN.md` — and not before end of September.** Nothing has yet been scored against the scorecard that exists; what `run-001` teaches about Figma craft is the input to writing the second one, and writing it first means guessing at the questions |
| **Should Figma get its own ruleset file, instead of a fenced Figma section inside the neutral one?** | Raised 10 Sep while fixing the Legend rule. Today one file holds the platform-neutral "which component" rules plus a fenced section per platform, and the Figma section overrides the neutral part. It works, and it keeps the contract in one place. A separate file would separate them harder but means two files to keep in step. **Recommendation: not before the end of September** — revisit after the deadline |
| **Should the "three things we name that aren't components" table move out of *Platform limits*?** | Same underlying question, found the same day. That table holds "Card grid", "Infinite scroll" and now "Legend" — none of which is a component on *any* platform, so the content is platform-neutral and it is sitting inside a platform section. It is there for a mechanical reason: *Platform limits* overrides *Which component*, so anything inside it wins, which is exactly what a "never place this" needs. Moving it out means restating that precedence explicitly. **Recommendation: leave it until after September**, then decide both organisation questions together |
| **Nothing joins a doc's variant axis name to the registry's property name. Should the two be made to match, or a mapping recorded?** | Raised 18 Sep, assessing decision trees, and **narrowed the same day after finding it half-answered.** *Where* the Figma half lives is already a task — *Put the variant property definitions into the four Figma registries* — and the registry already won, because `figma-sync-component-sets/SKILL.md` step 4 asks for it. **What no row covers is the join.** `button`'s doc names five axes — Emphasis, Size, Context, Icon, Badge — in neutral words, and the registry will name Figma's properties in Figma's words. An agent holding both still has to guess which pairs with which. **The docs must stay neutral; that is settled and not in question.** **Recommendation: record the mapping in the registry, beside the property definitions**, so the doc never learns a Figma word and the agent never guesses. The alternative — renaming doc axes to match Figma — puts a platform's vocabulary into the neutral layer, which the whole repo is built to avoid. Not tested either way |

### About components

| Question |
| --- |
| ~~`Image Ratio` and `Brand Logo` exist in two libraries under different keys — which should an agent use?~~ **Answered 21 Sep 2026 — neither.** `Image Ratio` joined **Never select** that day; `Brand Logo` was already there as a brand asset. Both keys are marked in both libraries, so which key an agent finds no longer matters |
| ~~**`Button Card` — forbid it now, or keep it selectable until a replacement is decided?**~~ **Answered 21 Sep 2026 — forbid it now.** Gabriel: it is available on no platform, so it should not be selectable. **Removal from Figma is separate and still needs investigation.** The three rules that pointed at it — `Button`, `Card` and `Select card group` — now route to `Card` with the whole container as the action, and so do the three docs. |
| **Eight component docs cite you by name and date — does the rule against attributions forbid that, or only change records?** `CLAUDE.md` says who decided a thing and when is repo bookkeeping and that a component doc never carries that record, naming `-audit`, `-ledger`, `-eval` and `-rules-ai` as the only homes. **Eight component docs carry *“Gabriel, <date>”* anyway:** `badge-store`, `burger-menu`, `button-card`, `feedback-thumb-buttons`, `mega-menus`, `menus`, `navigation-bar`, `navigation-bar-app`. **Four of the eight are pages you read and approved**, so either the rule is narrower than it reads — about *change* records, not about sourcing an assertion — or those pages drifted and nobody noticed. **Why it needs settling before the banner task above:** a banner is exactly this kind of sentence, and writing 75 of them under an unsettled rule multiplies the problem by 75. Raised 22 Sep 2026 |
| **`Button Card` and `Button Card Group` should leave Figma — what has to be checked first?** Both are forbidden as of 21 Sep 2026 and neither was ever built. Gabriel asked for an investigation before removal. **Nobody has said what the investigation is:** who still has instances placed, which files, and whether anything downstream breaks when the component set goes |
| **Two stray navigation bars sit in the Patterns library — should they go?** Found 21 Sep 2026 on the `Mega menus` page: an instance of a separate component set named **`.❌ Navigation bar (outdated)`**, and a **detached frame** called `Navigation bar/Default/1024 px/Off` whose name preserves a brand, breakpoint and bottom-bar structure the live component no longer has. Gabriel said to ignore the outdated one for the docs, and the docs do. **Neither is removed, and both still render on the page a designer opens.** |
| **Are the seventeen missing variant combinations in `Map template` and `Estimation card` deliberate?** Read live 22 Sep 2026. **`Map template` is short by nine**, and three rules account for all of them: **web has no dark map** (3), **Android has no closest zoom** (4), **the closest zoom has no price map** (2). The web one is the odd one — web is the only platform missing a style every other platform has. **`Estimation card` is short by eight**: **`Small` has no expanded form** (3), and **`Horizontal` exists as a single variant** — collapsed, selling price, nothing else (5). **Not tested, and nobody has been asked.** Three other short grids were confirmed deliberate on 21 Sep 2026 and are recorded as never to be re-raised; **these seventeen were not among them**, so a session counting either grid today has nothing to read. Both docs say the absences are untested rather than guessing |
| **Can `Estimation card`'s Kind move off `unresolved`?** Its four parts were read live 22 Sep 2026 — price range, confidence indicator, estimation details and feedback module, each its own set with its own axis, reached through 91 exposed sub-instances. **That is not enough to call it composed.** The ruleset defines composed as *assembled from public components*, and all four parts are private helpers on its own page, registered nowhere. So the evidence narrows it to container or all-or-nothing and does not settle it. **Left as `unresolved` on purpose** rather than changed on one session's reading. **The open question in `components-rules-ai-audit.md` since 16 Sep is now narrower, not closed:** it used to be *nothing in this repo describes it*, and a doc now does |
| **The live `Navigation bar` component carries a text node reading "❖ Bad component".** Found 21 Sep 2026, inside a frame named `Alert` in the single variant, on a layer named `Link - AVIV`. **Not established** what it refers to or who put it there — it reads as a linting or review marker rather than content. It is inside the component, so it travels into every instance placed from it |
| Seven components can be selected but are described nowhere. What are they for? |
| ~~Is `Badge` something you pick on its own, or only ever attached to a host?~~ **Answered 18 Sep 2026 — both.** Gabriel: the variants are visual, there to cover the surfaces a badge can sit on. It is mainly anchored to a host such as a tab or a button, and it is **open** to standing alone for a case not covered — a count beside a title. The `Badge` rule row was corrected the same day |
| **Five pages in `charts/` are not component docs. What should they be called, and should the filename grammar say so?** Raised by Gabriel 21 Sep 2026. `charts.md` is a routing page, and `legend.md`, `chart-colors.md`, `chart-accessibility.md` and `filters-and-actions.md` are guidance shared across the chart types. **None has a registry entry**, and `legend` was ruled not a component outright on 10 Sep, when the answer to its sizing problem was to stop calling it one. **`coverage.py` already separates them** — it lists them under *Chart pages that are not registry components* and does not score them, so no measurement is wrong today. **What is wrong is that nothing in their names says it.** Every other file in this repo announces what it is through its suffix — `-index`, `-rules-ai`, `-audit`, `-ledger`, `-eval` — and these five look exactly like a component doc from the filename alone. **Recommendation: a `-guidance` suffix**, leaving `charts.md` as the routing page, so the grammar answers the question without anyone reading the file. The alternative is a folder of their own, which is a bigger move and breaks more links. **Not urgent** — no agent has been misled by it yet, and nothing measures them wrongly **A second half of the same tension, found 23 Sep 2026: `Legend` is a *Choose* row in `components-rules-ai.md`** — the ruleset routes an agent to it, tells it there is no legend component, and says looking for one is the correct thing to refuse. So the name sits in the ruleset as a decision point while naming no component. That is the same trap shape as `Card grid` and the OS status bar, and it works — but it means renaming the file is not the whole answer: **whatever these five become, the ruleset row has to say the same thing.** |
| `Tab Bar` is being rebuilt — confirm agents should not use it meanwhile |
| `Footer` is Figma-only and owned by another team — confirm it stays out of scope |
| Is `Flag` just an asset, or a real component for country and locale? |
| Four documented components are missing from the selection guide |
| Does anything follow "Identity and media" on the live Confluence page? |
| ~~24 components can be selected but have no documentation and no routing entry~~ — **answered by the files, 23 Sep 2026.** Both halves are wrong now. `components-coverage-ledger.md` says **one** selectable component has no doc, `Pop-up`, which you parked. `scripts/check-rules-reach.py` says **all 73 selectable components are named by at least one rule**, so none lacks a routing entry. Nobody updated the question as the facts moved under it |
| How do we catch an agent that hand-builds an empty state instead of using the real one? |
| 29 docs have no `### Platform` section. Is that deliberate, or 29 unanswered questions? |
| Should a section be markable *not relevant*, and by what rule? The coverage ledger has no such mark on purpose |
| ~~`CLAUDE.md` says this repo has no tooling bar two deliberate exceptions~~ — **already done.** It reads *"Three deliberate exceptions"* and names the Node one. The question outlived its own fix |

### About icons

| Question |
| --- |
| What are `One`, `Two` and `Three` **for**? Confirmed 14 Sep as modified Font Awesome numerals — the `3` is redrawn. What they are used for is still unknown, to you too |
| **Which icons were modified after being taken from Font Awesome?** Origin confirmed 14 Sep; `One`/`Two`/`Three` were redrawn. A name diff finds additions, not modified drawings — that needs eyes |
| Is *Furnitures* the right category name? It holds dishwashers and refrigerators — appliances, not furniture |

### About colours

| Question |
| --- |
| Should borders gain a genuinely transparent token? |
| Two surface colours (`Accent/Light` and `Active`) resolve to the same value — deliberate? |
| The colour table still needs completing against Figma |

### About text, shadow, borders and grid

| Question |
| --- |
| The `Display` text style was never seen on any screen we sampled. Is it actually used? |
| Does the "emphasis plus container size" mechanism hold beyond the three screens checked? |
| A text style renders differently from its recorded value — worth telling whoever owns that Figma file |
| `Energy tag`'s border can't be confirmed; the Figma link we were given pointed at `Checkbox` |

---

## Notes — recorded, no action needed

- **The prototype stays throwaway.** Gabriel, 24 Sep: `/prototype` output is for testing, ideation and fast prototyping. Nothing merges to production from it.

Things that turned out to be true and are worth not rediscovering.

| Date | Note |
| --- | --- |
| 24 Sep 2026 | **The iOS and Android skills were read to judge a design skill feeding them a spec.** **Proved by reading the files:** **iOS** is `ios-prototyping` in `~/.claude/skills/`, run by `/prototype` from the `gsl-ios` repo. It takes a plain-text brief, plus an optional Figma link, and builds a throwaway SwiftUI screen with no gates. It reads its own design-system reference in `gsl-ios/.claude/skills/swiftui/references/aviv-design-system/`, 400 lines, and never this repo. **Android** is the `product-coding-guide` agent in `~/.claude/agents/`, run by `/vibe` from `gsl-android`. It **changes existing screens**, asks before each step and opens a draft PR. It is not built to create a new screen from a brief. `figma-to-code` on the unmerged `feat/ai-prototyping` branch builds DS components from Figma, not screens. **No file anywhere joins this repo's component names to either platform's code names** — searched this repo for `DSButton`, `DSComponents`, `incrementalcounter`, `aviv_design_system_ios`, found none. Examples of the gap: `Counter field` is `CounterField` on iOS and `incrementalcounter` on Android; `Modal bottom sheet` is `BottomSheet` on iOS. **Corrected the same day:** a first reading said iOS lacks `Energy tag`, `Listing card` and `Navigation bar`. **That was wrong.** They sit outside `DSComponents/Components/`, in `Patterns/`, `TechnicalComponents/` and `MobileSpecificComponents/` — `DSEnergyTagList`, `DSListingCard`, `DSNavigationView`. **The local `gsl-ios` checkout was also 21 design-system commits behind `origin/develop`** — read the remote, never the working copy |
| 24 Sep 2026 | **`components-rules-ai-audit.md` still recommends the opposite of your 24 Sep decision.** Its table *Where each kind of availability data belongs* says the Figma half lives in the registries, and calls the doc's Figma cell *a hand-maintained duplicate*. You decided the readiness table is the one place availability lives. **Guessing that both can stand:** the registry is the evidence, the cell is what an agent reads, and `check-readiness-figma.py` keeps them in step. The audit was not rewritten, because that change was not approved in the readiness task. **Worth one sentence there when the `:452` line is removed** |
| 23 Sep 2026 | **Eval run 4 scored 60/64, and three gaps it could not close are worth not rediscovering.** The cold agent read `components-rules-ai.md` and nothing else — **one tool use, one file**, which is the evidence the run was actually cold rather than merely instructed to be. Three things it looked for and could not find, none of which is a contradiction and none currently filed as a task: **One, no rule covers a combined language-switch-and-profile panel.** `Menus` is never-select and reroutes to `Navigation bar` *'whose own controls include the language menu'* — **the profile half is addressed nowhere in the file.** That is eval row 34, which it answered correctly and flagged as half-unsupported. **Two, nothing says which control a bare label's info icon sits inside.** The Icons section names the general pattern — `Button`, `Text Button`, `Link`, `Chip`, `Floating Button Group`, or a component's own icon slot — but a DPE label sits in none of those, so the agent had to pick. Eval row 58. **Three, the breakpoint names are used and never defined in the ruleset.** `XXS`, `XS`, `SM` appear in the `Action Menu` and `Modal Bottom Sheet Menu` rows and in `Burger menu`, with no table giving their widths. **Not load-bearing for any of the 64 answers**, which is why this is a note and not a task — but an agent asked to place `Action Menu` at a named width has to look outside the one file it is allowed to read. **Four rows scored as misses and only three were the ruleset's fault** — row 54's intent was written badly and could not isolate `Pop-up`. That is a task row, not a note |
| 23 Sep 2026 | **`Built outside the design system` says every selection is reported; its `Badge store` row says only to place it.** Found 23 Sep while drafting eval intent 43. The section's own rule reads *"A run that selects one should say so in its report"*, and the three rows below it do not say the same thing: `Score tag` and `Feedback thumb buttons` both read *"Select it. Report that you did"*, while `Badge store` reads *"Place it. There is nothing to generate on any platform."* **Two readings are available and they differ** — either the section rule governs all three and the `Badge store` row is just terser, or the row deliberately exempts it because placing a replica of an Apple or Google badge tells nobody anything about adoption. **The second reading is the likelier one and it is a guess, not a fact.** Intent 43 was written to the narrower row: it expects `Badge store` and does not require a report line, so a run cannot fail on an ambiguity nobody has settled. **No check can see this** — it is one sentence disagreeing with another inside the same ruleset, which is exactly the fault class an eval exists to find and `check-rules-docs.py` cannot. Worth one sentence in the ruleset whenever that section is next opened |
| 22 Sep 2026 | **The ten `-figma.md` pages were in no tracker at all, and Gabriel's asking is what found it.** Created the same day and reachable only from their own usage doc — `components-index.md`, the ruleset inventory and the coverage ledger all named none of them. **Now listed** in `components-index.md` under *Tool specifications*. **The generated ledgers are right to ignore them** — `coverage.py` and `template-drift.py` measure the usage doc alone, by design, so a tool page is not a coverage gap and not template drift. **A second staleness surfaced with it:** `README.md`'s *Where to look* row still read *"63 component docs across 61 folders"* and said `charts/` held three pages plus five supporting ones. Reality is **76 docs across 69 folders**, and `charts/` holds eight. Corrected. **Everything else checked out:** all 98 registry names appear exactly once in the inventory, no duplicates; the index's 69 rows match the 69 folders exactly, with no row pointing at a missing file and no folder missing a row; both generated ledgers regenerate to no change. **The lesson is the one that keeps recurring here** — a count typed into a page goes stale the moment a file is added, and only a script or a direct question catches it |
| 21 Sep 2026 | **The web navigation was read end to end in Figma, and the bar itself is the broken part.** All five Patterns entries read live; **the registry matches live on all five**, so none of this is drift in our files. **`Navigation bar` has one variant, and its only property is literally named `Property 1`** — Figma's placeholder for a property nobody named — with the single option `Default`. Its base component is **360 × 72**, a phone width, though the page uses it at 1024 × 80. Its parts are `Alert`, `Content left`, `Content right`. **There are three navigation bars in that library, not one.** The live set; a separate set named **`.❌ Navigation bar (outdated)`**, still instantiated on the Mega menus page; and a **detached frame** on that same page called `Navigation bar/Default/1024 px/Off`, whose name preserves a brand, breakpoint and bottom-bar structure the current component no longer has. **So the desktop half of the web navigation is drawn against two bars that are not the current one.** **`Mega menus` is a content catalogue, not a configurable component** — 96 variants across Brand (7: Default, Immonet, Immoweb, Immowelt, Logic-Immo, Meilleurs agents, SeLoger), Breakpoint (1024 / 1366 / 1536 px) and Menu, whose options are each brand's real menu entries — *SL • To rent*, *IWT • Finance*, *IMT • Real estate appraisal*. One variant is one brand's one menu at one width. **`Burger menu` and `Burger menu (profil)` are structurally identical** — Bottom bar (Off/On) × Breakpoint (320 / 768 px), four variants each — and differ only in content, which is consistent with `profil` being forbidden and `Burger menu` not. **Those two breakpoints confirm Gabriel's account** that the burger menu is the mobile replacement for the mega menu. **`Menus` is the profile menu and the language menu** — Type (Default, Simple) × Content (**Profil**, **Language**) × Brand (6), seven variants of a possible twenty-four. That matches the ruleset's *international content needs* and is the first concrete evidence of what it is for |
| 21 Sep 2026 | **Three incomplete variant grids in the Components library are deliberate — Gabriel, 21 Sep 2026.** Read live the same day. `Badge Store` has 24 variants, not 32: **`Type=Filled` exists in `Theme=Light` only**. `Navigation Bar (App)` has 18, not 24: **`Platform=Android` never carries `Home Indicator=true`**, which is an iOS element. `Pop-up` has 12, not 16: **`Device=Phone` never carries `Orientation=Landscape`**. **None of the three is a bug and none is to be re-raised.** A session that counts a variant grid and finds it short should read this row before filing anything |
| 21 Sep 2026 | **`Feedback thumb buttons` is the only component in the system kept without being adopted — Gabriel, 21 Sep 2026.** Built by a team outside the design system, kept because other teams might want it, never integrated. **It stays selectable**, and the rule is now in `components-rules-ai.md`: choosing it is the signal that more than one team needs it, which is the condition for adopting it properly. A run that selects it should say so in its report. **Proved live 21 Sep:** the set has no `Platform` property, its only axis is `Device` with `Desktop` and `Mobile`, the Figma page titles it *"(Web)"*, and no such component exists in the web code repo. So it is web only, and built nowhere. **One piece of evidence was weaker than first reported, and is corrected here:** a banner reading *"Will be moved to the product library in the future"* sits on that page, and **the same banner sits on the `Feedback bar` page in the Patterns library** — found 21 Sep while writing that doc. It is not specific to the thumb buttons and should not be read as being about them. **Gabriel's ruling stands on his own knowledge, not on the banner.** |
| 18 Sep 2026 | **Two variant flows send an agent to a component that has no doc.** `modal-bottom-sheet` routes a small amount of content to **Pop-up**; `date-picker` routes date entry with no calendar to **Date field**. Both targets are real — they sit in `components-rules-ai.md`'s inventory at lines 502 and 538 — and **both are marked *no doc***, so the agent is routed correctly and lands nowhere. **`scripts/check-links.py` cannot see this**, and should not: neither pointer is a link, it is a component name in prose. **Not filed as its own task** because both components are inside the existing question *24 components can be selected but have no documentation and no routing entry* — this is two named instances of it, and the reason it is worth answering |
| 18 Sep 2026 | **`Floor selection`'s variant flow deliberately claims nothing about appearance, and the reason is a gap.** A first draft said the component has no emphasis, size or style variant. **That was cut before it landed** — it rested on the registry, which records `variantCount: 6` and `Props: Floor type, State, Error` but **not what `Floor type`'s options are.** If `Floor type` turns out to be an appearance choice, the cut line would have been false in the one file an agent reads. **`State` and `Error` are states, not choices**, and belong in `Interactive States & Loading`, where the doc already covers them. So the flow carries two axes, field width and header, and both trace to the doc's own words. Resolving `Floor type` needs a live Figma read and is covered by the existing registry task |
| 17 Sep 2026 | **`dropdown` and `modal-bottom-sheet` merged: 6 images to 36, and 10 to 40, with nothing lost.** They were chosen to test the case `select-card-group` could not — a component where Zeroheight *also* fills sections the repo has. **It found the failure I expected and three I did not.** The expected one: the source re-adds sections the repo moved by hand. `modal-bottom-sheet` printed the scrolling paragraph twice, once under `Scrolling` and once under `Touch Target & Layout`, where it was moved this morning; `dropdown` did the same four times. **Fixed by asking how much of a source section is already on the page, not whether its first sentence is** — checking the first line missed all four, because those sections open with an image. **The three Gabriel found by reading the draft:** a nameless image sitting directly above a named table is the picture that table replaced and has to go, with the caption it carried; a table whose column names the page already has is a replacement, and the check for it was running on only one of two code paths; and Zeroheight writes some sub-titles as a bare short line — `Header`, `Footer` — which need bolding to read as labels. **One near-miss:** the image cleanup deleted the hero on both pages, because a hero also sits directly above a table, the readiness one. That is the third exception the hero has needed |
| 17 Sep 2026 | **A regenerated doc is merged into the repo's, never substituted for it — measured on `select-card-group` before touching anything.** The test: regenerate from Zeroheight, then compare section by section. **The two barely compete.** Zeroheight fills the visual sections and has **nothing** for `When to use`, `When NOT to use` or `Variant Selection Flow` — 88 words written here by hand, including a decision tree no export could produce. The repo's `Related Components` table carries `Priority` and `Example Scenario` columns and repo-relative links; the regenerated one has two columns and `zeroheight.com` links, which the migration exists to retire. **A wholesale replacement would have destroyed all of it.** The merge rules, in order: repo section empty and the source has it → take the source's; both have it → keep the repo's words and add the source's pictures; only the repo has it → leave it; only the source has it → add the section under the parent it sits under *there*, because appending put `Error` after `Accessibility`. **Result on `select-card-group`: 3 images to 30, 0 sentences lost, no new zeroheight.com links.** **One rule came from Gabriel reading the draft:** the hero was being appended rather than replaced, so the page showed two, the wrong one first. A hero is never added to — the same rule the do/don't placer already needed |
| 17 Sep 2026 | **The image gap on the nine near-complete components is closed — 25 missing this morning, 0 now.** The last five needed Gabriel to look, because nothing in the file said where they went. **His answers, 17 Sep:** `snackbar`'s goes in the *iOS/Android — clicking action button or waiting 5 seconds* cell; `energy-tag`'s two are France's **worst** rating and Germany's **second best**, so one appends and one inserts at position two; `wizard`'s is the **Pressed** state of the error step. **`tables`' image is deliberately not added** — it is a whole table shown for display only, and it would compete with the sub-component table that used to be called Anatomy. **One thing his answer exposed:** `wizard`'s Error table had no `Pressed` column at all, while *To do* and *Done* both split the same six states into two tables of three. Error is now split the same way, which also keeps it under the template's five-column cap |
| 17 Sep 2026 | **The images that looked unnamed were do/don't rules, and the placer was reading the wrong field.** A gallery item carries a `name`; a do/don't item carries a **`label`** (Do, Don't, Caution) and a **`caption`**. The placer read `name`, found it empty on every do/don't item, and reported them as unplaceable. **Six were identifiable all along**, and all six sit under `Usage` in the source — which the template calls `### Usage Guidance`. **Two of them were genuinely new content:** `kpi` had nothing at all about not relying on colour alone, and its *Usage Guidance* said `Not documented`. It now carries the rule as a matched DO/DON'T pair. **Three were not new and were nearly added twice:** `snackbar`, `button-card` and `segmented-control` already carried the same sentence with a different picture, so adding a row would have printed the rule twice on the page. Caught in review; they are image swaps, and the placer now checks whether the caption is already on the page before adding anything |
| 17 Sep 2026 | **48 component images were swapped for the Zeroheight version, on Gabriel's call that Zeroheight has not changed in weeks and is therefore the reference.** Reviewed on a published page showing both pictures side by side, 52 pairs across 11 components; he marked swap on all of them. **Matched by column name, never by filename** — the doc's table says `\| Default empty \| …` and the source's gallery names an image `Default empty`, which survives the three filename corruptions the migration introduced. **The first attempt at applying them was wrong and was caught in a dry run:** replacing the image reference as text changed every cell using that file, and the docs reuse one picture across cells — `button-card` uses a single file in **seven** of them. Each swap is now addressed by line and column index instead. **What it fixed, visibly:** `toggle`'s *Tooltip* column had been showing the *Optional* image, the same file twice in one table. **27 files are now unreferenced** and are filed as a task, because deleting always asks first |
| 17 Sep 2026 | **Four of the six entries in `components/missing-images.md` are fixable from Zeroheight, and I said earlier they were not.** The earlier check compared **hashes** and found no match, which was true and useless. **The right key is the column name.** A doc's table says `| Default empty | …` and the source's gallery names an image `Default empty`; that is the match, and it works through a renamed file. **Fixed on 17 Sep: `progress-bar` 2, `snackbar` 1, `text-field` 1** — all four cells held a remote Confluence attachment URL, and now hold a local file. Two are left, `tag` and `phone-number-field`, both standalone images with no table and so no column name to match on. **Why the hashes never matched: the filenames are corrupted, in three different ways.** An SVG saved as `name.svg.png` by my own extractor; a leading underscore dropped by the Confluence migration (`_YsTHgKXfLGgIAn8KlxOBA` → `YsTHgKXfLGgIAn8KlxOBA`); and a character inserted, `0dfceb07a896e570f3ee01c` in the repo against `0dceb07a896e570f3ee01c` on Zeroheight. **`missing-images.md` now overstates the problem and needs correcting — not done, it is Gabriel's call** |
| 17 Sep 2026 | **Gabriel's premise about the docs held, and it is what made image placement possible.** He pointed out that the repo docs are reorganised Zeroheight content, so the text beside an image in the source should still be in the doc. **Measured on the nine smallest gaps: 21 of 25 missing images were anchored by their own surrounding paragraph**, and the four that were not are page heroes, which the template already places. So finding where an image goes is solved. **What is not solved is editing a table that is already there.** Joining an image to the table holding its siblings was matched by image hash, and the source reuses one image across blocks — `toggle` came out as `Left \| Hover \| Right \| Disabled selected`, two positions and two states in one row saying something false. **Caught in review, reverted, and the step was dropped rather than repaired.** 10 images placed safely; 15 left to a person and filed as three tasks. **Three narrower rules came out of the same review:** one image is not a comparison table, a set whose names are all identical has a header that tells the reader nothing (`\| Closing \| Closing \|`), and a second hero is a replacement rather than an addition |
| 16 Sep 2026 | **Every component doc now matches the template. 57 of 57, from 8 this morning.** Finished 16 Sep in one pass after the decisions were agreed. **The pass was not run by name, and the reason is the whole lesson:** the drift report proposed one destination for each of the eight `### Interaction` sections and one for each of the four `### Scrolling` sections. **Reading all twelve showed nine were wrong.** `checkbox` and `checkbox-group`'s *Interaction* says the whole row is clickable and the row height is 48px — that is a touch target. `button-group`'s describes single versus multiple selection — a variant. `card`'s says cards are not clickable and a cell content goes inside — usage prose, which is where Gabriel put it. `action-menu`'s *Scrolling* recommends against scrolling at all — usage guidance. `top-bar`'s is a fixed-versus-scrolls comparison table — a variant. **17 sections moved, 22 absent sections written as `Not documented`, 3 readiness tables given their Figma column** (each doc carries a figma.com link, which is the evidence the rule asks for). **Two docs had to be rebuilt by hand:** `modal-bottom-sheet` and `phone-number-field` carried behaviour sub-sections with no `## Behavior & Responsiveness` above them, so inserting the missing H2 in file order swallowed them into *Variants & Modifiers*. **What is left: nothing on the components.** Five off-template headings remain, all on chart support pages, which document a part of a chart rather than a component — whether the template applies to them at all is still undecided |
| 16 Sep 2026 | **`Content & UX Writing` now has a fixed core and a free slot, and the three core rules are required of every component.** Gabriel's decision, 16 Sep, taken after measuring all 57 docs: **43 say something there, 16 say nothing**, and what they say runs to **50 distinct labels of which 35 are used exactly once**. The template's own three — `Capitalization`, `Length Limits`, `Label Formula` — were the top three at 19, 19 and 17 docs, so they cover **fewer than half** of the pages that wrote anything. **Neither a flat list nor a free slot alone could hold that**: the three are the questions every component can answer, and the tail is each component's real writing rules — `rating` needs number notation per language, `energy-tag` per-country wording. **The shape adopted is the one `Variants & Modifiers` already uses**: fixed part, plus `### [Writing topic]` taking any name. **Why required matters for a later audit:** an audit compares a claim to reality and can only do that if it knows where the claim lives. Three fixed questions give **171 checkable facts** across the library; a free-form section gives none. `coverage.py` now reads a `<!-- required: -->` marker from the template and scores each of the three as its own column. **The cost, stated up front:** the ledger fell from 70% to 62% filled, because `Label Formula` is answered by 12 of 57, `Capitalization` by 18 and `Length Limits` by 19. Nothing got worse; it became visible |
| 16 Sep 2026 | **The same heading name means different things in different docs, and this is the third time it has caught me out.** Found 16 Sep while deciding whether to ban `Overflow content` from the new writing slot. **`text-area`'s says** content exceeding the vertical space is handled by the resize handle or by scrolling — behaviour, and misfiled. **`radio-button-group`'s says** labels should be under 3 words and should wrap onto two lines rather than ellipsis — genuinely writing, and correctly placed. Same two words, two different sections. **The earlier two instances were the same shape:** `### Width` and `### Labels` looked like misfiled layout and writing sections and were real variant categories; `Device` and `Horizontal scroll` looked like breakpoint and layout sections and are `tables`' own declared variants. **The rule that follows: never classify a heading by its name alone.** The drift report proposes, a person reading the section decides. No exception list was added for `Overflow content`, deliberately |
| 16 Sep 2026 | **Template drift is 36 headings, not the 115 first reported, and the report was wrong three times before it was right.** Each correction came from reading the docs, never from re-reading the script. **First: 64 false positives.** The template writes one H3 as `### [Variant Category Name]` — a slot that takes any name — and the report compared every heading against a fixed list of titles, so `avatar`'s `Shapes and sizes` was flagged for using the template correctly. **Second: 9 more, from over-correcting.** The fix was then made to reject a free-slot name that collides with a section title, on the theory that `### Labels` under *Variants & Modifiers* was writing guidance in the wrong place. All nine were read on 16 Sep and **every one is a real variant category** — `progress-bar`'s `Width` holds a Fixed / Full width comparison, `energy-tag`'s `Country` holds France and Belgium. The collision is wording, not misfiling. **Third: the scripted pass does not exist.** It was written, dry-run and thrown away — in every case the heading belongs under a *different parent*, not one level down where it stands. **Where it landed: 57 component docs, 29 match the template, 28 need something** — 9 misfiled sections, 25 renames-and-merges, 2 link lists. **The lesson for the next report of this kind:** a name means different things in different places, and a rule applied to names alone over-reports |
| 16 Sep 2026 | **`Anatomy` will not become a template section.** Gabriel's decision, 16 Sep, asked because three docs use the heading and `Listing summary` needed somewhere for its eight sub-components. **His reasoning: a component's elements can be shown or hidden and often cannot all appear at once, so a picture of every part at once misleads.** What those sections actually hold — which sub-components exist, and what each can be set to — is **Modifiers**, which already exists. Written into `template-drift.py` as a mapping, so the report says *rename to Modifiers* rather than flagging a gap |
| 16 Sep 2026 | **Two of the three `unresolved` components are settled, and the evidence was their new docs.** Both were generated from Zeroheight on 16 Sep; Gabriel agreed the finding the same day. **`Listing summary` is composed** — its anatomy table names eight sub-components (thumbnail, tags, price tag, title, feature list, location, helper text, action), each with its own enable/disable, quantity and size rules, which is the definition the ruleset gives for *assembled from public components*. **`Floor selection` is all-or-nothing** — its doc names exactly one part, a header carrying label, asterisk, tooltip and helper text, and one part is not an assembly. **What changed for an agent:** nothing for `Floor selection`, which was already told to place it whole — but it now says so because somebody checked, not because nobody looked. `Listing summary` moves into the parts-counting test. **`Estimation card` stays unresolved**: no doc, no Zeroheight page, nothing in this repo describes it. Written into `components-rules-ai.md`, `components-audit.md`, and the open question in this file is closed |
| 16 Sep 2026 | **The Figma column in a readiness table answers a different question from the other three, and Gabriel settled which.** Asked 16 Sep, because Zeroheight's readiness line carries Web, iOS and Android only and the generated Figma cell was falling back to `Not documented`. **His answer: Figma means the component exists in a Figma library — if it does, a check.** Web, iOS and Android mean it is built on that platform. **Generated docs now derive it:** a `figma.com` link under the table is the evidence, so the cell reads `Ready ✅`. The rule is written into `components/component-template.md` under the table, so a human filling one by hand reads the same thing |
| 16 Sep 2026 | **The Zeroheight `Floor selection` page describes counter fields in its own Content section.** Spotted by Gabriel 16 Sep, reading the first generated draft. The sentence is *"Counter fields should always have a label, to help the user understand what information to enter."* **Proved verbatim from the source** — present in the extractor's raw block list and in the page's own DOM, so nothing was rewritten in generation. **Guessing it is copy-paste from the Counter field page**, which the same page's Related components table links to. **Corrected to "Floor selections" in the doc that ships**; the Zeroheight page itself is not worth fixing, since it is being discontinued. **Worth carrying forward:** a generated doc inherits whatever the source got wrong, and only a human reading it catches that |
| 16 Sep 2026 | **Zeroheight documents only 2 of the 25 selectable components that have no doc here.** Checked 16 Sep against the styleguide's own INDEX page, which is a table of every component with a link per column — Figma, Documentation, Dev Specs, Storybook. **Proved by reading that table's 89 rows:** `Floor selection` and `Listing summary` have a Documentation link; 18 of the 25 are listed with a Figma link and an empty Documentation cell; 5 are not in the index at all — `Burger menu (profil)`, `Date Field`, `Filter button`, `Map template`, `Button Card Group`. **What it means for the gap:** Zeroheight is not the source for 23 of the 25. Figma and the web code are. **Worth keeping:** that same INDEX table carries a Figma link for the 18, which is the starting point when those docs get written |
| 16 Sep 2026 | **The Zeroheight-to-Confluence route lost roughly three quarters of every component's images, silently.** Measured 16 Sep on `Date picker`, as a control before building the direct extractor. **Proved by counting:** the live Zeroheight page holds **70** unique content images; `components/date-picker/images/` holds **20**, of which 16 are still on the live page; `date-picker.md` references **12**. **Why it went unnoticed:** nothing ever compared the two counts, and a doc with 12 images looks complete. **Guessing this is not specific to Date picker** — the same route produced all 55 docs, and `text-area` showed the same shape, its Usage DO image (`95d51eab3636afd009f992`) present on the live page and nowhere in this repo. **Not filed as a task** because re-extracting 55 components is Gabriel's call on scope, not a desk job |
| 16 Sep 2026 | **A Zeroheight page cannot be read without a browser, and cannot be read correctly without scrolling the right element.** Learned 16 Sep building `scripts/zeroheight-extract.mjs`. Three traps, each of which fails silently rather than erroring. **One:** the page is client-rendered, so `curl` returns a 200 with an empty shell — no error, no content. **Two:** images load only on entering the viewport, and the page scrolls inside `.page--wrapper`, not the window; `window.scrollBy` reports success and loads nothing, which yielded a 2-image page where there were 20. **Three:** there are **two** editor content containers — a 53-character intro and the real body — so `querySelector` takes the first and returns a one-sentence document. All three are handled in the script and written into its header comment |
| 16 Sep 2026 | **`Top` icon alignment with an image placeholder has no rule, and Gabriel confirmed none exists.** Asked 16 Sep while updating Cell content. The Figma property `Placeholder left alignement` governs the `.placeholder left` slot, whose own `Type` is `Image` or `Icon` — **proved live**. The spec Gabriel wrote describes only the 24×24 icon: centre it on the first line of text. An image is taller than a text line, so the same wording cannot mean the same thing. **His answer: an image can stand in for the icon to build a list or a similar row, and there is no further specification.** `cell-content.md` now says so in those words, so a reader stops looking rather than inferring one |
| 16 Sep 2026 | **The FigCli plugin window spins on "Scanning..." forever when the daemon is down, and never says so.** Found 16 Sep after Gabriel reported the plugin loading indefinitely. **Proved:** `ps` found no process for the pid in `~/.figma-cli-daemon.pid`, nothing was listening on port 3456, and `daemon status --debug` said *Not responding*. **Why it looks like a hang rather than a failure:** `plugin/ui.html` scans ports 3456-3460, and on finding nothing it silently retries every 1.8 seconds with no timeout and no error state. A dead daemon and a slow one render identically. **Why the daemon was down:** it shuts itself off after 60 minutes idle, by design, so any morning after a quiet evening starts this way. **The fix is two steps** - `cd ~/figma-cli && node src/index.js daemon start`, then Gabriel closes and reopens the plugin. Restored on 16 Sep; the stale pid file corrected itself once the new daemon won the port. **The opposite trap already recorded on 14 Sep still holds:** when the *plugin* drops because Figma switched files, `daemon status` reports healthy and lies. The two failures need different tests - `daemon status` catches a dead daemon, an `eval` catches a dead plugin |
| 16 Sep 2026 | **Every Figma link in `components/` points at the retired Gemini library file, not the live GSL one.** Found 16 Sep while reviewing `cell-content.md` against a URL Gabriel pasted from the live file. **Proved by grep:** 40 component docs contained the file key `xxqSJcKOphrgimxRQbvtfe` (`2.-Gemini-Components-Library`) and **0** contained `ABqcGx0cmJWozuJ8OoW6f2`, which is the key `figma/figma-libraries-registry.json` records for the Components tier. `cell-content.md` was fixed the same day as part of its own task, so the count now stands at **39 and 1**. The node IDs in those links are from the old file too, so they are not repairable by swapping the key alone. **Why the link checker never saw it:** `scripts/check-links.py` resolves repo-relative paths, and these are external URLs. **Same family, not yet counted:** 43 docs still link to `zeroheight.com`, the source the migration was meant to retire. **Not filed as a task** because fixing 40 links needs one live node-ID lookup per component, which is Gabriel's Figma session, not a desk job |
| 16 Sep 2026 | **`tokens/grid/` was moved under `tokens/breakpoint/` in the working tree, uncommitted, and it broke seven links.** A plain `mv` at 09:07 on 16 Sep — **proved** by `tokens/` and `tokens/breakpoint/` changing mtime in the same second, while the file's own mtime stayed at its 4 Sep checkout. The moved copy was byte-identical to `main`, so nothing was ever at risk of being lost. **Nothing else was updated:** six pages still pointed at `grid/`, and the file's own `../spacing/` link broke because it now sat a level deeper. **Reverted the same day** on Gabriel's go; the link checker went back to 0 broken across 1,496 links. **Guessing at the motive:** the backlog task *Give grid and breakpoint a ruleset an agent is allowed to read* — grid values are stated per tier, so nesting grid inside breakpoint looks natural. **Why the flat shape wins anyway:** `.claude/rules/token-docs.md` names twelve peer category folders, and the precedent already exists — `spacing-rules-ai.md` covers sizing while `sizing/` stays its own folder. One ruleset over two categories has never needed nesting. **The breakage a link checker cannot see:** that same rules file lists `grid/` in prose, not as a link, so any future move leaves it silently wrong |
| 16 Sep 2026 | **The Confluence page *Component Documentation Coverage Audit* is stale and disagrees with this repo.** Page `3480193375` in ADS, written 21 Aug, is the page the local coverage ledger was modelled on. **Four of its columns are wrong for every repo doc.** It marks *When to use*, *When NOT to use*, *Variant Selection Flow* and *Related Components* as missing on most components; measured 16 Sep, all 55 documented entries fill all four. **Why they differ:** it audited the live Confluence pages, and the Zeroheight-to-Confluence migration filled those sections afterwards. **Two other differences:** it lists 50 components against this repo's 55 documented entries — `Listing card`, `Phone number field` and `Tables` are absent from it — and its ➖ *Unrelevant* marks are inferred from component type, which the page itself states. **Not filed as a task** because whether that page stays alive is Gabriel's call: `components/components-coverage-ledger.md` now answers the same question from files rather than judgement |
| 16 Sep 2026 | **Two different things in this repo are called *platform*, and they measure differently.** Found 16 Sep while building the coverage ledger. The `Figma \| Web \| iOS \| Android` readiness table sits at the top of **52 of 55** docs. The `### Platform` section under *Usage* — prose saying a component is restricted, such as *"pagination is only used on the web"* — is in **26 of 55**. A count of one is not a count of the other. The ledger now reports them as separate columns for that reason |
| 16 Sep 2026 | **A bad score has four possible causes and the ledger can name two of them.** Raised by Gabriel after testing Figma's own internal design agent on 16 Sep. A screen can be wrong because the documentation is wrong, because the agent executed badly, because the prompt was badly written, or because the platform's tooling could not do it. **Only the first two have a verdict**, so the other two get filed against a documentation page that did nothing wrong. His proposal: one input agent asks the user questions, writes a platform-neutral spec, then emits each platform's prompt — consistent wording, one place to update, comparable runs. **Assessed the same day. What is true:** freezing the prompt removes one of the four causes, and the spec is the first design step, *Define the content*, which has nothing in it today. **What the proposal claims and does not deliver:** the per-platform difference does not vanish, it moves into the emitter; and tooling stays uncontrolled, because identical words still hit different capabilities. **What it risks:** the input agent becomes an unlogged variable of its own, so every run before a change to its questions stops being comparable — the spec and the generated prompt have to be saved into the run folder, the way the brief already is. **Split into four rows** — three in *Scoring a finished screen*, one in *Define the content* |
| 16 Sep 2026 | **The colour-description generator has no home in this repo.** The 227 descriptions were produced by a script in the session scratchpad and written straight to Figma, so nothing here can regenerate them. **Why it matters:** the rule the whole architecture rests on is *what is pushed to Figma is generated, never hand-edited* — and today there is nothing to regenerate from. The moment a token page changes, Figma silently stops matching. **Not filed as a task yet** because where a generator belongs is undecided: `scripts/` holds the two that exist, but neither writes anywhere |
| 15 Sep 2026 | **All 227 Brand colour variables now carry a description, written from this repo on 15 Sep.** Generated from the seven colour token pages, then written to the `AI - Assets description` branch of Foundations. Verified after the write: 227 of 227 described, nothing described outside the Brand collection, and twelve spot-checks matched the generated text exactly. **The branch is not published — that is Gabriel's call.** |
| 15 Sep 2026 | **Variable names are not unique in the Foundations file, and a script that addresses a variable by name will write to the wrong one.** Proved the hard way on 15 Sep: **1,010 variables are prefixed `Color/`**, not the 227 in the Brand collection, and `Color/Symbol/SkinColors/Skin100` exists **twice**, in two different collections. A first write pass used `vs.find(x => x.name === …)`, which returns whichever copy the API lists first — **147 variables outside the Brand collection were given descriptions that did not belong to them.** All 147 were cleared, and the write was redone addressing each variable by its id. **The rule for every future script: resolve the collection first, capture ids, then write by id. Never by name** |
| 15 Sep 2026 | **The FigCli plugin commits only one variable write per `eval` call. Every write after the first in the same call is silently dropped.** Found 15 Sep: a batch of 40 name-and-description pairs wrote exactly one, six batches wrote exactly six, and re-fetching each variable by id inside the loop did not help. **The working shape is one HTTP call per write** to the daemon's `/exec` endpoint, which needs the `X-Daemon-Token` header — the token is at `~/.figma-ds-cli/.daemon-token`. At that rate 227 writes take about a second, so this is a shape constraint rather than a speed problem. **Second trap in the same family:** the plugin wraps submitted code in an async function and infers where to put the `return`. A trailing `if/else` block defeats that inference — the code still runs, but the result comes back empty, so a loop reports zero successes while having written something |
| 15 Sep 2026 | **The colour descriptions are already written. 209 of the 227 Figma variables are covered by rows that exist today.** Checked 15 Sep by matching every `Color/*` variable name in the Brand collection against the seven colour token pages, which hold **383 rows** between them in the form *Token · When to use · Don't use for · Used by*. **64 have an exact row, 145 are covered by a parent row, 18 have none.** **All 18 uncovered are in families the ruleset already forbids** — 12 `Symbol/SkinColors/*` and 6 locked `Native/*`. So nothing a designer may legitimately bind is undescribed. **What this changes:** pushing descriptions into Figma is a mapping job, not a writing job. The only authored text needed is *Not for use* for the 18, and a state line for leaves whose parent row does not distinguish Default from Hover and Pressed |
| 15 Sep 2026 | **The 227-versus-218 gap is probably the nine `Native/*` tokens, and the arithmetic fits exactly — but it is not proved.** The audit's 218 is counted from the **web code repo**, `brands/<BRAND>/figma/colors.{light,dark}.json`, not from Figma. Figma's Brand collection holds 9 `Native/*` colour variables, and 218 + 9 = 227. `native.md` documents three Native roots, which expand to more leaves in Figma. **Guessing**, and cheap to settle: compare the 227 Figma names against the 218 keys in the code repo and list the difference |
| 15 Sep 2026 | **The `3 - Brand` collection holds 264 colour variables, and 227 of them are the semantic `Color/*` tokens a designer binds.** Counted on the branch, 15 Sep. The other 37 are locked component-level variables — `Component (🔒)/Accordion v1` 18, `Accordion v2` 18, `Footer` 1. **The 227 break down as** Surface 99 · Content 45 · Border 27 · Symbol 22 · Scales 19 · Native 9 · Background 6. Names match the ruleset exactly — `Color/Surface/Default/Default` is the repo's `surface.default.default`. **Gabriel, 15 Sep: Brand is the only collection to describe.** Mode is for switching mode, never for picking a colour; Breakpoint and Platform are Figma-only and have no counterpart in code |
| 15 Sep 2026 | **Figma shows 227 semantic colour tokens; the colour ruleset and audit say 218. Nobody has explained the nine.** Found 15 Sep while counting the Brand collection. The ruleset's *"73 of the 218 tokens have no consumer"* and *"all 12 brand and mode combinations define the same 218 token keys"* both rest on the smaller figure. **Not tested:** whether the nine are new tokens added since the audit, tokens the audit excluded deliberately, or a counting difference. Worth settling before any description is generated from a per-token list, because a generator keyed on 218 would silently skip nine variables |
| 15 Sep 2026 | **Thirty-one of the 227 Brand colour tokens are ones the ruleset forbids outright** — 22 `Symbol/*` and 9 `Native/*` — and today they carry no description at all. Together with `Border/Focus`, `Surface/Decorative/*` and the unreachable status leaves, that is most of the deny-list sitting unlabelled at the exact moment a designer picks a colour. **Why it is worth recording:** a description is the cheapest possible guardrail. A rule in a document has to be read before the mistake; a description is read during it |
| 15 Sep 2026 | **A Figma variable's description can be read, written and restored from the terminal — proved on the branch, 15 Sep.** Run through `~/figma-cli` in Safe Mode against `AI - Assets description`, a branch of the Foundations library. The test read `Modes/Border/Subdued/Default`, wrote a marker, read it back, then put the original value back and confirmed the restore. `hasDescriptionProperty: true`, `writeWorked: true`, `restoredCleanly: true`. **Component descriptions were already proved by reading the code** — `setComponentDescription` at `~/figma-cli/src/figma-client.js:5026`, Markdown supported. **What this unblocks:** guidance can be generated from the rulesets in this repo and pushed into Figma, which is the architecture Figma's own agent recommends. It is also the first time anything here would **write** to Figma; all four `figma-sync-*` skills only read |
| 15 Sep 2026 | **Not one colour variable in the Foundations library has a description. Zero out of 2,412.** Counted on the branch, 15 Sep. So the asset-description layer Figma's agent recommends as the primary guidance surface is currently empty for colour |
| 15 Sep 2026 | **Figma holds 2,412 colour variables, not 218.** The repo's 218 is the count of semantic token keys; the Figma file carries far more rows. Five named collections: `1 - Primitive` 440 · `2 - Mode` 388 · `3 - Brand` 514 · `4 - Breakpoint` 130 · `5 - Platform` 179. **A further 1,323 colour variables sit in eight collections the plugin API will not name** — `getLocalVariableCollectionsAsync` returns only the five, but the variables reference eight more ids. Their names are prefixed `Modes/` and carry brand emoji, for example `Modes/Border/On secondary/🔴 SeLoger/Disabled`. **Not tested:** whether those are legacy, hidden from publishing, or something else. **Why it matters:** any plan to describe "the colour tokens" must first say which collection a designer actually binds from, or it describes 2,412 rows instead of a few hundred |
| 15 Sep 2026 | **A `5 - Platform` variable collection exists in Foundations, with Web, iOS and Android as modes** — 179 variables, 38 of them colour. Found 15 Sep while counting collections. Worth knowing before any claim that this repo's platform-neutrality has no counterpart in Figma: the library already models platform as a mode |
| 15 Sep 2026 | **Figma now blocks the synchronous variables API.** `figma.variables.getLocalVariables()` throws *"Cannot call with documentAccess: dynamic-page"*. Every script must use `getLocalVariablesAsync`. **Also:** the FigCli plugin wraps whatever you send in an async function, so top-level `await` works — but a script that **opens with a comment line** breaks its pattern match and returns nothing, silently, with exit code 0. Both cost a round of debugging on 15 Sep |
| 15 Sep 2026 | **Six questions on the process map are decided by a designer and a PM together, and nobody has said who has the final word.** They are: which Confluence space a context page goes in · which template it uses · how the UX hypothesis is worded · what status a prototype has · user test or A/B test · what we change after release. **The workshop board does not say, and neither does the France Product Playbook** — it states ownership per phase, not per question. **Gabriel, 15 Sep: this is not his scope and not his decision.** It is a conversation with management, not a question he owes an answer to. Recorded so the map's *Design + Product* value is understood as a real state of affairs rather than an unfinished cell |
| 15 Sep 2026 | **Seven of the strategy brief's eighteen AI use cases have no step on the workshop board, and six of those seven are design-system work.** Found while building `project/design-process-map.md`. Unmapped: UX writing and states, localisation, responsive versions, design-system documentation, conformity audit, contextualised design-to-code, visual design-code QA. Only *utilisateurs synthétiques* among the seven is not Gabriel's. **Why it matters:** the designers' process map stops at *Prototype*. Everything between the prototype and the shipped screen is unmapped by the guild and already owned by this repo, which is the clearest argument for where Gabriel takes the lead. **Not tested:** whether the guild would agree those steps are missing, or would say they happen inside the *Prototype* card |
| 15 Sep 2026 | **The board holds nothing at all under *Iteration*** — no task, no tool, no pain point, no ROI. **Guessing:** either it was never discussed on the day, or nobody does it. Worth one question to the designers before any roadmap claims to cover follow-up |
| 15 Sep 2026 | **The ROI row is empty for every sub-step of *Validation*, and for all of *Follow-up*.** So the two steps closest to proving value are the two with no stated benefit. Recorded rather than filled in — inventing the missing cells would put words in the designers' mouths |
| 14 Sep 2026 | **The Font Awesome origin is confirmed, and confirming it made the shortcut smaller rather than bigger.** Gabriel, 14 Sep: the icons come from Font Awesome **and were then modified** — `One`, `Two` and `Three` are redrawn, and the `3` is not the glyph Font Awesome draws. **Why that matters more than the origin does.** The hope behind the 33-of-33 name test was that Font Awesome's own catalogue could describe ~440 icons for free. It cannot. A name diff finds the icons GSL *added*; it cannot find an icon GSL *redrew*, because the name is unchanged. So reading the catalogue to learn what a GSL icon depicts would be right most of the time and **silently wrong an unknown number of times**, which is the worst shape a reference can have. **One modification was found by looking at three icons**, which says nothing reassuring about the other 451. **What it changes:** the open question is no longer *is this Font Awesome* but *which drawings were changed*, and that needs a visual pass rather than a text one. It is also why the provenance is recorded in `icons/icons-audit.md` and deliberately kept out of `icons/icons-rules-ai.md` |
| 14 Sep 2026 | **Figma is reachable from any session on this machine, and two earlier statements in this conversation were wrong about that.** `~/figma-cli` was found live on 14 Sep — `node src/index.js canvas info` returned a real answer while the icon work was being written, which means the FigCli plugin was already running in Figma. **What had been said twice, and was wrong:** that this session had no Figma tools and that the icon re-sync needed a different session. It needs the **Foundations library open**, not a different session. **How the mistake happened:** the check made was *are Figma MCP tools loaded in this session*, and the answer was no. That is a real answer to a narrower question than the one that mattered, which was *can this machine reach Figma at all* — the same shape of error as the two icon-naming measurements the same day. The access paths are recorded in the session memory note `figma-reachable-without-plugin` |
| 14 Sep 2026 | **The icon names are not as clean as the first measurement said, and the correction matters more than the original claim.** The first pass on 14 Sep checked uniqueness and near-collisions, found 454 unique names out of 455 and 21 near-identical pairs, and reported the naming as being in good shape with about eight names needing a human. **Uniqueness held. Convention compliance did not.** A second pass found **fifteen names that break lowercase-kebab** — seven with capitals, five using spaces instead of hyphens, and **three carrying a trailing space**. The trailing spaces are the real problem: `assistance `, `virtual-staging ` and `two-three-dimensional-draw ` are invisible in every rendering of themselves, so an agent matching `assistance` fails while appearing to have looked correctly. **Why this is recorded rather than just fixed:** the first number was produced by a check that only asked *are these names unique*, and that question cannot see a trailing space. The lesson is about the check, not the icons |
| 14 Sep 2026 | **Run-002 produced two more findings after its eight were ruled, both `ruleset gap`, both on one `info` icon.** Gabriel spotted them on 14 Sep, looking at the finance breakdown rows. **Nine: the right icon, the wrong variant.** The `info` icon carries `Filled`, `Circle` and `Square` axes and **all three default to `Off`**, so placing it and setting nothing gives a bare glyph. The circled variant exists and is what `Text Field` uses beside its own label. **Ten: the icon stands alone, with no button around it.** No states, no hit area, no accessible name. **Why neither is an agent error:** 455 icons sit in the registry with **zero** documentation — no page, no ruleset, no usage entry — and **zero** accessibility rules exist across all seven rulesets. There was nothing to read and nothing to get wrong. Both became tasks the same day |
| 14 Sep 2026 | **No ruleset in this repo contains a single accessibility rule, and nothing had noticed.** Checked cold on 14 Sep while ruling run-002's icon finding: `components-rules-ai.md` and all six token rulesets were searched for *accessibility*, *hit area*, *touch target*, *44px* and *48px*. **No hits, anywhere.** The one place a contrast figure appears is the `Surface/Dark` note in the colour ruleset, which cites WCAG 1.4.11 for a single token — and the CO2 letter rule added the same day. **This is broader than icons.** Run-002's bare info glyph is the first case where it produced wrong output, and it will not be the last: the scorecard asks nothing about accessibility either, so an inaccessible screen scores clean. Not yet a task, because what belongs in a platform-neutral ruleset and what belongs to each consuming agent is an open question |
| 14 Sep 2026 | **Run-003 is the first run to fail nothing.** Same brief as run-002, scored cold against the same sheet. Twenty-three library instances, every styled value token-bound, all five hand-built elements carrying complete declarations. Run-002's three failures are all fixed: the chart's internal spacing untouched, the partner offer declared, `Energy Tag` on the energy rating only. Five items still need your ruling, two of them the restricted scale colours you already accepted once. **What this does not prove:** the scorecard asks nothing about content, copy, or whether the layout is any good |
| 14 Sep 2026 | **One *Blocked by* in *Put them on the screen* is stale.** The row about recording Figma text-field names still says a session must run off `~/.claude/` with Figma Desktop open, because the plugin lives in that config only. Both halves are now wrong — the Desktop Bridge is retired, and `figma-cli` is machine-wide. The same wrong precondition was corrected in `generate-and-score` on 14 Sep. Left as it stands rather than rewritten mid-run |
| 14 Sep 2026 | **The icon names are in better shape than anyone assumed, and that shrank a task from weeks to half a day.** Measured on 14 Sep, not estimated: **454 of 455 names are unique**, they are already grouped into 17 categories, and a full pairwise comparison found **21 near-identical pairs** — most legitimately distinct, like `arrow-left` against `arrow-up-left`. Only about eight need a human. **Gabriel's point, and he was right:** the names already carry most of the meaning, so documenting icons is a pass that flags the ambiguous ones, not a catalogue of 455 entries. **Two are outright defects rather than ambiguities** — `light-bulb`/`lightbulb` is one icon under two spellings, and `maginifying-glass-spark` is a typo. Both need a Figma rename, and both are on the task list |
| 14 Sep 2026 | **A tag and a scale showing the same rating twice in one container is a quality problem, and this repo has nowhere to put it.** Ruled by Gabriel on run-002's `Energy tag` finding, 14 Sep. The agent placed an `Energy tag` reading *A* directly above a seven-step ladder whose *A* step was already raised — the same fact, stated twice, ten pixels apart. **The tag itself is defensible:** `energy-tag.md` calls it *"a standardized, at-a-glance indicator of a building's energy performance"*, and a green or red tag is easier to place than a yellow or orange step on a ladder. **Which of the two to drop is a user-testing question, not a documentation one** — if testing shows the ladder alone is hard to read, the tag is one answer to that. **Why it has no home:** `.claude/rules/compliance.md` says *"Compliance is not quality… Never add a quality question to the scorecard"*, and the quality bar is the deferred milestone that `status.md` records as existing only in Gabriel's head. So it cannot be a scorecard question and cannot be a ruleset rule. Recorded here until the quality bar gets a method of its own. Gabriel's call in the same conversation: that method is too large for this deadline and is deliberately not being designed now |
| 14 Sep 2026 | **The `Donut chart` main component carries an internal gap of 56, and 56 is on the spacing deny-list.** Found cold while ruling run-002, 14 Sep. `spacing-rules-ai.md` → **Do not use** says `Spacing/56` has *"No documented purpose anywhere — not in any usage note, layer, or rhythm table, and no consumer."* Eighty-three lines earlier the same file lists 56 as page-rhythm, and says **"Never put a page-rhythm token inside a component or card. No component in the system does, and that is the single most reliably-followed rule in the spacing docs."** The donut appears to do exactly that. **Not tested:** whether the component binds `Spacing/56` or uses a plain number — nobody has read the main component's bindings. It matters either way, because the verification behind that claim covered 60 **web** component token files and no Figma component at all. **This belongs with the *Settle whether `Spacing/56` is a page-rhythm token or a forbidden one* task**, which is already on the list — check the donut before settling it, because a live consumer changes the answer |
| 14 Sep 2026 | **A cold session re-derived the backlog's own donut findings without being shown them, which is the first real test of the one-task-one-chat rule.** Run-002's eight items were ruled in a fresh chat given the run report and nothing else — no access to the chat that built or scored the run, and no sight of this file's existing rows. It independently found that the legend sentence in `components-rules-ai.md` was backwards, that the library offers only the beside placement, and that `legend.md` permits below **or** to the left. All three were already written down here on 14 Sep by the run chat. **Why that is worth recording:** the cold-session rule was adopted on the argument that a session which built something is the worst judge of it, and until now nothing had checked whether a cold session actually reaches the same place. It did, on the one finding where both sessions had the same evidence |
| 14 Sep 2026 | **Keys in `figma/figma-icons-registry.json` are component-*set* keys, not component keys.** `importComponentByKeyAsync` throws *not found* on them; `importComponentSetByKeyAsync` resolves. Found in run-002 at the cost of two failed calls. Nothing is wrong with the registry — the rule file simply never says which of the two kinds of key it holds, and the two API calls are named almost identically |
| 14 Sep 2026 | **Two files disagree about who writes the compliance ledgers.** `compliance-run-ledger.md` and `compliance-flag-ledger.md` both open with *"Written by the checking agent — never edit by hand"*. `generate-and-score` step 5 has the **orchestrating** session append both rows, and the prompt it gives the scorer scopes that agent to the report only. Run-002 followed the skill, as the newer and more specific instruction, and the scorer confirmed it appended nothing. Harmless while exactly one of them does it. Worth settling which, because the ledger headers are what a cold session reads first |
| 14 Sep 2026 | **`Card` has no padding of its own — whatever fills its content slot has to bring its own inset.** Measured on a live instance: `Radius=16, Color=Light` at 328px reports padding `0,0,0,0`, and a swapped-in content component sat flush to all four edges until it was given `Spacing/16` itself. Nothing is broken — `Card` is a surface, not a container — but the name reads like a container, and the failure is silent rather than loud: the build succeeds and the button simply touches the edge. Worth knowing before the next screen puts one on a page. Found while building the finance block in the `Claude-to-Figma — SL` playground file |
| 12 Sep 2026 | **The run report now has two authors, and nothing enforces which writes first.** The generating agent writes `## Declarations` before scoring; the checking agent writes everything else afterwards and must never touch that section. Both rules are stated — in `compliance-scorecard.md`, in the component ruleset and in `.claude/rules/compliance.md` — and all three are instructions, not mechanisms. A declaration reworded after the verdict is known would look identical to one written honestly, and git history is the only thing that could tell them apart. **Not accepted — deferred, 12 Sep.** Gabriel chose one file per run over a separate declarations file, and a separate file would not have helped anyway: nothing stops an agent writing any file, so that is the same instruction wearing a second filename. The mechanism that does work is comparing the report's declarations against the adapter's copy in the facts file, which is now a task under *Scoring a finished screen*, blocked on the adapter existing |
| 11 Sep 2026 | **A ruleset gap goes to the backlog, not straight into the audit. Gabriel's decision, 11 Sep.** Two files had said otherwise — `compliance-scorecard.md` and `compliance-flag-ledger.md` both sent a flag ruled `ruleset gap` directly to the relevant `-audit.md`, while `CLAUDE.md` says every finding lands in the backlog. Two destinations meant two sessions filed the same finding in different files. Both files now say the backlog first; doing that task writes the rule into the ruleset and the reasoning into the audit. **The audit is where reasoning settles, never the queue.** This is also the one edit made to the scorecard during the re-sorting work, and only because answering the question required it. **Refined 14 Sep 2026, and the headline still holds:** a `ruleset gap` still lands in the backlog and never straight in the audit. What changed is that it no longer files itself as a task — Gabriel decides per item whether to fix it on the spot, in which case it lands as a *Done* row instead |
| 11 Sep 2026 | **The task list is sorted by the sequence of designing a screen now, and this is the third sort in three days.** Severity came first, then impact, now the steps — and the reason the first two failed is the same each time: both graded *defects*, so neither could see a step nobody had built. *Define the content* and *check the content* had never produced a single row between them, and content is the least documented part of the whole system. Gabriel found the mechanism: work backwards from the objective, ask what a step needs, and a missing step shows up as a step with nothing under it. **Two sections became columns** — startable-versus-blocked became *Blocked by*, reaches-the-canvas became *Due by September* — because the last attempt failed a check when a section went stale while a row's position said nothing about why it was there. **What stays out: numbers on the steps.** `.claude/rules/project.md` already bans numbers that encode only what order states, so no file anywhere says "step 3" |
| 11 Sep 2026 | **Each flag now lives in two files and nothing checks they agree.** A flag is written into `report.md` and appended to `compliance-flag-ledger.md`. That is the same risk already recorded for the scores, which sit in both `report.md` and `compliance-run-ledger.md`. Both duplications are inherent to append-only evidence and neither should be removed — what is missing is a stated **direction**: the report is written first, and every ledger row is copied from it, never the reverse. Without that, a session correcting a ledger row can leave the report saying something else and both look authoritative |
| 11 Sep 2026 | **The scorecard's checks each guard a step of designing, and no check says which.** Mapped while re-sorting: `C1 · Provenance` and `C2 · Tier ceiling` guard *define the components to use*, `C5 · Declaration` guards *define what needs to be built*, `C3 · Token binding` and `C4 · Authorisation` guard *choose the tokens*, `C6 · Layout` would guard *place them according to the design guidance*. Three steps — define the content, put them on the screen, check the content — have **no check at all**. So a report says `C4` flagged a token and joining that to a step happens in Gabriel's head, which is the loop he described not closing. It is now a task under *Scoring a finished screen*; the scorecard itself was deliberately not touched in the same commit as the queue |
| 11 Sep 2026 | **The task list is sorted by impact now, not by how badly a defect fails.** Gabriel asked for it, and the reason it was needed is specific: a severity sort can only rank things that are *broken*, so it had no slot for things that were simply **missing**. Five of twelve token categories have no ruleset an agent may read, and there was no row for writing them — nothing was wrong, so nothing was graded, so the task never appeared. Two rows moved a long way: the scored generation run rose from last in its block to fifth, because it is the only row that tells us whether any of this works; the ruleset header links fell, because that row's own text says *"no evidence any agent has followed one"*. Severity survives as the first tie-breaker, which is where *fear the silent ones* now lives. **The three severity labels were reworded in the same pass** — "ships wrong, breaks no rule" read as a contradiction unless you already knew "rule" meant *a line in one of our own rulesets*, and Gabriel said plainly he could not parse it |
| 11 Sep 2026 | **One restatement of the writing rules survives on purpose, in the line that loads at session start.** The `task-next` skill's description still says it proposes a task *in plain language*. It is frontmatter — the sentence that makes the skill offer itself when Gabriel types `/task-next` — so it describes behaviour to a router rather than instructing a writer, and rewording it risks the trigger. A rider on whatever next opens that file, never a task |
| 11 Sep 2026 | **The generating skill is Gabriel's to build, and it is deliberately later — but two things about it are already decided.** It produces the screen *and* the report; a run whose screen exists but whose report does not is unfinished. And it does **not** define what a report contains — this repo does, in `compliance-scorecard.md` — or the iOS agent will invent a second format and the history splits in two. The skill owns gathering the facts from its own platform and writing the two files; that is the adapter, and adapters are platform-specific by definition. **One practical catch: `design` is already a built-in Claude Code skill name** (the canvas/mockup one), so a `/design` command would collide. Pick another name |
| 11 Sep 2026 | **The adapter does not exist, so the first reports are written by hand.** The scorecard now specifies `facts.json` precisely — seven required facts, an exact shape, an omitted field meaning *unavailable* and `false` meaning *no*. Nothing produces it. Until the generating skill is built, a run's facts and report are hand-written, and a hand-written facts file is exactly the kind of thing that quietly stops matching the contract it claims to follow. Worth checking the first two against the schema by eye rather than assuming |
| 11 Sep 2026 | **A report's scores and its ledger row can disagree, and nothing checks that they don't.** The cost of splitting the run log out of the scorecard on 11 Sep. It is the right split — the ruler must not be rewritten when a measurement is recorded — but it creates two places holding the same five percentages. `scripts/check-links.py` checks that files exist, not that numbers agree. If runs become frequent this is what to automate first |
| 11 Sep 2026 | **A session with no Figma tools wrote down a fact about a Figma file, and the check that ran in the same session passed it.** `project/how-a-run-is-reported.md` said `run-001` could not be recovered because *"the Figma file has moved on"*. Nobody looked — neither that session nor this one can see Figma, those tools live in `~/.claude/` only. It also contradicted the backlog, which lists scoring `run-001` as a live task blocked on that exact question. Caught by a **second, independent read** in a different chat, not by the check that ran alongside the writing — which is the argument for one task, one chat, stated better than the rule states it. The claim to watch for is the confident one about a system the session cannot reach |
| 11 Sep 2026 | **The scorecard now carries two different example runs, and one of them is in a code block.** The adapter-contract section shows run `001` / wireframe `serp-v1`; the report template shows run `007` / `listing-detail-mobile`. Neither is wrong and nothing breaks — they are illustrations of different things — but a reader tracing one example through the file finds it changes identity halfway. A rider on whatever next opens that file, alongside the leftover rule numbers in the spacing ruleset and the two `⚠︎ undescribed` lines in the component ruleset. Not worth a task of its own |
| 11 Sep 2026 | ~~**`status.md` is at 921 words**~~ — **`wc -w` says 966 on 11 Sep, after the writing-rules task shortened the page by 11 words.** The same command read 977 immediately before that task, so the 921 recorded here was measured some other way and the two figures were never comparable. Use `wc -w` and nothing else. What stays true: the report task added a section and pushed three things out to make room — the task-sorting rule, a stale line about the energy-colour question, and a paragraph about the parked chart-sizing task, all of which were already written out here. It is within one screen, but the margin is thin: the next thing added still has to push something else out |
| 11 Sep 2026 | **The Patterns and Experiences libraries have been listed end to end, and there are fewer of them than the inventory suggests** — 21 Patterns and 11 Experiences, read straight from `figma/figma-patterns-registry.json` and `figma/figma-experiences-registry.json`. Worth not re-deriving: the next time a rule needs to claim "no higher-tier component is this", the whole set is small enough to check by eye rather than to assume. Patterns: `Bar graph`, `Breadcrumb`, `Burger menu`, `Burger menu (profil)`, `Date Field`, `Date Picker`, `Donut chart`, `Feedback Bar`, `Filter bar`, `Filter button`, `Filter dropdown container`, `Footer`, `Info State`, `KPI`, `Line chart`, `Media Upload`, `Mega menus`, `Menus`, `Navigation bar`, `Top Bar`, `Wizard`. Experiences: `Estimation card`, `Floor selection`, `Listing Card`, `Listing summary`, `Map Polygon`, `Map Polygon backdrop`, `Map template`, `Phone Number Field`, `Table`, `mapPinsV2_IWT`, `mapPinsV2_SL` |
| 11 Sep 2026 | **The component ruleset now describes `Estimation card` in one place and calls it undescribed in two others.** The new simulator rule says what it is — a finished estimate with price range, confidence, and a selling-or-renting type, all sourced from its registry entry. Two older lines still read `⚠︎ undescribed, see audit`. They are not wrong in fact: they mean *there is no usage doc page*, which is still true. But an agent reading a description and then being told the thing is undescribed has been given two answers. Not a defect worth its own task — a rider on whatever next opens that file, alongside the leftover rule numbers in the spacing ruleset |
| 11 Sep 2026 | **A task that corrects a file can leave behind a sentence saying it hasn't been corrected.** The colour ruleset carried *"The audit file still says 83"* — true when written, falsified hours later by the same task, which fixed the number in seven places across the audit but not the sentence pointing at it. Both files had said 73 for a day. The harm is specific: an agent reads the ruleset and may **not** open the audit, so it was being told a correct figure is disputed with no way to check. Worth watching for whenever a correction spans two files — fix the claim *about* the other file, not just the number in it |
| 11 Sep 2026 | **Graphify is retired, and the graph is archived outside the repo.** It cost 990,970 tokens to maintain a knowledge graph over a 172,818-word corpus — 4.2× what reading the whole repo costs — and was queried exactly once, by the session that built it. A targeted grep answers the same questions for about 220 tokens, and this repo is already hand-indexed by the filename grammar, the routing pages and the where-to-look table. The graph, its config and the full reasoning now live in `~/Desktop/ai/gsl-ds-graphify-archive/`; the decision is in `project/decisions.md`. `CLAUDE.md` says plainly not to rebuild it, because the skill is installed globally and will otherwise offer itself to a future session as if nobody had set it up yet |
| 10 Sep 2026 | **Splitting work between a planning model and a cheaper executing model was considered and dropped — for now.** The idea: Opus writes an unambiguous spec, a cheaper worker carries it out, Opus checks. It is possible — a sub-agent is defined by one file in `.claude/agents/` that pins its own model, and it appears as a block inside the same chat, nothing to switch and nothing to remember. A hook **cannot** do it: hooks can only block a model switch someone else asked for, never start one, so there is no way to flip the session model automatically. Dropped because only three open rows qualify, all half-hour jobs, and deciding it cost more than it would ever have saved. **What would reverse it: a task with real fan-out** — the Figma registry syncs (~455 icons, ~98 component sets), or matching 98 Figma names against 60 code directories. On one of those a pinned worker pays for itself inside a single task, and it should be raised then. **The test for whether a task can be handed over at all:** needing a lot of *files* is fine — the worker reads them in its own context, which is the win. Needing a lot of *the conversation* disqualifies it. If writing the spec means writing down the thinking, the thinking is the task and there is nothing to hand over |
| 10 Sep 2026 | **The task list was sorted by damage but never by deadline, and it put a web defect next on a Figma project.** Fixed 10 Sep: the first question is now whether a task changes what lands on the Figma canvas. An agent designing in Figma can place any component in the libraries, so whether one is also built on web changes nothing about the page it draws. Seven tasks reach the canvas, four do not |
| 10 Sep 2026 | **Five backlog rows had gone stale in a single session, three of them falsified by the task being logged at the time.** Adding new findings was in the check skill; re-reading existing rows was not. `task-check` now corrects rows about files the task touched — `status.md` and the backlog directly, anything else reported and asked about first |
| 10 Sep 2026 | **The French energy ladder in `scale.md` was wrong for five of its seven classes, and the error had a shape.** The German scale was recorded correctly, then the French column was filled in by walking the same palette from the start and taking the first seven steps. France does not work that way — seven classes still have to span green to red, so the code samples across the palette, skipping `Green300` and `Yellow200` and reaching `Red100`/`Red200` at the bottom. Measured on the hex values: the old ladder ended at a warmth of +44 at class G, which is where class **E** sits on the corrected scale. G — the worst rating — was an amber |
| 10 Sep 2026 | **Audits get corrected in the same task that disproves them; the decision log never does.** Gabriel's call, 10 Sep: raise it while you are already in the topic rather than filing it for later — and still ask before touching the file, exactly like any other change. The convention sets *when* it is raised, not whether it needs approval. Written into `CLAUDE.md`. The line is what the file is for — an audit describes the system, so a figure known to be wrong is just wrong; `project/decisions.md` records what was believed on a date, and correcting that destroys its only purpose. First application: the colour audit's "83 tokens have no consumer" corrected to 73 in seven places |
| 10 Sep 2026 | **The colour ruleset's claim that nothing consumes the energy colours is false.** It lists `Scale/*` under **Never use** — *"consumed by an external team, not by any GSL component"*. `libraries/patterns/energyclassslider/src/EnergyScale.tsx` in the GSL web repo binds `scales.energy.green100` through `scales.energy.red200` directly. A GSL component consumes them. The audit's headline "83 of 218 tokens have no consumer" is overstated by at least the 12 energy tokens |
| 10 Sep 2026 | **The energy filter slider ships on web and was never added to the Figma libraries** — Gabriel, 10 Sep. It lets someone filter listings by energy range (A to C). So a design agent asked to build a search filter has working web code to copy and no Figma component to place. Colour varies by country; France is the only target, and brand makes no difference |
| 10 Sep 2026 | **The component docs' platform-readiness rows are stale, and building anything on them produces confident wrong answers.** Checked all 13 web statuses against the web code repo at `~/gsl-core-web-design-system`: five components the docs call `To Do`/`WIP`/`In progress` on web are shipping today — `Bar graph` (`libraries/patterns/barchart`), `Coachmark`, `Line chart` (`libraries/patterns/linechart`), `Segmented Control` and `Slider`. All three docs with no readiness row at all — `Tabs`, `Date Picker`, `Phone Number Field` — are also live on web. Gabriel spotted this from memory before any check was run |
| 10 Sep 2026 | **The web code repo is the real answer to "does this component exist on web", and it is already on this machine** — `~/gsl-core-web-design-system`, components at `libraries/ui/src/` and `libraries/patterns/`. `tokens/scripts/` already reads that repo to generate the token ledgers, so a generated availability list follows a pattern this repo has used before. A hand-copied list drifts; a generated one cannot |
| 10 Sep 2026 | **The web repo's per-component agent files are auto-generated, and not the finished contract they look like.** `ds-bundle/components/<area>/<Component>/<Component>.prompt.md` is one file per component with variants and a typed prop interface — but read in full, `Tabs.prompt.md` mixes 13 real props with 14 generic layout passthroughs (`marginTop`, `zIndex`, `order`) each carrying the same meaningless comment, and its **"Variants" list is padded with Storybook interaction tests** — *Should Handle Selection With Keyboard*, *Should Not Select Disabled Tabs* — presented exactly like real design variants. An agent would choose a test as a variant. Useful as evidence of what exists on web; unsafe as a contract. The earlier note here called it "what we're writing by hand", which overstated it |
| 10 Sep 2026 | **An energy scale exists in web code** — `libraries/patterns/energyclassslider/src/EnergyScale.tsx`, alongside `EnergyClassSlider.tsx`. The energy-rating (DPE) colour question is the highest-impact open item on this page and was framed as *no GSL component does this*. That framing may be wrong for web. Worth reading before answering the question |
| 10 Sep 2026 | **`Navigation Bar (App)` and `Navigation bar` are two different components — checked, and they do not contradict each other.** The ruleset's platform table says `Navigation Bar (App)` is mobile-only; `navigation-bar.md` says Ready on web. Those looked like the same component stated two opposite ways. They are not: the inventory carries both, the app one with no doc. Recorded because the near-identical names invite exactly this mistake, and it was made here once already |
| 9 Sep 2026 | **"Widen the `audit` branch category" was deleted from the task list, not done.** It only ever named one skill, and branches are gone from this repo entirely. Recorded here in case it ever needs bringing back |
| 10 Sep 2026 | **There are two Claude config folders on this machine, and they do not share their tools.** `~/.claude/` holds the four personal skills and the Figma plugin; `~/.claude-personal/` is a second, complete config with neither. Whichever a session runs off decides what it can do — one cause behind both today's missing pieces. Gabriel worked this out; the earlier note here blamed background sessions, which was wrong |
| 10 Sep 2026 | **The skills half is fixed:** `~/.claude-personal/skills` is now a link to `~/.claude/skills`, so both configs see the same four. Takes effect in new sessions, not ones already running. **Figma is not fixed** — plugins carry install state and an `enabledPlugins` setting the second config lacks, so the same trick is riskier and was left alone |
| 9 Sep 2026 | Corner radius is applied in Figma as a plain number, never as a bound variable — confirmed across the component library and three real screens. A generating agent must set the value, and a checker must not flag it as an unbound token |
| 9 Sep 2026 | Border thickness has no separate evidence file; its rules come from the notes inside its own token page. Honest, but it is the one ruleset without a sibling audit |
| 9 Sep 2026 | The "Newest" sort control on the real results-list screen is a hand-built frame, not a real Button — spotted during the radius audit. Exactly the failure this project exists to prevent, in a shipped screen |
| 9 Sep 2026 | **Only three files knew about the new way of working** — `CLAUDE.md`, `status.md` and this one. The rules file for `project/` still specified the old four-step ritual and loads automatically, so a future session would have been handed the old process. Became the next task |
| 9 Sep 2026 | **The corner-radius and shadow rules already existed and were already verified** — radius against nine components and three screens, shadow against eight components. The gap was only that they sit in files agents are told not to read as rules. Turned that task from research into moving text |
| 9 Sep 2026 | Twelve brand PDFs in `design-language/` were deleted by another session; Gabriel confirmed the removal. The folder is gone |
| 10 Sep 2026 | `status.md` is back inside its one-screen budget at 897 words, after the three git commands were cut. The next thing added to it still has to push something else out |
| 10 Sep 2026 | **The graph was rebuilt scoped and now passes the test the old one failed.** 583 things, 822 connections, 57 groups, from 143 files. Asked the same question — what does `-rules-ai` mean and who reads it — every source it returned is a live file: the decision log, `CLAUDE.md`, `README.md`, `tokens/README.md`. No archived briefs, no deleted PDF, and the retired four-step ritual is gone. The 9 Sep graph answered the same question citing all four |
| 10 Sep 2026 | **Scoping bought accuracy, not money.** The rebuild cost 814,930 tokens against the first run's 998,164 — 18% less, for 92% less text. Reading is not where the cost sits; the number of separate things to reason about is. Assume roughly this much again for any full rebuild, and use `--update` for anything smaller |
| 10 Sep 2026 | **49 of 936 connections point at something that isn't in the graph.** Six agents read the repo in parallel and split on what to do about links crossing between their batches — two emitted them, two deliberately did not. So whether a "Related components" link survives depends on which batch happened to hold that file. Never read a missing connection as "these two things are unrelated" |
| 10 Sep 2026 | **A dated entry in `project/decisions.md` still names `components.md`, and should stay that way.** It records what was true in August. Rewriting a decision log so it matches today makes it useless as a record — the point of the log is that it says what was believed when |
| 10 Sep 2026 | **Renaming a path-scoped rules file does not break it.** Verified live: after `.claude/rules/components.md` became `component-docs.md`, reading a file in `components/` still loaded it. The frontmatter `paths:` block does the work; the filename is free |
| 22 Sep 2026 | **`compliance/briefs/brief-001/run-001/` holds two screenshots and no report**, because it predates the report convention settled on 12 Sep. Nothing is wrong with it and nothing needs doing — it is recorded because `project/how-a-run-is-reported.md` walks through a run folder naming `report-run-001.md`, which makes that name unresolvable, and the new name check carries it as a named exception. **If run-001 is ever re-run or back-filled, that exception should come out of `scripts/check-links.py`** |
| 10 Sep 2026 | ~~**Nothing in this repo checks that links resolve.**~~ **Superseded the same day** — `scripts/check-links.py` was written on 10 Sep and does exactly this. **Closed 22 Sep 2026** — it reads filenames in single backticks too, 346 of them, and fails on one that names no file. A name in bare prose, with no backticks around it, is still invisible and is meant to be: there is no way to tell one from an ordinary word. `/task-next` also runs `git status` first and traces what referenced anything deleted, which covers a deletion not yet committed |
| 10 Sep 2026 | **`internal/git-basics-tutorial.md` is about branches, which this repo stopped using on 9 Sep.** Starting a topic, worktrees, which branch a chat is on — none of it applies any more. It also only ever covered `git status`, not `git log` or `git revert`. Nothing depends on it, so nothing is broken, but it now describes a workflow that no longer exists |
| 9 Sep 2026 | The eight human-facing files totalled 15,400 words — about 60 pages. That, not the 120 machine files, is what made the project unreadable |

---

**Two measurements that do not agree, recorded 18 Sep 2026.** The row *Write the
fifteen selectable components that still have no doc* says sixteen names and the
decision log says the gap fell to **18**. `components/coverage.py` run the same
day, before any of the five new docs, counted **19 selectable gaps**. The five
docs and `Image Ratio`'s move to Never select took it to **12**, so the arithmetic is consistent either way — it is the
starting figure that two sources spell differently. Nothing acts on it. Worth
one look the next time the gap is quoted.

**Three files carry uncommitted whitespace, from before 18 Sep.**
`compliance/compliance-scorecard.md`, `components/components-rules-ai.md` and
`project/design-process-map.md` all show table-padding changes only — columns
aligned with spaces, no word altered. It looks like an editor's markdown
formatter ran over them. Left untouched, and **they will ride along in the next
commit unless they are dealt with first.**

## Done

| Date | What |
| --- | --- |
| 25 Sep 2026 | **The schools PRD for Test 2 is written, in `~/gsl-specs/prd-schools.md`.** It carries the designer's content word for word, from Gabriel's three screenshots: title, `Beta` label, explanation, five sample schools, short disclaimer, source and the three-paragraph detailed disclaimer. **Gabriel's rule, 25 Sep:** a PRD names what each item holds and what a control is for, never the component, container or layout — so no *row*, no *modal*. Private schools are now in scope; the prompt no longer mentions icons or tokens. **Proved:** every string re-read against the screenshots; a word scan finds no component name except the existing address button. **Not tested:** Test 2 itself |
| 24 Sep 2026 | **The design skill no longer takes the person's words as constraints, and tells `/prototype` where to place a block.** From Gabriel's first real run, `spec-002`: the skill ruled out `Pop-up` and `Modal bottom sheet` because the idea said *banner*, then built a `Card` with invented content; `/prototype` put it over the whole app. `SKILL.md` now says: every interface word in the idea is a wish, never a reason to rule a component out; before any built element, name the component the rules give for the need and rule it out by the need only; the build request says to place each block inside the named screen, never over the whole app. Two self-check lines added, one asking a block on an existing screen to name that screen. **Not tested:** a re-run |
| 24 Sep 2026 | **The prototype page is now a workflow.** `project/how-a-prototype-is-made.md` rewritten on Gabriel's ask: the six steps — idea, questions, validate, design, build, feedback — each with what, who and why, then where things live and how to keep it current. Gabriel: *"based on a workflow… easier to understand"*. The design skill had left five pages stale; all updated: `skills/README.md`, `README.md`, `scripts/README.md`, `CLAUDE.md` (four tooling exceptions, not three). Today's calls are a new entry in `decisions.md`; the earlier entry was not rewritten. `check-all.py`: links pass, the one fail the five known disagreements |
| 24 Sep 2026 | **The design skill can now pick a colour token, not just a family.** Gabriel spotted it: `references/` had `color-rules-ai.md`, which picks the family, but none of the five family pages that pick the token inside it, and the generator had cut the links to them. **Now copied** — `background`, `surface`, `border`, `content`, `scale` — cut before `## Tokens`, so no value tables; under their own names, so the ruleset's links resolve again. `SKILL.md` says colour takes two reads, and a token marked **Not used** is never chosen. **Proved:** the other five rulesets each name their allowed tokens, so colour was the only gap; `Content/Subdued` and `Surface/Status/Error` read correctly in the copies; `check-all.py` six of seven, the one fail the five known disagreements. **Not tested:** a real run |
| 24 Sep 2026 | **The design skill exists, in `skills/design/`, linked into `~/.claude/skills/` so it runs from `~/gsl-ios`.** `/design` plus an idea: up to nine questions, the person validates goals and acceptance criteria, then the screen is designed from `references/` without asking about components, saved to `~/gsl-specs/`, and built by `/prototype` in the same chat. Feedback comes in the same chat, in three sizes. `references/` holds 12 files generated by `scripts/build-design-references.py` — eight rulesets, the two iOS maps, and the variants of 73 component docs. **Your decisions, 24 Sep:** one flow, not separate commands; goals validated before any screen; specs saved outside every repo so they outlive the prototype. **The first check failed** on one contradiction about where a spec is saved; fixed at the source in `spec-rules-ai.md`. **Proved:** every file and heading the skill names exists; regenerating `references/` is identical; `check-all.py` six of seven, the one fail the five known disagreements. **Not tested:** a real run |
| 24 Sep 2026 | **The iOS demo has a page for people.** `project/how-a-prototype-is-made.md` explains the flow, what each file is for, and how to run the token-map script. Today's decisions are in `project/decisions.md`, with what they cost. Gabriel asked for it: a lot is being built, and it needed one place to read |
| 24 Sep 2026 | **Every token has its iOS name, or says it has none.** `tokens/tokens-ios-map.md`, generated by `tokens/scripts/extract_ios_token_map.py` from `gsl-ios` `origin/develop`. **283 tokens: 248 proved, 35 not found, none guessed.** A colour is proved when iOS fills its path from an asset named after that exact token; all 201 such rows were re-checked against the iOS source with 0 problems. All 22 tokens `spec-001` uses are proved. The first check failed: `--check` called the file stale whenever iOS got any commit. Fixed the same day; a planted fault still fails it. **Not tested:** no iOS engineer has confirmed it, and app code outside the design-system package was not read |
| 24 Sep 2026 | **The spec format exists.** `specs/spec-rules-ai.md` defines eight sections in design-system names only: goals, acceptance criteria, assumptions, screen, inventions, change log. `specs/spec-001.md` is `brief-001` written in it, 43 elements. The first check failed on three contradictions between format and example; all three fixed the same day. **Proved:** every name resolves, every composed element is declared, every goal has a criterion, every link resolves. **Not tested:** no fresh agent has written or built from a spec yet |
| 24 Sep 2026 | **Every selectable component now has its iOS name, in `components/components-ios-map.md`.** First task of the new priority: Gabriel, 24 Sep, the iOS demo replaces the Figma milestone. Read from `gsl-ios` `origin/develop` at `f488b6040a`, because the local checkout was 21 design-system commits behind. **73 of 73 mapped, checked by script: 44 proved, 5 guessing, 18 not found, 6 not for iOS.** A markdown table, Gabriel's choice over a JSON registry or a ruleset column. **No iOS engineer has confirmed a single pairing.** `check-all.py`: six pass, and the one fail is the five known disagreements, identical before and after |
| 24 Sep 2026 | **Every readiness table's Figma cell is now checked, and all 75 agree with the registries.** `scripts/check-readiness-figma.py` is the seventh check in `check-all.py`. **Ready**, Gabriel's definition of 24 Sep: a registry holds the component and the entry is not flagged outdated, in progress or unverified. Flagged entries are listed by hand in the script, because the registry `status` is free prose — `Map template` contains *OUTDATED* only because it names an excluded sibling. **Six cells fixed** — `snackbar`, `select-card-group`, `bar-chart`, `donut-chart` said not ready; `energy-tag` and `modal-bottom-sheet` held a link. The 8 Sep audit had found the same six and left them. **Proved it fails** on a copy with three planted faults, one per direction. **The audit's *all 98 verified live* was false** and is corrected: two entries say they were not re-verified, `Tab Bar` is in progress. **`/task-check` caught my first correction overclaiming** — *95 verified* was a count nobody made — and it was rewritten before commit. Web, iOS and Android cells are still unread |
| 23 Sep 2026 | **The repo has a sixth check, and it answers the question Gabriel named as the end goal.** `scripts/check-rules-reach.py` reads `components-rules-ai.md` against itself: for every inventory entry not marked never-select, does any rule above the inventory name it? **A component no rule names can never be selected, however well an agent follows the rules** — and no check could see that, because `coverage.py` reads the registry against *docs* and `check-rules-docs.py` reads *docs* against rules. Nothing read the inventory against the rules. **Result: 73 selectable components, all 73 reachable.** `status.md`'s hand count of 21 September was right and is now checked rather than claimed. **Proved it fails, three ways:** a planted new component, a real one (`Divider`) with every mention stripped, and the same hole planted in the real file and run through `check-all.py` — FAIL reported, component named, file restored from git. **It found a bug in itself first:** the first run called 71 of 73 unreachable, `Button` included, because an unpaired `**` in prose pairs with the next one paragraphs away and the span swallowed whole sections. A name is now capped at 70 characters and may not cross a line, commented at the line that caused it. **Seven lines across five files said the repo had five checks** — `CLAUDE.md` twice, `README.md` twice (one enumerates them), `scripts/README.md` plus a row for the new script, and the `task-check` skill three times. `project/decisions.md` also says five and is deliberately untouched. **What it deliberately cannot see, both in the docstring:** whether the inventory itself is complete, and whether the rule that names a component is any good |
| 23 Sep 2026 | **The backlog's questions are sorted by whether an agent is blocked, not by topic.** Gabriel's filter, 23 Sep: does your answer change what an agent builds or how its output is scored? **Seven of 45 do** — the icon variant axes, whether an agent may read a token values page, the `Decorative` colour boundary, the two middle shadow levels, the grid layout system, the energy filter slider, and `Map template`'s outdated components. They sit in a new first section with a *why it blocks* column written for the five that had none. **The rest are real and none stops a compliant screen**, so they wait. **The section header says plainly that the filter is not importance** — a question below the line may matter more to the design system; it is below because nobody is blocked by not knowing. **Two questions were struck as already answered by the files:** *24 components can be selected but have no documentation and no routing entry* — one has no doc, `Pop-up`, and all 73 have a rule, so both halves were wrong; and *`CLAUDE.md` says two deliberate exceptions* — it already says three. **Nothing was lost: 45 questions before, 45 after**, compared key by key against `HEAD`. **The Tasks section was deliberately not re-sorted** — `task-next` picks the next task from its design-step order, so re-sorting it would break the rule that chooses the work |
| 23 Sep 2026 | **The component eval went from 31 intents to 64, and one of the existing 31 was wrong.** 33 rows drafted, one per rule written after the last run and reached by no existing intent: the six never-select rulings, the three-component navigation family, the built-outside-the-design-system flag, the nine components documented on 21 and 22 September, the two Experiences runs 1–3 never reached, and the chart, legend, bare-icon and declaration rules. **Every expected answer was traced back to a line in `components-rules-ai.md`** — 34 distinct names checked, none missing. **Row 17 would have scored a pass on an answer the ruleset forbids:** it accepted `Pop-up` for a blocking confirmation **on web**, and `Pop-up` became apps-only on 21 September. Corrected, with row 54 added for `Pop-up`'s own case in an app. **The run instruction changed too** — it said *ask for one component name*, which tells the agent a component exists; twelve rows now have no component as their answer. Header, scoring bands and `components-index.md`'s count all follow the new number, the last on Gabriel's explicit go. **Nothing here has been run.** Drafting proves the file is well-formed and internally consistent; whether a cold agent reaches these 64 answers is exactly what nobody knows yet, and it is the next task. **One gap left deliberately: no intent tests the count-the-parts threshold**, which has its own row with its own run method |
| 23 Sep 2026 | **An examining file now carries the full name of the file it examines — three renames, and the rule written down.** Gabriel's rule, 23 Sep: `components-eval.md` became `components-rules-ai-eval.md`, `components-audit.md` became `components-rules-ai-audit.md`, and `compliance-audit.md` became `compliance-scorecard-audit.md`. All three are `git mv`, so history follows them. **21 references fixed across 15 files; `check-all.py`'s link and filename check passes** — 2,364 links and 369 filenames. **The rule is in four places** so it cannot drift: the suffix table and a new section in `README.md`, the grammar line in `CLAUDE.md`, and both folder rules. **Its boundary is stated with it:** a file about a *topic* keeps the topic's name, which is why `color-usage-audit.md` and the five audits like it are unchanged. **`compliance-scorecard.md` was deliberately not renamed to an `-eval`** — no file in this repo is its target, and it has no answer key, so its questions produce a report rather than a score that compares across dates. That distinction is now written into `README.md` and `.claude/rules/compliance.md`. **`README.md` no longer confesses the component-audit name as an *honest inconsistency*** — the rule retires it, leaving one. **Nothing dated was rewritten:** `status.md`'s done log, `project/decisions.md`, this list and `project/archive/` still carry the old names, and all four are exempt from the name check by design. The one file edited against that grain was `report-run-002.md`, on Gabriel's explicit yes, and only the filename inside it changed |
| 22 Sep 2026 | **`README.md` and `CLAUDE.md` now name `check-all.py` as the thing to run, and say what a clean run does not prove.** Both had enumerated the repo's checks in prose and **both were already incomplete before the runner existed** — neither mentioned `check-rules-docs.py` or `check-tool-neutral.py`, which have existed since 18 and 22 September. **Three passages changed**, all of them sentences explaining what the repo is rather than rows in a table, which is why they waited for Gabriel instead of going in with the runner. **The warning travelled with the fix:** both pages now say a clean run proves the docs are well-formed and proves nothing about whether a sentence is true, because no check here opens Figma, the web code or the app. A grammar fault went too — `README.md` read *"the one deliberate exceptions are"* |
| 22 Sep 2026 | **The five checks became one command, and `task-check` runs it.** `scripts/check-all.py` runs every standing check and prints one verdict — **0.57 seconds, and it changes nothing unless `--write` is passed.** The three read-only checks run as subprocesses; the two generators are run into a temporary file and compared, so the question they answer here is *is the committed page still current*, never *rewrite it*. **Proved it is not a check that only ever passes:** a junk line appended to the coverage ledger made it report `STALE`, and restoring the file made it report `current`. **It exits 1 today**, on the five `check-rules-docs.py` disagreements that predate it — so the skill carries a decision tree for telling a known defect from one the current task just made, and names those five explicitly. **Two lines each in `coverage.py` and `template-drift.py`**, because both crashed on an output path outside the repo — a crash *after* the file was already written. **What the skill says matters more than the runner.** A clean run proves the docs are well-formed and **proves nothing about whether a sentence is true**; every check reads this repo against itself, and `map-template.md` passes all five. That warning is in the script's docstring, in the skill, and in the printed output, with the four questions nobody checks listed beside it. **Gabriel rejected a hook for this**, and the reason is recorded: a hook fires with nobody asking, and he already types `/task-check` at the end of every task |
| 22 Sep 2026 | **`components/missing-images.md` is deleted — the problem it described no longer exists.** It listed images a doc still pointed at by remote URL. **Proved 22 Sep 2026:** both remaining entries named files that no doc references and that are not on disk, and a scan of **all 1,611 image references** found zero remote URLs and zero non-pictures. **Its one lasting lesson was not moved anywhere, because it was already recorded better.** `.claude/skills/zeroheight-merge/SKILL.md` carries all three filename corruptions — an SVG under a `.png` name, a dropped leading underscore, an inserted character — and the match-by-column-name rule that replaced filename matching. **Nothing listed the page**, so no table of contents changed; the one surviving mention is a dated note in this file, which is a record of what was true on 17 Sep and is never rewritten |
| 22 Sep 2026 | **Confluence is out of the component documentation, and the checker now enforces it.** Gabriel, 22 Sep: *"No Confluence page should be present or mentioned in the documentation"*, with one exception he confirmed — **provenance stays**, because an `-audit` recording where a fact came from is the only place the word is useful. **Three live URLs were found, all in component docs.** Two were **images that loaded from Confluence rather than from `images/`** — `phone-number-field` and `tag` — and neither file existed locally, so each page was rendering one picture that worked and one that only worked for someone signed in. Both sat directly under a local image that already did the job, so both were deleted rather than downloaded. The third was a CDP database link inside a sentence in `button.md`; the sentence stayed, the link went. **The template lost its publishing section and the two rules built around it** — *"Confluence, when a doc is published there"*, the framing that layout rules cover *"a Confluence page built from it"*, and the five-column table cap whose stated reason was that Confluence splits wider tables silently. The cap survives; its reason is now about rendering generally. **`components-index.md` lost its note** about `API Web` sub-pages living in Confluence. **Why the existing check missed all of it, and it was right to.** `check-tool-neutral.py` strips links before judging a line, by Gabriel's earlier ruling that *"a doc may point at a tool but must not be written in terms of one"*. Confluence now has **no link exemption**: it is matched against the whole line, and the pattern includes `atlassian.net` because a bare attachment URL never spells the word out. **Proved it bites** — a Confluence link and an attachment URL were appended to `tag.md`, both were caught, and the file was restored. **Two grammar faults in `coverage.py` fixed in the same pass**, both mine from 16 Sep and both only visible now the gap has fallen to one: *"1 components"*, *"All 1 carry a sentence"*, and *"These have no page"* for a single row. A `plural()` helper now covers the six sentences that counted. **Nothing was found to update in the coverage ledger itself** — Gabriel asked for it to be brought up to date and regenerating it changed nothing; it had already been regenerated after the seventeen new docs landed. **75 of 98 registry entries now have a doc, and `Pop-up` is the only selectable one left without.** |
| 22 Sep 2026 | **The link check reads filenames, not just links — and the eleven stale names it found are fixed.** Two rules files had been telling every session the indexes were `components.md` and `tokens.md`, months after both were renamed to `-index.md`; `tokens/review-progress.md` named a `colors-tokens/` folder that has never existed, in nine places. **Both backlog rows closed together**, as the second one asked. **`check-links.py` now answers a third question:** does a filename in single backticks still name a file that exists? **346 names checked, alongside 2,366 links, and clean.** **A bare name is a name, not a path** — every real file is indexed under every suffix of its path, so `surface.md` in a rules file resolves to the one in `tokens/color/`. **Raw, the scan found 108 unresolved names and was unusable; five filters take it to the 11 real defects.** Skipped, each with its reason in the code: placeholders (`<name>-figma.md`, `report-run-NNN.md`); grammar fragments (`-audit.md`), which are suffixes and not filenames; `~/` paths, outside the repo by construction; `project/backlog.md` and `project/decisions.md`, which record names true on a date; and `.json` names, because the web code repo's files are named here by the dozen and none is ours to find. **Four real files that live elsewhere are listed by name with a reason each** — two Confluence pages `component-web-ai-docs` writes, and two worked examples in `how-a-run-is-reported.md`. **Proved end to end, twice:** ten synthetic lines run through the matcher, where all three old strings flagged stale, all three new ones passed, and placeholders, `~/` paths and a `python3 …` command line were not read as names; then a throwaway file carrying `components.md`, which made the real run fail with exit 1 and pass with 0 once removed. **Three sentences describing the checker as answering two questions were corrected** — `scripts/README.md`, `README.md` and `CLAUDE.md` — on Gabriel's go. **`status.md` is skipped by the name check, and the check itself made the case:** it failed on the very sentence in the done log reporting this fix, because that sentence has to name the old file. It joins `project/backlog.md` and `project/decisions.md` for the same reason — all three record what was true on a date — and every rename task after this one would have hit the same wall. Its live pointers are links, still checked. `check-rules-docs.py` held at 5 disagreements, none added |
| 22 Sep 2026 | **No usage doc explains a component in terms of a tool any more — 58 lines gone, `check-tool-neutral.py` clean.** **Every one of the 58 named Figma.** Zeroheight, Confluence and Storybook were already clean, appearing only inside links, which is the form Gabriel ruled correct. **Roughly 40 of the 58 were mine**, written between 19 and 22 Sep. **Ten `-figma.md` sub-pages exist, and not one is empty** — `action-menu`, `alert`, `autocomplete`, `carousel`, `cell-content`, `dropdown`, `info-state`, `media-upload`, `navigation-bar`, `wizard`. Each usage doc links to its own. **66 components have no such page and should not**, which is Gabriel's rule: only where there is something to say. **What moved rather than died:** six `Figma tip` blocks, the *"feel free to detach the component"* sentence repeated in three docs, `cell-content`'s `Placeholder left alignement` misspelling, and `navigation-bar`'s explanation of why it has a single variant — a technical account, not a usage one, on Gabriel's reading. **What was simply cut:** sourcing lines, and the words *in Figma* hanging off measurements. **A measurement is a property of the component**, so *"Height: 20 in Figma"* is now *"Height: 20"*. **One thing I got wrong twice and the checker caught both times.** First the checker treated any line holding a URL as a link, which let three tips through — a line can be a tip **and** carry a link, so links are now stripped before the line is judged. Then the sentence I added to each doc pointing at its sub-page said *"Working in Figma?"* — naming the tool outside the link, ten fresh findings. Reworded to *"a tool specification"*. **Neither would have been found by reading.** **Also landed:** the rule is in `component-template.md` so a new doc cannot reintroduce it; `-figma` is in the filename grammar in `README.md` and `CLAUDE.md`; `.claude/rules/component-docs.md` records the sub-page; and `template-drift.py` was globbing every `*/*.md` and would have measured a Figma page against the component template — it now matches the usage doc alone, as the other three checkers already did. **Deliberately not done:** the readiness table's own accuracy, which is Gabriel's separate task with access to the web, iOS and Android repos |
| 22 Sep 2026 | **The last four selectable components have docs — the gap is down to the one Gabriel parked.** `Badge Store`, `Feedback Thumb Buttons`, `Estimation card` and `Map template`. **Documented: 71 → 75; selectable gaps 5 → 1**, and the one left is `Pop-up`, held since 21 Sep until its overlap with `Modal bottom sheet` is settled. All 80 docs match the template, links clean at 2,366, no doc names a tool, and the ruleset check **held at 5 disagreements — none added**. Ruleset inventory and disk agree on all 96 rows. **They fill 13 to 14 of 16 sections**, against 9 to 12 for the five written on 21 Sep. **Two tool specification pages were written**, because both had something real to say: `estimation-card-figma.md` and `map-template-figma.md`. **Ten → twelve.** **The finding that outranks the docs:** **`Map template` is built entirely on components the library itself marks outdated** — 81 superseded price pins, three in every one of its 27 variants, plus 27 superseded floating actions. **The current pin sets are used zero times.** **Proved twice, two ways:** once by walking every variant, then re-checked with a differently-written query that tallies instances by name. Both returned 81 and 27. **It makes one ruleset line false today** — the pin sets are said to be *"reached by placing `Map template`"*, and placing it reaches the old set. **Gabriel, 22 Sep: the map is one product's, already implemented, and not a priority**, so it is a question rather than a task. **`Estimation card` embeds `Feedback thumb buttons`**, which is the first place in the repo where that unadopted component is shown genuinely in use. **Two typos preserved verbatim** in `Map template`, because a sync matches the literal string: the axis `Plateform`, and the option `Very zommed / 3D`. `Estimation card` has a third — its confidence axis was never renamed from `Property 1`. **Seventeen variant combinations are missing across the two**, all recorded as untested rather than guessed at. **On the connection:** `figma-cli` reads live Figma through a plugin in Safe Mode, **one file at a time, and it is the file the plugin was launched in — not the active tab**. The documented `FIGMA_FILE` override **fails silently**, returning the wrong file with no error, so every read this task made was preceded by confirming `figma.root.name`. Moving the plugin between files ends its session and it must be relaunched |
| 22 Sep 2026 | **The web navigation is documented as one system — four docs, and the ruleset now says so where an agent chooses.** `Navigation bar` rewritten, `Mega menus`, `Burger menu` and `Menus` written from scratch. **Documented: 68 → 71; selectable gaps 7 → 5.** All 73 docs match the template, links clean, and the ruleset check held at 5 disagreements — none added. **The rule an agent reads now says the three belong together:** the bar holds the entries, the mega menu is what one opens on desktop, the burger menu is what replaces it on mobile, and **the width decides — never a free choice**. **One readiness row was wrong and is corrected:** `navigation-bar.md` claimed **Ready** on web while its own next sentence said the component had not been developed. No navigation bar exists in the web code, in any package. **Gabriel settled the single-variant question:** it is deliberate, a Figma constraint, and better for them — **breakpoints are handled by Figma variables rather than variants**, so one component works at every width. **The development architecture does not mirror it**, and the doc says so, to stop a reader treating the Figma structure as a build description. **`Mega menus`' ownership note was wrong and is corrected:** not *"not built or maintained by the design system"* full stop, but **the Figma was built by the design system team**, the component is **not stored here yet**, and **the transfer is ongoing**. **`Menus` got a doc despite being never-select**, the same shape `Button Card` uses — it describes the component and opens by saying it is not permission. It is the profile and language panel: six branded profile variants, one unbranded language variant, and `Type` tracks the content rather than being a free choice. That is the first concrete account of what it is, which the open investigation needs |
| 21 Sep 2026 | **The checker stopped comparing a doc against the wrong rule row.** `norm()` was stripping parentheses, so `Navigation Bar (App)` and `Navigation bar` folded to one key, the second row overwrote the first, and the line printed about `navigation-bar.md` was **false**. **Rule rows 70 → 71; disagreements 6 → 5; one bogus "no rule names it" line gone.** **The first fix was wrong and the checker caught it.** Simply keeping parentheses broke `checkbox-group` and `radio-button-group`, which write `**Button group (multi-select)**` — a qualifier, not a name. **Nothing distinguishes the two by spelling**, so `resolve()` now asks the ruleset: if the full form is a Choose row it is a name and is kept, otherwise the brackets were a qualifier and are dropped. When neither form is known both sides fold the same way, so a doc and its row still agree with each other. **`bolded()` now returns raw text** rather than normalised names, because `norm()` turns brackets into spaces and left `bare()` nothing to strip — the second wrong attempt, also caught by re-running. **One doc edit came with it:** `navigation-bar.md` wrote `**Navigation Bar (App), mobile only**` as a single bold span, which reads as a component called *navigation bar mobile only*. The qualifier now sits outside the asterisks. **That limit is recorded in the script's own docstring** so the next writer is not caught by it |
| 21 Sep 2026 | **`Date Field` and `Feedback Bar` have docs — the first two written from the Patterns library.** **Documented: 66 → 68; selectable gaps 9 → 7.** Both match the template, `check-links.py` is clean, and neither added a disagreement — still 6. **`Date Field` turned up a contradiction the ruleset cannot see:** the rule sends you there when *no calendar is offered*, and **the Figma component carries a calendar button** at the right-hand end of the field. Whether it opens a calendar, is decorative, or is a leftover from the `Date picker` it shares a page with is **not tested**; the doc says to follow the rule, not the button. **Also found:** on web there is no standalone date field — `DateField` exists only inside `DatePicker` and is not exported, so a web build cannot place one. **`Feedback Bar`'s scale is one to five**, built from a button group configured with seven slots, two of them hidden — a Figma leftover, not a rule. **Its scale carries no anchor words**, so a screen reader announces five bare numbers. **Held back on purpose:** `Burger menu` and `Mega menus`, which belong to the navigation topic and would be written twice otherwise |
| 21 Sep 2026 | **`Button Card` is forbidden, and the four places that sent an agent to it were moved the same hour.** Gabriel: available on no platform, so not selectable. **Three rule rows** — `Button`, `Card`, `Select card group` — and **three docs** now route to `Card`, with the whole container as the action. `Card`'s own row had been deferring that exact case to `Button card`, so the case simply returns to it. The `button-card.md` page keeps its content and opens with a never-select banner, the same shape `Cell Content` uses. **`check-rules-docs.py` went 9 → 6 across the change** — the two new disagreements it raised were the docs not yet moved, and both closed. **The coverage ledger picked the change up on its own**, and now warns about `Button Card` beside `Cell Content` without being told to. **Removal from Figma is not settled** and is an open question |
| 21 Sep 2026 | **Six of your rulings are in the ruleset, and one correction went the other way.** **`Score Tag` is built on web** — my own doc said it was not, written two hours earlier. It lives in `@gsl-core-web/design-system-patterns-tag`, which is why every search of `libraries/ui` missed it. The doc now records the four translated tier labels, and that **Figma defaults to `Diamond` while web defaults to `bronze`**. **The same scan gap ran through the colour audit:** `content.md` said the score tokens serve *"a component that has no implementation"*. Corrected, with the general lesson written in — **a zero in that file means "not used in `libraries/ui`", never "not used"**, and `Floor selection`, `Phone number field`, `Map pin`, the charts and the energy class slider all sit in the same unread package |
| 21 Sep 2026 | **A component may now be flagged as built outside the design system, and it is not the same as Never select.** Your wording: usable by any team, and **selecting one is the signal it should be adopted properly**. Three carry it — `Score tag` (patterns package, seller lead scoring), `Feedback thumb buttons` (another team, Figma only), `Badge store` (Figma only on purpose, a replica of two badges Apple and Google publish, mostly footers and landing pages). **Also ruled:** `Alert` is a fixed-shape modal expecting a decision back, replicable on web from `Modal bottom sheet` until it is built; `Pop-up` is apps only and stays undocumented until its overlap with `Modal bottom sheet` is settled. **`Navigation Bar (App)` has its own rule row** in your words — the main way a user navigates an app, and that is the whole of it |
| 21 Sep 2026 | **Three docs written from a live Figma read — the first of their kind here.** `State Message`, `Navigation Bar (App)` and `Score Tag`. **Documented: 63 → 66; selectable gaps 12 → 9.** All three match the template with nothing outstanding, and `check-links.py` and `check-rules-docs.py` both stayed where they were — seven disagreements before, seven after. **What the Figma read gave that no other source could:** `State Message`'s five types each bind their own colour and their own gap token, and `Helper` alone has no icon — which the web build confirms. `Score Tag` wraps a `Tag` that is **not exposed**, so its four tiers are fixed rather than conventional. `Navigation Bar (App)` is 49 high on iOS and 80 on Android, the label moves beside the icon on iOS tablet only, and **Android tablet tabs cap at 168 instead of stretching** — a real platform difference nothing in the repo recorded. **One gap it also exposed:** the new `Navigation Bar (App)` doc has no rule sending an agent to it, which is now its own row |
| 21 Sep 2026 | **The five components with web code have docs — documented 58 → 63.** `Badge`, `Image Slider`, `Loading State`, `Text Button` and `Tooltip`, each written from its rule row plus `~/gsl-core-web-design-system` and nothing else. No Zeroheight page, no Figma. They fill 9 to 12 of the 17 template sections against the pilot's 8. **Where a source gave a capability and no rule for choosing it, the doc says the condition is not documented** rather than inventing one — the failure that cost four invented conditions on 18 Sep. **Gabriel's reading found five things no check could see**, all on components he opened: `Badge` may stand alone rather than only being pinned to a host, which corrected its rule row; `Text Button` exists because `Button`'s horizontal padding stops a tertiary button aligning under a text block, so "revealing more content" is one use and not the job; `danger` is for erase and delete and `inverted` is a dark surface and nothing more; an external link takes the external icon, **and the built component does not supply it**; and a form's submit is a **primary** `Button`, correcting a misreading of his own earlier note. **Two rules he settled outright.** Attributions leave component docs because people read them — `CLAUDE.md` carries the exception now, and `phone-number-field`'s was the only other one in the repo. And `Image Ratio` joined **Never select** as a Figma-internal helper for designers, which removed the one pointer in the whole ruleset that sent an agent to a component with no rule row and no doc. **Checks:** links clean at 2,244; template drift 0; `check-rules-docs.py` unchanged at the same 7 pre-existing disagreements; the inventory and the filesystem agree on all 96 rows |
| 18 Sep 2026 | **The coverage ledger stopped keeping its own copy of what the ruleset already says.** Three fixes Gabriel approved in one sitting, after asking whether every component is documented now. **One: a figure was typed in, not counted.** The ledger's caveat on the `Platform` row read *"which of the 29 are deliberate"* — 29 was correct when written on 16 Sep and was 31 by 18 Sep. It is now counted from the column it describes. **Two: the ruleset's inventory had gone stale in ten rows, not the eight first reported.** Seven still read `⚠︎ Undescribed — see audit` for components the same file describes forty lines earlier — `Badge Store`, `Feedback Bar`, `Feedback Thumb Buttons`, `Mega menus`, `Estimation card`, `Button Card Group`, and `Button Bar`, **which had gained a full doc in the immediately preceding commit**. Three more had been added to the never-select table on 18 Sep and never marked 🚫 in the inventory — `Burger menu (profil)`, `Menus`, `Filter button`. All ten rewritten from the ruleset's own words, none invented. **Three: the script now reads the inventory instead of duplicating it.** Its two hand-kept tables — which components may never be selected, and what each undocumented one is for — were copied out of `components-audit.md` on 16 Sep and were wrong two days later. Both are gone; `coverage.py` parses the inventory, treats a purpose beginning 🚫 as never-select, and quotes the ruleset's sentence verbatim. **What that changed in the numbers.** The gap fell from 22 selectable-but-undocumented names to **18**, because four turned out to be never-select. **Nothing reads `Unknown` any more** — on 16 Sep six did. **And a check was added so this cannot happen quietly again:** every run compares the inventory's `Doc` column against the filesystem and prints any disagreement into the page. Tested against a deliberately broken copy of the ruleset, where it named `Button Bar` correctly. **`components-template-drift.md` was regenerated in the same pass** — it had never been re-run after `Button Bar` landed, so it counted 59 docs instead of 60. **Two things deliberately not done, both needing their own chat:** whether a section may be marked *not relevant*, which the three new writing bullets made urgent, and writing the five missing pages |
| 18 Sep 2026 | **`Button Bar` has a doc — the first written from web code rather than Zeroheight, and the shape Gabriel approved for the rest.** Built from three sources and nothing else: **his own one-line description** of what the component is, **its ruleset row** for `When NOT to use`, so `check-rules-docs.py` passes on it, and **`~/gsl-core-web-design-system` for the behaviour** — two button slots, a responsive direction, `displayMode` page or modal, and the rule that a single button may span the width while two never do even if both ask. **Every section it cannot fill says `Not documented` and says why** — no invented conditions, the discipline that cost four cut claims earlier the same day. **One thing was cut on his reading:** a note under `Variants & Modifiers` saying no Figma read had been done and where the variant count was recorded. **A component doc is platform-neutral** — that knowledge belongs where the agent looks for Figma, not in the doc. **It already has a home** and needed no new row: `Button Bar` is one of the 32 registry entries carrying neither `nodeId` nor `auditedDate`, which is the exposure-walk task. **Documented components: 57 → 58.** Selectable with no doc: 16 → 15 |
| 18 Sep 2026 | **The check that proves a doc and the ruleset still agree now exists — `scripts/check-rules-docs.py`.** It compares each doc's `When NOT to use` against its ruleset row's `Otherwise`, **as sets of component names, never as prose** — the doc is meant to rephrase. **It found 12 disagreements on the day it was written, and five of those were made that same afternoon** by the new rows for `State message`, `Score tag`, `Date field` and `Mega menus`: each row named an alternative the target doc did not name back. All five are closed. **That is the case for the script in one line** — five drifts created and caught within hours, by a change nobody would have thought to re-check. **It also reports two things it cannot compare rather than passing them silently:** six docs whose `When NOT to use` is prose instead of the template's table, and twelve reachable only through an `Otherwise` cell with no `Choose` row of their own — the second is not a fault and the output says so. **Two parsing rules were needed and are commented:** a bold span may carry more than the name (`**On web → Pop-up**`), so the name is what follows the last arrow; and a span saying something is deliberately *not a component* is not a name |
| 18 Sep 2026 | **Asking the user has its own section, and the two components in it are told apart by what they ask for.** Gabriel, 18 Sep: **`Feedback bar` asks for a notation, `Feedback thumb buttons` asks for a like or dislike** — which replaces the guess written earlier that day, that they differed by how much the block carried. **The guess came from Figma properties and was labelled as a guess**; the correction is his and is dated in the file. *Providing feedback and status* had grown to hold **two opposite directions** — seven rows where the system tells the user something, two where it asks — and the two now sit under *Asking the user for something*, his idea. **One line was added that neither of us raised:** `Rating` is not an alternative to either, because it displays an opinion that already exists and is non-interactive, while these two collect one |
| 18 Sep 2026 | **Two open questions in `components-audit.md` are struck through and answered.** Question 4, `Filter button`, open since the audit was written: only inside `Filter bar`, Gabriel's decision. Question 2, the seven components described nowhere: **six of the seven answered by him from memory**, each with a one-line purpose now in the file; **`Estimation card` is still open** and is what the question now means. The convention the file already used — strikethrough plus a dated answer — is followed |
| 18 Sep 2026 | **Every component in the libraries can now be reached by a rule — 94 of 94, from 78 that morning.** The last six were the ones nobody had ever described, and **Gabriel described five of them from memory on 18 Sep**; the sixth came out of its Figma properties. **Routed:** `Button bar` — two buttons at the foot of a form, sticky or not, confirmed by web code whose props are exactly two button slots plus a responsive direction; `Badge store` — App Store and Google Play links, our own replicas, with a pointer separating it from `Badge`, one word away and unrelated; `Feedback thumb buttons` — asking the user's opinion, **not built on web, checked at his request and confirmed absent**; `Feedback bar` — the same job as a laid-out block with an illustration and its own buttons; `Mega menus` — top-level navigation on the main B2C and B2B sites, **not built or maintained by the design system**, in Figma because several teams share it, and **provisional** like `Menus`. **Forbidden:** `Button Card Group` — never developed, should be removed from Figma. **Two things are written down as unknown rather than guessed at:** no usage rules exist for the thumb buttons, and the boundary between the two feedback components is read from Figma properties rather than any rule — both labelled inside the ruleset and both a backlog row. **Gabriel's idea that asking and telling belong in separate sections is a row, not a change**, because he offered it as an idea |
| 18 Sep 2026 | **`Filter button` is forbidden — it is what a `Filter bar` is built from.** Gabriel, 18 Sep: use it only internally, inside the bar. **Added to Never select alongside `Filter dropdown container`**, which is the same relationship to the same parent, so the two now share one row. **The evidence that made it decidable** was an absence: `~/gsl-core-web-design-system` has no `FilterButton`, while `ImageSlider` and `LoadingState` both do — and those two turned out to be real standalone components the same afternoon. **A question that had been open since the components audit is now closed**, and `components-audit.md` still carries it as open question 4 — that file describes the system, so it needs the answer written in. **Not done here**, because editing an audit is a file change like any other. **Components with no rule at all: 7 → 6**, and all six are the undescribed ones |
| 18 Sep 2026 | **`Menus` is on Never select, provisionally, and the word matters.** Gabriel, 18 Sep: it was built to cover international content needs, nothing requires it to build anything today, and the decision should evolve when confronted with real product. **Recorded as provisional in the ruleset itself**, with the date and the reason, pointing at `Navigation bar` — whose own variant flow already lists the language menu among its controls. **A backlog row carries the trigger to revisit**, because a provisional entry with nothing scheduled to re-open it is just a permanent one that reads as temporary. **The precedent is `Tab Bar`**, already on the list as *mid-refactor, unclassified*. **Components with no rule at all: 8 → 7** |
| 18 Sep 2026 | **Three more components are routed and one is forbidden — and the three I had proposed forbidding were wrong.** Gabriel pushed back on *Image Slider, Filter button and Loading State to Never select*, and checking the **web code repo** settled it: `ImageSlider` and `LoadingState` are real standalone components in `~/gsl-core-web-design-system/libraries/ui/src`, with their own props — the slider takes `images`, a ratio, swipe and an optional link; the loading state takes a title, description, spinner size and variant. **Neither is a part of anything.** My reading had come from repo docs not mentioning them, which is absence of documentation, not evidence of being internal. **Routed:** `Image slider` under *Identity and media*, distinguished from `Carousel` by the code — images only against any content; `Loading state` under *Providing feedback and status*, with its overlap against `Info state`'s own Loading variant settled in the Otherwise; `Burger menu` under *Navigating between pages and sections*, from Gabriel's own description. **Forbidden:** `Burger menu (profil)` — *already adapted to consumer-content needs, never to be used*, Gabriel 18 Sep, dated in the file. **A confusion of mine, corrected:** I wrote `Filter button` and he read `Filter bar`. They are different components; `Filter bar` was always routed and is untouched. **Components with no rule at all: 12 → 8** |
| 18 Sep 2026 | **Four of the nine unrouted components now have a rule row, and the other five turned out not to be routable.** The task was *route the 9*; reading the evidence first changed the shape, and the honest answer is better than nine invented rows. **Routed, each from words the repo already had:** `State message` — three docs' Related tables describe it as inline feedback inside a form field; `Score tag` — `energy-tag` already sends seller lead scoring to it and `tag`'s own DON'T forbids using a Tag instead; `Date field` — `text-area` says date fields only input dates, and `date-picker`'s flow already pointed at it; `Floating button group` — it has a full doc and simply had no row, so its own `When to use` and `When NOT to use` became one. **Pointers were added in both directions**, so an agent on `Tag` learns `Score tag` exists and one on `Feedback message` learns `State message` does. **Two naming defects, same shape as `Feedback Messages`:** the library sets are `State Messages` and `Feedback Messages`, plural, while the rules name them singular — the library name is now stated in the row where each is chosen. **The five not routed are two questions in this file**, three that look like parts of other components and two burger menus nobody can tell apart. **Components with no rule at all: 16 → 12** |
| 18 Sep 2026 | **`Phone number field`'s breakpoint table was inverted, and is corrected.** It read *Web XXS-XS (0-599px) → Dropdown* and *Web SM-XXXL (>599px) → Bottom Sheet* — the reverse of every other component in the system, where **Showing context-specific actions** sends narrow widths to a bottom sheet and wide ones to a dropdown. **Gabriel confirmed it was inverted, 18 Sep.** Both the table and the variant flow now read bottom sheet on narrow, dropdown on wide, and the correction is dated inside the file. **The image kept its `Dropdown` caption and moved to the row that now says Dropdown** — it was the only picture in the table, so nothing else had to be matched. **Raised the same day it was fixed**, which is what *propose the correction in the same task* is for: it was found while rewriting that flow, and filing it as a row for later would have left a doc that still looked authoritative |
| 18 Sep 2026 | **Five of the seven thin variant flows are extended, and the material was already inside their own pages.** That is the finding: the flows were thin because nobody had read past the `Usage` section, not because the source was empty. **`filter-bar`** gained the all-filters button and the primary button, both sitting unused in its `Modifiers`. **`media-upload`** gained the file counter and the filename, including that a hidden filename still shows under a non-image preview. **`legend`** gained position — below or left, never above — and interactive versus static. **`chip-group`** gained wrapping and the fact that action chips cannot be selected, and is no longer byte-identical to `chip`. **`phone-number-field` was rewritten outright**: its four axes were all runtime states repeating its own `Interactive States & Loading` section, and are now label visibility, the per-platform label behaviour, the country selector by breakpoint and the shared form header. **Four claims were written and cut the same hour, all invented conditions**: *never mix chip types*, *the group never scrolls horizontally*, *below is the legend's default*, and show/hide conditions for `filter-bar`'s two buttons. Each is now a visible *not documented* line, and all seven are one backlog row. **The legend cut mattered most** — `components-rules-ai.md` says `Donut chart` can only place its legend beside the chart, so a default of *below* would have contradicted the ruleset; the flow now points at **Where a chart's legend sits**. **`counter-field` and `donut-chart` could not be done** and are a narrowed row: one has nothing but the shared form header, the other is `To Do 🚧` on all four platforms |
| 18 Sep 2026 | **A radio button group's option limit is now readability, not a number — changed in all eight places that stated it.** The old rule said `≤5 options`, and `radio-button-group.md` contradicted itself: line 28 capped at five, line 34 sent anything above five to `Dropdown`, and line 52 offered a two-column layout at **six or more** — a branch the cap made unreachable. **Gabriel's rule, 18 Sep: two columns is what raises the limit, and what decides it is whether the label still reads.** Up to 5 in one column; 6 to 10 in two columns when every label fits **2 lines or fewer on mobile**; in one column a label may run to **3 lines, 2 recommended**. Above 10, `Dropdown`. **Two columns on mobile are allowed** — the source's *especially on mobile* for one column is a default, not a ban, confirmed by him. **Ten is a ceiling with one named exception**, because he said the rule should move with project context and an agent told a rule can evolve will ignore it: when the choice is the whole screen with no competing content — an onboarding step — the count may go higher, and it is recorded in the run's report beside the **When nothing fits** declarations. **Changed in:** `radio-button-group.md` (When to use, When NOT to use, Related, and the flow), `dropdown.md` (When NOT to use, Related), `components-rules-ai.md` (both rows plus the exception note), `components-eval.md` (tests 3 and 4 cited *the ≤5 threshold*; both still expect the same answer, since 12 exceeds 10 and is *in a form*, so the exception cannot apply). **Verified: no `≤5`, `>5 option` or `Six or more options` string is left anywhere in `components/`** |
| 18 Sep 2026 | **All 57 variant flows read end to end; nine faults found, seven fixed.** **Two were real contradictions.** `button-group` said *8 or 9 items → use it* and, nested directly under it, *above 7 → use Chip group or Dropdown* — both cannot hold. Split into three siblings, keeping the source's own facts: the component holds 2 to 9, above 7 is energy or CO₂ selection on desktop and nothing else. `radio-button-group`'s is **not fixed and is a rule change** — see the question in this file. **Four were an axis-wide rule trapped inside one branch**, so it read as applying to that branch only: `carousel`'s *arrows are mandatory on web*, `button`'s *match the size of an adjacent button or field*, `line-chart`'s dots accessibility note, `filters-and-actions`' user-testing requirement. All four moved into the axis heading after an em dash — **the convention six flows already used**, including `energy-tag` and `progress-bar`. **One was on the wrong branch outright**: `rating`'s *never hide the rating amount in the condensed variant* sat under *Full, all stars*. **One was a gap inherited from the source**: `tag` allowed dropping the label when the icon is recognised while still calling icons optional, so nothing forbade a tag with neither. **What the read also showed, against expectation:** the custom components have the strongest flows — `energy-tag`, `tables`, `modal-bottom-sheet`, `listing-card`. The weak ones are the simple components, and they are now three task rows |
| 18 Sep 2026 | **`listing-summary` has its `Related Components` table, and *Related* reaches 57 of 57.** It was the last doc of 57 without one. Four rows in the house style — itself, then `Listing card` (High), `Card` (Medium), `Table` (Low) — each carrying that doc's **own opening sentence** as its Usage, so no component is described twice in two different ways. **`Card` gets both halves of its relationship stated**, because it is the one component that is simultaneously an alternative and a container: pick it instead when the content is not a property, and place a listing summary inside it when it is. **Found while writing it:** `listing-card`'s own table names `Listing summary` as plain bold text with no link, while every other row in that table links. Not touched — it is a sentence in a doc, so it is Gabriel's call |
| 18 Sep 2026 | **Every component doc now answers when to use it and when not — 57 of 57.** `floor-selection` and `listing-summary` were the last two saying `Not documented` on both; `components/coverage.py` puts *When to use* and *When NOT to use* at 100%, joining *Variant flow* from earlier the same day. **Written in the house style the other 55 already use** — a bold one-line definition, a tier-and-kind line quoting **Highest tier first**, then an *Instead, when… | Use* table — copied from `phone-number-field` and `listing-card`, which are the same Experience tier. **Every row traces to a source:** floor-selection's two DON'Ts already named `Counter field` for room counts and `Text field` for larger numbers, and its 0-to-100 limit and letter ban come from its own `Content & UX Writing` and states sections; listing-summary's rows come from its one DON'T and from the ruleset's own split — *a property summary card* is `Listing Card`, *a more flexible property summary than Listing Card allows* is `Listing summary`. **One row was drafted and cut:** *comparing many properties as tabular rows → Tables*, lifted from `listing-card`, which would have contradicted this doc's own DO saying a listing summary can sit inside a table. **What is left on this doc:** `listing-summary` is now the only one of 57 with no `Related Components` table |
| 18 Sep 2026 | **Every component doc now answers which variant to pick — 57 of 57.** `floor-selection` and `listing-summary` were the last two saying `Not documented`; `components/coverage.py` puts *Variant flow* at 100%. Both were written **from each doc's own words only** — floor-selection's widths from `Touch Target & Layout`, its header from `Modifiers`, listing-summary's seven slots from the `Sub-components` table — and no Figma property is named in either, so the docs stay platform-neutral. **Three claims were corrected before they landed:** *never full-width* softened to *not recommended*, which is what the doc says; the label rule reworded to the doc's own *starting with a capital letter*; and a pointer to **Which component** fixed to **Highest tier first**, which is where the Listing card versus Listing summary split actually lives. **What this came out of:** an assessment of the Smashing decision-tree article. The conclusion was that the repo already has both layers the article describes — intent tables for which component, axis lists for which variant — and that adding trees would duplicate them. Human-facing trees were proposed and Gabriel declined them |
| 18 Sep 2026 | **Every component that had an image gap now carries every picture its Zeroheight page holds.** 25 components, **371 pictures**, done in four batches, each read by Gabriel before it landed. **0 sentences lost, 0 duplicated, 0 `zeroheight.com` links gained**, on every one — measured against each doc's committed version. Biggest gains: `coach-mark` 3 to 21, `floating-button-group` 3 to 14, `date-picker` 21 to 70. It took **28 fixes to `scripts/zeroheight-merge.py` and one to the extractor**; nine came from Gabriel reading the drafts and two from re-running components already merged, including one where a fix destroyed 12 pictures silently. Every fix is commented at the line that caused it |
| 18 Sep 2026 | **The 36 files that were not pictures are gone.** Figma node JSON saved under a `.png` name by the old Confluence migration, each written into a doc as an image and each rendering broken. **Proved by reading the bytes.** Eleven were replaced by the pages' own sections; 25 were removed outright, file and reference together, leaving a visible gap rather than a broken icon. `scripts/check-links.py` could never see them — it asks whether a file exists, never what it is — so **the merge script now reports any referenced file that is not a picture** |
| 18 Sep 2026 | **Two things the old migration left in every doc are gone.** 60 files carried a `<!-- Source: …confluence… -->` line that nothing read. And every component doc opened with an H1 repeating its own filename, so the name was read twice — removed from all 62 docs and from the template. **That second one moved the page hero**, which used to sit under the H1; a doc with no H1 is all preamble, and the preamble passed straight through, so the hero would have stopped being replaced in silence. See `project/decisions.md` |
| 16 Sep 2026 | **`Cell Content` gained an icon-alignment axis and a zero padding, and the registry entry it had was wrong in three ways.** Gabriel changed the component in Figma, then asked for the doc and the registry to follow. **Live read, then verified by a second independent query:** 20 variants became **33**; the new variant property is `Placeholder left alignement` (`Middle`/`Top`, misspelling preserved, **not** the `placeholder-left-alignement` the brief named); `Padding` gained `0`. **Three limits are enforced by the variant set, so the doc does not have to claim them:** `Top` exists only with `Alignment=Horizontal`, `Padding=0` only with `Clickable=OFF` (three variants, the complete non-clickable set), and `Clickable=OFF` only ever pairs with `State=Default`. **Reclassified Pattern 1 to Pattern 2** — `.placeholder left` is exposed on all 33 variants, giving a designer a `Type=Image/Icon` switch on the leading slot. Gabriel confirmed the old Pattern 1 entry was **simply wrong**, not out of date; it carried no `nodeId` and no `auditedDate`, now a trap in the sync skill. **Two rules nothing can enforce went into the doc instead:** `0` padding is forbidden on a clickable cell content, because Figma dropped those variants but the web component still accepts it; and a shown trailing icon forces `Middle`, which no channel can block at all — `Icon Right` is a BOOLEAN, and a boolean cannot be removed from a combination the way a variant can. **The geometry was measured rather than copied from the brief:** under `Middle` the 24×24 icon centres on the whole text block, under `Top` on the title's own centre, and the trailing icon stays on the whole-block centre under both. The equal top edges under `Top` follow from icon and title both being 24px, and are not the rule. The body-only case is labelled as intent, unmeasured, because the title is a toggle rather than a variant. The stale Figma link was swapped to the GSL library — the other 39 are a note |
| 16 Sep 2026 | **Component documentation coverage is now counted rather than judged.** `components/components-coverage-ledger.md` scores all **98 registry entries** against the component template, and `components/coverage.py` regenerates it. **Modelled on a Confluence page and deliberately built differently.** That page — *Component Documentation Coverage Audit*, `3480193375` — decided each cell by reading pages; here every mark is a string match, because the repo docs already carry the literal words `Not documented` 156 times. **Three marks, no fourth.** `✅` the section has content · `❌` the section says `Not documented` · `⬜` the heading is absent. **The Confluence page's ➖ *Unrelevant* was dropped on purpose** — it states that its own ➖ values were *inferred from component type*, which is a guess presented as a fact, and no relevance rule exists to replace it with. That question is now in *About components*. **What it measured.** 55 entries have a doc, 26 entries have none and are selectable, 17 have none and never should be. **506 of 715 template sections are filled — 71%.** Four sections are at 100% on all 55: *When to use*, *When NOT to use*, *Variant Selection Flow*, *Related Components*. **`Accessibility` is at 11%** — 49 of 55 say `Not documented` — and **`Breakpoints` at 33%**. Those two became a task row. **Three judgement calls that changed the output, all recorded in the page itself.** `Bar graph`, `Donut chart` and `Line chart` are registry entries with real docs, so they score as themselves rather than collapsing into one *Charts* row; `charts.md` is a routing page with none of the template sections and would have scored a row of blanks that meant nothing, so it and the four other support pages are listed instead. A **`Readiness`** column was added and excluded from the score, because the `Figma \| Web \| iOS \| Android` table at the top of a page and the `### Platform` section under *Usage* are two different things — 52 pages carry the first, 26 the second. And the `Platform` row is flagged in the page as the one row where `⬜` is probably correct rather than a gap. **Two bugs were found by checking the output against the files rather than trusting it.** A heading whose content sits one level down — `### Modifiers` carrying everything under `#### Icons` — was scored empty, which put *Modifiers* at 7% instead of 67%; the rule now reads a heading's whole subtree. And an `##` parent saying `Not documented` with no `###` children now marks those children `❌` rather than `⬜`, because the parent already declared them. **One number cross-checked against something written independently:** the ledger finds exactly `Date Picker`, `Phone Number Field` and `Tabs` missing the readiness table, which is the same three a backlog row had named by hand |
| 15 Sep 2026 | **A run now lives inside its brief, and the comparability label is gone.** `compliance/briefs/brief-001/` holds the brief written once, plus `run-001`, `run-002` and `run-003`. Two runs are comparable when they share a folder, which nothing can invent or mistype — run-002's scorer had invented a label because none existed. The three saved brief copies were byte-identical apart from a pasted Figma URL, which is a destination and not a requirement; it now sits in each report's **Destination** row. Run numbers stay global across every brief |
| 15 Sep 2026 | **The run ledger stopped pretending to be a measurement.** Your call: the scorecard measures, the ledger is history you skim. Its per-step columns mirrored the scorecard's steps, so every scorecard edit made them wrong — three edits in two days on a table with two rows. They are replaced by **Failures · Findings · Flags**, the scorecard's three kinds of verdict, which do not change when a question does. Run-002 reads `2 of 7` failures and run-003 `0 of 7`, the improvement the old columns could not show. No measurement changed; both numbers were already in the reports |
| 15 Sep 2026 | **`prompt` and `brief` were two words for one thing, and `brief` won.** The concept was already called a brief in the scorecard's report template, in `generate-and-score` ten times, in `CLAUDE.md` and in `README.md`. Only the filename said `prompt-run-NNN.md`. Renamed, and every reference across six files repointed |
| 14 Sep 2026 | **Neither Figma channel on this machine exposes a file key, and the two channels have different jobs.** Gabriel asked whether the official Figma MCP could supply the `fileKey` that FigCli cannot. **Tested, 14 Sep: it cannot.** The Dev Mode MCP at `127.0.0.1:3845/mcp` is live and answers, but `get_metadata`, `get_design_context`, `get_code_connect_map` and `get_code_connect_suggestions` each return **component** keys — 40 hex characters — and never a file key, which is 22 characters of base62. Through FigCli `figma.fileKey` is `undefined`, and `node src/index.js files` needs CDP, which Safe Mode does not give. **A file key comes from the URL and nowhere else**, so `figma-sync-libraries` must ask Gabriel for it. **The more useful half of his point:** figma-cli is not a replacement for the MCP, it is the cheaper and faster channel for bulk extraction and for drawing, while the MCP is official, read-only and **independent** — which is exactly what makes it the right way to verify a write rather than trusting the tool that made it. All five skills now name both channels and say which job each is for, instead of naming figma-cli as the only path. `generate-and-score` goes further: the building agent draws through figma-cli, the scoring agent reads through the MCP, so neither verifies its own work through the channel that made it. **Also corrected:** the component-sets tier match on file name was written as a limitation and is really the better rule — a tier is a design-system concept that carries to web, iOS and Android, and a file key is Figma-specific, the same reasoning that keeps keys off the icon pages |
| 14 Sep 2026 | **All five Figma-touching skills were pointing at a retired tool, and one of them would have refused work it could do.** Found while running the icon sync: `figma-sync-icons`, `figma-sync-component-sets`, `figma-sync-libraries`, `figma-sync-tokens` and `generate-and-score` all named the **Desktop Bridge** (`figma-console-mcp`), which Gabriel retired as unmaintained, and called `figma_execute`, `figma_get_status` or `figma_get_variables` — none of which exist. **All five now name `figma-cli`** at `~/figma-cli`, not on PATH, driven as `cd ~/figma-cli && node src/index.js run <file>`. **The worst of the five was `generate-and-score`.** Its first precondition said to **stop** unless the session could reach the Desktop Bridge and was running off `~/.claude/` rather than `~/.claude-personal/`. That is wrong twice: the Bridge is gone, and figma-cli is machine-wide and does not care which config root a session booted from. A compliance run would have refused to start and sent Gabriel to another session that would refuse for the same wrong reason. **Two things every rewritten skill now states, because both cost time today.** The daemon and the plugin are **separate checks** — `status` reports `✓ Daemon running` while the plugin is detached, and `Error: fetch failed` means the plugin, not the daemon. And **the plugin stops whenever Figma switches files**, so a sync that worked minutes earlier fails for no reason but the owner opening another document; only Gabriel can restart it. **One real capability was lost, and is written down rather than worked around:** `figma.fileKey` is `undefined` through FigCli and `node src/index.js files` needs CDP, so `figma-sync-libraries` can no longer read a file key from Figma at all. It must now ask Gabriel for the file URL and take the key from it. **The recipe was smoke-tested verbatim** after the rewrite |
| 14 Sep 2026 | **The icon registry is re-synced and the index regenerated, through `figma-cli` rather than the tool the skill names.** Gabriel renamed the three trailing-space icons by hand, then opened the Foundations library and started the FigCli plugin. **The diff was exactly three lines: 3 added, 3 removed, 0 changed**, with each added name sharing its `key` and `nodeId` with the removed one — which is what proves a rename rather than a deletion, so the skill's deletion guard never needed to fire. Nothing else had moved since 27 August across all 455 icons: no key, no node ID, no variant property. `icons/icons-index.md` was regenerated from the registry and its three hand-written tables survived, as the file's own provenance section requires. **Two things were learned that outlast this run.** `figma-sync-icons` still tells every session to run the **Desktop Bridge**, which Gabriel retired as unmaintained, and to call `figma_get_status` and `figma_execute`, which do not exist — the working path is `~/figma-cli`, not on PATH, driven as `cd ~/figma-cli && node src/index.js run <file>`. And **the FigCli plugin stops whenever Figma switches files**, so a sync that worked minutes earlier fails with `fetch failed` for no reason other than the owner opening another document; only Gabriel can restart it. Both are now rows in the skill's `known-traps.md`, along with the trailing-space check itself |
| 14 Sep 2026 | **Five of the seven icon questions are answered, and the biggest finding was not one of them.** Asked and settled in the same conversation: `RDC` is *rez-de-chaussée*, the ground floor · `file-cdd` and `file-cdi` are French contract types and are **France only** · `magnifying-bars` shows a trend or data, most likely for one classified listing, which is Gabriel's own reading and is labelled as such · `square-h` is the hospital sign · `comment` and `comments` are one speech bubble against two. **`One`, `Two` and `Three` stay open — Gabriel does not know either**, and his hypothesis that they served a designer-only Figma case before `Badge` had a large size is recorded as a hypothesis. **The finding underneath all of it: the set appears to be Font Awesome 6.** 33 distinctive Font Awesome names were tested against the registry and all 33 were present. **Guessing, strongly — not proved**, because no diff against the full catalogue was possible without network access. It answered three of the five questions on its own, and if confirmed it means ~440 of 454 names are already documented and only the custom real-estate additions need a human. **Deliberately kept out of the ruleset**: a generating agent does not need the provenance, and an unverified one would invite it to import assumptions from Font Awesome's docs that this design system never made. **A correction landed with it.** `light-bulb` and `lightbulb` were filed as one icon under two spellings and they are two different icons — `light-bulb` sits with `info` and `question` and means an idea; `lightbulb` sits with `dishwasher` and means a physical light. The rename task drops from 17 names to 13, and retiring one becomes a separate question. **The lesson is about the check.** Both times the naming was measured, the measure answered a narrower question than the conclusion drawn from it — first *are these unique*, then *do these look alike*. Neither can see meaning |
| 14 Sep 2026 | **Icons became the repo's fifth pillar, and two of run-002's icon findings are closed.** `icons/` now holds three files and no folder per icon: **`icons-index.md`** — all 455 icons in their 17 categories, generated from the registry, plus the 45 that carry a real variant choice and the seven names nobody can resolve; **`icons-rules-ai.md`** — the ruleset; **`icons-audit.md`** — the evidence and the open questions. **The rule that closed finding ten is stated twice on purpose:** in full in `icons-rules-ai.md`, and again in `components-rules-ai.md` under a new **Icons** section, because that is the page an agent is on when it decides what the user presses. An icon alone has no states, no hit area and no accessible name, so anything pressable sits in a `Button`, `Text Button`, `Link`, `Chip`, `Floating Button Group` or a component's own slot — and a tooltip may be triggered by any component but never by a bare icon. **What the ruleset deliberately refuses to do:** say what `Filled`, `Circle` and `Square` are for. Gabriel said the reasoning was lost and that no variant is a wrong choice today, so the rule is *match what is around you, and declare the choice*, labelled as guidance rather than a rule. Guessing there would have been worse than the gap. **Left out, and recorded as decisions:** no Figma keys — the registry stays the only source of identity; no rendered images; no folder per icon. **`README.md` gained three rows** — the routing question, the ruleset table and the pillar map. **The link check caught a real error before commit:** the ruleset linked to its own audit, which the no-ruleset-points-at-evidence rule forbids. Changed to a plain filename, as the spacing ruleset does it |
| 14 Sep 2026 | **Run-002's eight findings are ruled, and four of them were fixed in the same sitting.** Ruled cold: a fresh chat read `report-run-002.md` and the rules it points at, saw neither the chat that built the run nor the one that scored it, and took one item at a time. **The verdicts:** three `ruleset gap`, two `agent error`, two `accepted`, one `library defect`. **What the ruling changed about the findings themselves.** The legend item was *not* the library placing it wrongly — the library places it where `legend.md` allows, and the defect is a missing alignment property. The 56→2 gap override was *not* an agent error — no rule said what the gap should be, and Figma offered no alternative at mobile width. The `Energy tag` error was *not* the colour clash the report suspected — it is that the tag and the ladder state the same rating twice in one container. **Fixed on the spot, four files.** `components-rules-ai.md`: both legend-placement sentences corrected, a new **Where a chart's legend sits** section written, and the declaration rule given a floor so plain text in a published text style is no longer an invention while content filling a sanctioned slot still is. `color-rules-ai.md`: the CO2 ladder's letter colour written for the first time — `Content/Constant/Black` on `Blue100`–`Blue300`, `Content/Constant/White` on `Blue400`–`Blue700`, with the contrast table behind it. `compliance-scorecard.md`: its `library defect` worked example rewritten to match the ruling, and *"all seven rulesets"* corrected to six — `components-rules-ai.md` has no **Components first** section. `.claude/rules/compliance.md`: one exception added to *never rewritten*, so a run report's ruling column may be filled in once, by its third author, while everything else in the file stays fixed. **The contrast check is why one ruling changed shape.** Gabriel's proposed rule — `Default` on `Blue100`–`Blue300`, `Default/Inverted` on `Blue400`–`Blue700` — passes every step in light and fails every step in dark, because the CO2 fills do not flip with theme and those two tokens do. The split point was right; the token family was not. `Blue300` clears the 4.5:1 floor by 0.08. **Filed, not fixed:** the quality-bar redundancy note, the donut's internal 56 against the `Spacing/56` deny-list, and the Figma library row, which was rewritten because it had been asking for the wrong property |
| 14 Sep 2026 | **A `ruleset gap` no longer files itself as a task.** You raised it looking at run-002's eight items: some gaps close in one sentence, and a backlog round-trip then costs more than the fix. **You now decide per item — fix on the spot, or file.** **What did not change, and is why the old rule existed:** it still lands in `project/backlog.md` either way. As a task if filed, as a *Done* row if fixed on the spot. One destination, which is exactly what your 11 Sep decision was protecting — two destinations had meant two sessions filing the same finding in different files. Changed in `compliance-scorecard.md` and `compliance-flag-ledger.md`, the same two files the 11 Sep decision changed. The 11 Sep note on this page is refined rather than rewritten: its headline still holds, and only the self-filing half is superseded. **Method agreed in the same conversation, and it is not written into any file yet:** run-002's eight items get ruled in a **fresh chat with Figma access**, reading only the report, one item at a time, and **nothing is fixed until all eight are ruled**. A ruling session that starts editing does not finish the eight. The run chat was rejected for it — it watched the builder reason, so it would lean toward blaming the rules rather than the agent |
| 14 Sep 2026 | **The report is now written for you, and says so.** Four defects you found in run-002's report, all four in files I wrote. **One: the reader was never named.** The report was doing two jobs — evidence for its own answers, and a thing you read to decide. The scorecard now states plainly that the primary reader is Gabriel, and the order follows: **verdict, decisions you must rule on, answers, the builder's declarations, evidence last.** The evidence section is the one part still written for a machine, and it is where every locator now lives. **Two: a writing rule that was missing.** A node ID is a coordinate, never part of a sentence — name the thing in words, put the locator in a column. The bad example is quoted in the scorecard from your own message. The scorer wrote for a machine because nothing told it not to. **Three: failures had no human ruling.** Flags carried four verdicts and failures carried none, so a failure was recorded and never decided. Both now go in one table, with a fifth verdict added: **`library defect`** — the Figma library is wrong, not the agent and not the documentation, so no doc change fixes it. It exists because run-002 produced one: the ruleset says the donut legend sits below, the Figma component places it beside. **Four: the ledger contradiction.** You were right that a rule was missing, and wrong about where — the rule existed **twice**, naming two agents. Both ledger files say *written by the checking agent*; my skill's step 5 read as the orchestrator's job and its step 4 said *"give it nothing else"*. The scorer was correct to stop. The scoring agent now writes both, and step 5 is the orchestrator **verifying** the rows exist, never writing them from the report. **Run-002's report was rewritten into the new order — no answer and no finding changed, only structure and wording.** It went from three flags to eight items needing a ruling, because failures and the two uncovered findings now appear. **The ledger rows were not touched.** They had been appended after all, in `18bbdb9`, contrary to what the scoring agent reported — and they are append-only. A note in the flag ledger records that run-002's three rows predate the rule, and that runs from 003 carry every item |
| 14 Sep 2026 | **`generate-and-score` exists — the skill that runs one compliance run end to end.** Six steps: open the run, build, screenshots, score, ledgers, report back. **It orchestrates and does neither half itself**, which was your requirement and is stated four times in the file, because the tempting failure is answering one "obvious" question yourself and becoming the scorer. **Step one is the fix for the defect that lost two runs:** the folder is created and the brief saved to disk *before* anything is built, so a session's first act names the run it is in. **The anti-gaming rules, written out rather than assumed:** the scorer is spawned fresh, never a fork, never handed the orchestrating conversation, and its prompt carries only the run folder path and the Figma frame pointer — never what was built, never what the builder said. The builder is told not to read anything in `compliance/`, because the scorecard is the marking scheme. **One gap found while writing it and fixed in the same pass:** the builder needs the declaration format, which lives in the scorecard it may not open — so the skill now pastes the three-field template into the builder's prompt instead of sending it to look. **Subagent use is authorised in the file**, naming your 14 Sep decision, so a future session does not refuse it under the personal rule against spawning agents unasked. **Marked `status: draft` in its frontmatter and it says so in a *Known gaps* section: it has never been run.** Also recorded there — the scorer is Figma-only, nothing is stored but the report, and nothing checks a report against its ledger row. Both maps now say nine — `README.md` in the same change, and `CLAUDE.md:149` straight after, on your go. That second line was left out at first because it is prose in your own file and not on the change-without-asking list. Verified: frontmatter parses, the skill registered in this session, link check clean |
| 14 Sep 2026 | **The adapter is out of the compliance pillar, and the two-agent rule is in.** You called the translator too advanced for the end-of-September deadline, and you were right that it was buying the wrong thing. **What it was buying:** portability to web and iOS, plus a stored `facts-run-NNN.json` a future question could be re-run against. **What it was not buying, and what everyone assumed it was:** an independent verdict. **The rule that actually delivers that is now written down** — the scoring agent is never the generating agent, starts fresh, never sees the building conversation, and never asks the builder anything. Your argument, and it is the sharp one: the scorecard is a public file, so a builder knows every question before it is asked; letting it write the facts lets it shape the evidence without ever stating a falsehood, because it only has to choose what to mention. A declaration is a confession and is safe; facts are evidence and are not. **I had recommended letting the builder hand-write the facts and recording the weakness — that was wrong, and recording an incentive does not remove it.** **What changed:** `compliance-scorecard.md`'s adapter contract, pipeline, required and optional facts, JSON shape and worked example became one section, *what the scoring agent reads*; the three question tables' *Facts used* column became *What the scorer must see*, restated in plain language instead of dot-notation field names; `.claude/rules/compliance.md` lost the adapter paragraphs and gained the two-agent rule; `how-a-run-is-reported.md`'s five-step flow is four steps with a new section on why the scorer must be a different agent. A run folder is **three files** — brief, screenshots, report. **Kept on purpose:** the questions stay in design-system vocabulary, so a second platform needs a second scoring agent and not a second scorecard. **Accepted cost, stated in three files so nobody rediscovers it:** nothing is stored but the report, so a question changed later cannot be re-run against an old screen. `unavailable` changed meaning — it was *the adapter has a gap*, it is now *the scorer could not tell from the file*. The audit keeps all the adapter reasoning under a dated correction marking it history. Verified: link check clean, no live file mentions an adapter or a facts file, and the three-file count agrees across all four places that state it |
| 12 Sep 2026 | **No ruleset in this repo links to evidence any more, and `check-links.py` reports clean for the first time.** Five header credit lines and one mid-file line offered a live link labelled *Evidence* — the exact word an agent goes looking for when a rule does not decide its case — pointing at a file it is forbidden to read. Each filename stays in the sentence as plain text with the line saying outright that a generating agent must not read it, the shape set in the components ruleset on 11 Sep. **Run as a bounded sweep rather than the half-hour row, at your call:** read all six token rulesets end to end, fix everything a script or a grep can confirm, file everything else. That bound is what kept it honest — the rule was fix-what-is-checkable, so nothing was quietly rewritten on judgement. **Three more mechanical defects came out of the full read.** The spacing ruleset still said *Rules 6 and 7 are unverified* and *Rules 3–5* after the rules lost their numbers; both now name the rules, and the claim is stated from what the file itself proves — **Container padding** and **Page rhythm** carry no verification line, while **Component spacing stops at 32** and **Containment** each record being checked against every component that could break them. Typography's closing line had the same leftover and now names **The eleven used styles**. And the spacing and typography headers both labelled a link `colors-tokens/color-rules-ai.md`, a folder that has never existed — the target was right, the text a reader sees was not. **Four findings filed, not fixed**, the sharpest being that `spacing-rules-ai.md` lists `Spacing/56` as a page-rhythm token at `:60` and forbids it at `:143`, justifying the ban with *"not in any rhythm table"* while the rhythm table sits in the same file. An agent obeying the first fails the scorecard's spacing deny-list for following the ruleset. Also raised as a question: every ruleset header links to its `*-tokens.md` values page, which `CLAUDE.md` forbids a generating agent from reading. **`border-width-rules-ai.md` needed nothing** — its evidence was inline from the start. Verified: link checker says *every link resolves, and no ruleset points at evidence*, down from 6 dead ends; no `Rule N` string survives in any ruleset; every rule named in the two rewrites exists as a heading in its own file; no `colors-tokens` path outside `tokens/review-progress.md`, now a backlog row |
| 12 Sep 2026 | **A declaration now has a physical home, and every run file carries its run number.** The rules ended every invention with *declare it — state, in the output* and never said where that statement goes, so an agent that composed something correctly had nowhere to put the record, and the scorecard's pivot question read an `output.declarations` field that nothing produced. It could only ever answer `yes`. **The home is the run's own report**, under a `## Declarations` heading the generating agent writes **before** scoring and the checking agent never edits. Same place on every platform, because a run folder is the same on every platform. **Your call on the name**, twice: one file per run rather than a separate `declarations.md`, and `report-run-<NNN>.md` rather than `report.md` — which pulled in the run-number rename as a prerequisite, since a rule cannot name a file the repo does not use. `prompt.md` was renamed on disk to `prompt-run-001.md`; screenshots keep their descriptive names, your open question resolved the same way. **The report now has two authors**, stated in three files — the scorecard, the component ruleset and `.claude/rules/compliance.md` — along with the reason the order matters: a brief is saved before generating so it cannot be reworded to match the output, and a declaration is written before scoring so it cannot be retrofitted to pass. **Two defects in my own work, found by the check, not by me.** One: the worked example in the report template and the worked example in `facts.json` described the same declaration in different words, in a file that says the adapter copies one into the other — they are now identical strings. Two: step 4 of **When nothing fits** had bold nested inside bold, which renders as neither, in the one file a generating agent parses. **One contradiction I created and removed:** the ruleset linked to `compliance-scorecard.md`, which `README.md` says is read by a checking agent and not a generating one. The link is gone and the block format is restated inline instead, so the ruleset is self-sufficient. **What this did not close:** nothing mechanically stops a declaration being reworded after the verdict — you chose to wait for the adapter and compare the report against its copy in the facts file, over committing each run twice. It is a task under *Scoring a finished screen*, blocked on the adapter. Verified: link check 0 broken and the same 6 pre-existing dead ends, all in `tokens/`; no `prompt.md`, `facts.json` or `report.md` reference survives outside `project/decisions.md`, which is never rewritten by rule; the anchor the scorecard points at resolves |
| 12 Sep 2026 | **The *Parts* column is out of the components ruleset, and the audit says why.** It listed what a hand-built imitation of each higher-tier component would be assembled from — `Listing Card` is `Card` · `Image Slider` · `Tag` — and existed for one reader, the parts-counting check deleted earlier the same day. **The argument for removing it was not that it was unread but that it was a hazard without a reader:** it printed an assembly recipe for the exact thing **Highest tier first** forbids, in the one file a generating agent reads, and the ruleset carried a warning line purely to defuse its own table. **Checked cell by cell before deleting — nothing needed salvaging.** `Map template`'s pin sets are already stated in full under **Never select**; `Info State` as a shell is already in the Kind table; `Table`'s *see Platform limits* was generic routing that section already covers; `Floor selection`'s open question was itself framed around the deleted check. **What the deletion pulled in:** the Kind sub-table lost two of its four columns — *does Highest tier first still apply?* answered yes three times out of three, and *detectable by counting parts?* had no consumer — and was rewritten around what each kind asks of someone **placing** a component. `unresolved` became a fourth legend row, so all four values used in the table are now defined. **A second parts rule was found and kept:** *the operational test — count the parts* counts **capabilities** (*filter controls* **and** *dropdown panels* **and** *applied-state handling* = 3), not component names, so it survives untouched; a line was added saying so, because the two were easy to confuse and one of them is now deliberately unwritten. **Three pre-existing defects fixed in passing:** the heading said *Two kinds* over a three-row table, two Kind values were bolded to mark a distinction that no longer exists, and the operational test named no branch for `unresolved`. **The audit keeps the finding that outlives the check** — every part name the ruleset stated in prose failed an exact match against the registries, `Image slider` where the inventory says `Image Slider` — under a dated correction marking the rest as history. Open questions 11 and 13 are struck through as dissolved, with the reason. **One thing this did not close, contrary to what I told you:** the 11 Sep note about `Estimation card` being described in one place and called undescribed in two others is now down to **one**, not zero — the inventory row at `components-rules-ai.md:499` still reads *Undescribed — see audit*, and it means *no usage doc page*, which is still true. `Listing summary`'s version of the same conflict is fully closed. Verified: link check 0 broken and the same 6 dead ends; every Kind value has a legend row; no reference to a deleted check survives in the ruleset |
| 12 Sep 2026 | **The scoring sheet is organised by the seven steps of designing a screen, and the six check codes are gone.** You asked for a simple shape: a list of steps, one or more checks per step, a yes-or-no answer, the reason investigated afterwards. The file is that now. **Every question is phrased so `no` is the compliant answer**, so reading a report is scanning one column for `yes` — no percentages and no thresholds anywhere, because every threshold it ever carried was `100%` or `zero`, which is a yes/no question wearing a percent sign. **`C1`–`C6` are deleted rather than renamed**; keeping them legible had cost a standing instruction to write the name beside the number in five files. **What the rewrite found, which is the point:** three of the six token deny-lists — radius, shadow and border width — had been written and wired into no check, so a `1.5` border, a shadow of `32` or a `Corner radius/*` variable broke a written rule and was never reported; all six are read now. And the sheet still said *83 of 218 colour tokens have no consumer* when its own evidence file had said 73 since 10 Sep — cut rather than corrected, on your call that the scorecard measures and the audit explains. **Two things dropped at your request:** `no regression`, which asked whether a run beat the previous one rather than whether it used the design system, and was the only judgment here needing two runs; and the parts-counting check, which reached 4 of **Highest tier first**'s 10 rows and only ever added the *diagnosis* — a hand-built `Listing Card` is still caught as hand-built, and naming what it duplicates is now a person's job on the reason. Declaration hangs off any hand-built element instead. **The three steps with no check say so in the file** rather than being absent. **The check failed first, on two defects, and both are fixed.** One: *was any token used that is not in the GSL token set?* cited "the token pages" — not a rule and not a file. It now cites `tokens-index.md`, which routes to all twelve token pages, and the file says why that is a rule and not an exception: the index does for tokens what **The inventory** does for components, so the list itself is the rule. It was inherited — the old sheet carried the same thing as a *deduction* under `C3 · Token binding` with no rule behind it either, and promoting it to a question is what made the gap visible. Two: the report template spelled one step *place them per* where its own heading said *according to*. **Scope added beyond what you approved, and flagged at the time:** two clauses in `components-rules-ai.md` asserted a check that no longer exists, and both walkthroughs in `project/how-a-run-is-reported.md` described percentages and hard-fail ordering. Both were live falsehoods the moment the sheet changed. **Caught in the writing:** the first draft numbered every step — `2 · Define the components` — which a note on this page from 11 Sep bans outright. Removed everywhere. Verified: link check 0 broken and the same 6 dead ends, all in `tokens/`; all eight questions name a source that resolves; the section headings and the template's Step column are identical strings; no step carries a number |
| 12 Sep 2026 | **How Gabriel is written to is now one list in his personal `~/.claude/CLAUDE.md`, and the gap opened yesterday is closed.** Three sources had been saying the same things in different words — his personal file, this repo's *How to write*, and a ruleset he found on another account. The merged list is 23 bullets in four groups: the shape of an answer, sentences and words, a **never** list of six named phrasings, and a rule that every claim states whether it was proved, guessed, or never tested. **`Every investigation` is deleted** — its four steps became the opening of one shape used by every long answer, so there is no longer a format to choose between. Five rules were dropped rather than moved, Gabriel's call: *each paragraph earns the next* and *every sentence leaves him with more* (neither can be checked against a draft), *the cut pass*, *simple words* and *concrete over abstract* (covered by the never list, which can be). **Known cost, accepted:** the restating test was the only rule that caught padding by inspection, and the two that survive — *don't pad*, *short sentences* — are both judgements. If padding returns, that is the rule to restore. The repo file gained one clause: agent-facing files escape the brevity rules and nothing else, so the never list and the proved-or-guessing label hold there too. **The personal file is outside this repo and is not in any commit** — a copy of the version it replaced was kept in the session scratchpad only |
| 11 Sep 2026 | **The repo's writing rules are down to what is true about this repo's files — 50 lines to 20.** Ten style bullets and the chat half of the cut pass were generic: true in any repo he opens, so they belong in his personal `~/.claude/CLAUDE.md`, which **loads even in sessions running off the second config folder** — proved this session, which runs on `.claude-personal`, has no `CLAUDE.md` of its own, and was handed `~/.claude/CLAUDE.md` regardless. What stays is the two-readers table and the rule that cutting one of his sentences is a file change like any other. One line improved in the move: the agent row said *nothing below overrides it*, which stopped being true once the rules it must beat moved to another file, so it now says it overrides every rule about brevity. **Four pointers were repointed, and two of them had said outright that the repo section was the only place those rules live** — `.claude/rules/project.md` twice, both task skills once each. **Known gap, accepted by Gabriel:** the eight moved rules are written nowhere until the personal file is done, which is the task above |
| 11 Sep 2026 | **The writing rules lost their escape hatch.** *Logic first, brevity second* ended with *a guide, not a gate* — a clause attached to the one rule that settles collisions between the others, which is the worst place to put an opt-out: an agent reading it discounts the rules on either side of it. Cut on Gabriel's go. The rest of the line is unchanged and still says what to prefer when the two halves collide. The 11 Sep entry below that records the clause being written stays as it is — it says what was decided that day, and that is what it is for. Raised by an assessment of which rules an agent can actually apply; the rest of that assessment is a task above, at Gabriel's request |
| 11 Sep 2026 | **The components ruleset no longer hands an agent a link into its own audit.** `components-rules-ai.md:6` opened with a live link to `components-audit.md`, labelled *evidence and rejected options* — the exact words an agent goes looking for when a rule does not decide its case, on a file it is told never to read. The filename stays in the sentence, in plain text, so a person still knows where the reasoning lives, and the line now says outright that a generating agent must not read it. That last clause is nine words more than the task asked for; Gabriel was told and let it stand. Verified: `check-links.py` reports 0 broken and 6 dead ends, down from 7, all six now in `tokens/`. The five plain-text *see audit* mentions further down the file were left alone — they point at nothing clickable |
| 11 Sep 2026 | **The `internal/` folder is gone from both maps.** Gabriel deleted `internal/git-basics-tutorial.md` and the folder with it, following through on the 10 Sep note that recorded it as obsolete once this repo stopped using branches. `CLAUDE.md` and `README.md` both still listed the folder in their *Where to look* tables. **`README.md`'s row was nearly missed:** it said *"e.g. a git tutorial"* with no filename, so a grep for the deleted file did not find it — the same blind spot as the open row about teaching the link check to see bare filenames, now with a second instance |
| 11 Sep 2026 | **The backlog is sorted by the seven steps of designing a screen, and the order is now reproducible from the file alone.** A task sits under the step it stops, so its position states its own reason. Two sections became columns — *Blocked by* and *Due by September* — because the previous attempt failed its check when a section drifted while a row's position said nothing. Five files carried the old rule and all five changed: the backlog, both task skills, `.claude/rules/project.md` and `status.md`. **It took two failed checks to land.** The first failed because only four of 25 rows stated their rank; the second because a group called *Every step* sat among the step headings and nothing said whether it was one — two defensible readings, two different next tasks. The fix deleted that group rather than documenting it: its two rows were bundled by task, not by defect, and each belongs under the ruleset it sits in. The picking rule now returns one answer with no judgement. **What it found, which is the point:** *define the content* and *check the content* have no rows at all, and the scorecard cannot see copy or tone either — the least documented part of the system had been invisible to the list meant to surface it |
| 11 Sep 2026 | **How I write to Gabriel is now one section in `CLAUDE.md`, and the four other statements of it are gone.** Four files gave the shape rules — plain language, short sentences, tables — in four wordings, and none of them caught a paragraph that is short, bulleted and still says nothing new. Gabriel supplied the missing half: every sentence must leave him with more than the last one, the relation between two sentences must be named rather than left to rhythm, and a cut pass flags every superfluous passage before anything is sent or saved. *How to write* carries both halves plus the split that keeps them safe — his pages get them, while `-rules-ai` files, registries and ledgers stay optimised for an agent, where restating a condition removes doubt instead of padding. Precedence is written down because the two halves collide: logic first, brevity second, a guide and not a gate. Flagging is silent in chat and out loud in files, because naming a cut in chat doubles the length of the thing the rule exists to shorten. `.claude/rules/project.md` and both task skills stopped restating the rules and point at the section instead; the three-rules table in `CLAUDE.md` is now two, since *no codes* is a writing rule and moved with the others. Verified: nothing outside `CLAUDE.md` states a writing rule, all four pointers resolve, link check 0 broken and the 7 dead ends unchanged |
| 11 Sep 2026 | **Four findings about what a newcomer cannot learn from this repo are now on the list.** Gabriel asked whether a person opening this project could understand it without technical jargon. The loop half holds up — `status.md`, the backlog, the decision log and the filename grammar answer where we are, what is left, and why the repo is shaped this way. The subject itself is never introduced, so *highest tier first* reads as a phrase rather than a rule: nothing anywhere says what a token is, what a component is, or what the four Figma tiers are. Two pillars have a page for a person — `tokens/` and `compliance/` — and each got one because a task happened to force it, which is why the other two have none: `components/` sends a reader to a routing page written for an agent, and `README.md`'s map sends one into `.claude/rules/figma-registries.md`, addressed to an agent. Filed as four rows in the block that does not reach the canvas, since none of them changes a Figma page before the deadline, plus a rider making "every pillar has a page for a person" an actual rule instead of an accident. Verified: four rows, four columns each, link check 0 broken and the 7 dead ends unchanged |
| 11 Sep 2026 | **There is now one list of what may change without asking, and it is in `CLAUDE.md`.** The same sentence was written out in three files and named only two exceptions, so a third one — added to the check skill so a new, renamed or deleted file gets listed where the repo lists files — would have been contradicted by the page a cold session reads first. `CLAUDE.md` now carries the whole list in one table with the test underneath it: does this change what a page *means*? If it does, it is Gabriel's. `.claude/rules/project.md`, the two task skills and `status.md` stopped restating it and point at it instead — three of them got shorter. The check skill gained the list-follows-the-file step between updating `status.md` and committing. First application, done in the same sitting: `project/how-a-run-is-reported.md` is now listed in `README.md` and in the project rules file, which closes the row filed for it an hour earlier. Verified: nothing outside `CLAUDE.md` still states the list, link check 0 broken, 7 dead ends unchanged |
| 11 Sep 2026 | **How a run gets reported is now written down for a person.** Gabriel asked how he would explain the reporting pipeline to someone else; the repo could only answer *why* it was built that way, in `project/decisions.md`, never *how it works*. `project/how-a-run-is-reported.md` now walks it end to end for a non-technical reader: what a run is, the five steps from brief to saved verdict, why the translator step exists at all and what it buys (one set of rules judging Figma today and web or iOS later), the four files a run leaves behind and who writes each, the folder after several runs, and why the ruler and the record are separate files. Two walkthroughs: Gabriel three weeks later reading one ledger to answer *is it getting better* — read down a single wireframe label, hard fails outrank every percentage — and the checking agent on its next run. Ends with what does not exist yet, the translator included. Diagrams are ASCII in code fences so they render in a plain editor. It **links** to the scorecard for the report template and the required facts and never repeats them. Verified: link check 0 broken and the 7 dead ends unchanged, no bare check code anywhere in the page, and no rule stated here that is not stated in the scorecard or the decision log. Not published anywhere — Gabriel's side only |
| 11 Sep 2026 | **Every generation run now leaves a report behind, and it is a file.** Gabriel asked for a report after each run — platform-neutral, readable by anyone, shareable on its own, kept so runs can be compared. The scoring sheet already defined what a report contains, but described it as something you *look at* and never as something *saved*, which is why `compliance/runs/run-001/` has a brief and screenshots and no verdict at all. A report is now a markdown file at `compliance/runs/run-NNN/report.md`, with a filled-in template in the scorecard so every platform writes the same shape. A run folder requires four things — the brief saved *before* generating, screenshots, `facts.json` and the report. The run log moved out of the scorecard into a new `compliance-run-ledger.md`: the scorecard is the ruler and gets rewritten when rules change, a run log is a measurement and must never be. Reasoning recorded in `project/decisions.md`, including what it costs. Verified: link check 0 broken and the 7 dead ends unchanged; the fence fix silences exactly the 5 links inside the new template and no pre-existing one; and the checker still catches real breaks either side of a fence, including a nested one. The check also found a genuine hole this task did not create and did not fix — nobody is told who assigns a run's wireframe label, which is now its own task |
| 11 Sep 2026 | **A block of controls that computes a live result now has a routing entry.** In the test the agent hand-built a mortgage simulator and then said it couldn't tell whether it should have. **Which component** now carries *Computing a figure from user input*, and it answers: no Pattern and no Experience is this block — checked against all 21 Patterns and all 11 Experiences in the registries, not assumed — so build the controls from `Slider`, `Counter Field` or `Text Field`, show the result with `KPI`, and follow **When nothing fits** to the end, declaration included. `Estimation card` is named as the near miss it is: it presents a finished estimate and carries no controls, so a simulator whose result is a price estimate is two halves — the Experience for the result, composed and declared for the controls. Verified: every component named exists in **The inventory**, the `Estimation card` description matches its registry entry, and the link check stayed at zero broken |
| 10 Sep 2026 | **A legend is switched on, not placed — and no internal part is ever placed.** The rules named `Legend` in the routing table like any other component, but all three charts carry it as a boolean and the underlying piece is private in Figma, so an agent went looking for something that cannot exist. It now says place the chart and switch its legend on. Two rules that were missing arrived with it: a Figma name starting with a dot is an internal part, reached through its parent and never placed or rebuilt — the first time that convention appears anywhere an agent reads it — and `Donut chart` has no side-placement option today, so nobody hunts for one. The first check **failed**: the Figma section still said "if it's named, you may place it", and it outranks the routing table, so permission beat the prohibition. Fixed by adding `Legend` to the short list of names that aren't components and narrowing that sentence. Figma vocabulary stayed inside the Figma section; the neutral rules stayed neutral |
| 10 Sep 2026 | **The energy colours are usable, and the mapping we held was wrong for five of seven classes.** `Scales/Energy` and `Scales/CO2` moved out of **Never use** into the colour ruleset with the French DPE classes mapped to exact tokens — verified against `EnergyScale.tsx`, the only production consumer. The old French column walked the palette from the start; France samples across it, so class G came out amber where it must be red. Corrected in the ruleset, the token page, the audit (83 orphans → 73, in seven places) and `tokens/README.md`. The ruleset now says where each rule came from and that Figma has never been checked |
| 10 Sep 2026 | **"Can I use this component here?" now has one answer for Figma, and an honest "ask" everywhere else.** Platform limits was three sources contradicting each other, seventeen spellings of readiness, and a rule that stopped mid-answer and pointed at a file agents may not read. Figma is now answered completely from the registries. The web list built from the component docs was **thrown away before it shipped** — checked against the web source code, it was wrong for five of thirteen, so web, iOS and Android now say plainly that nothing is established and the agent must ask. The dead end is gone, every component name matches the inventory, and the docs' readiness row is marked do-not-read on every platform |
| 10 Sep 2026 | **Two files called `tokens.md` and two called `components.md` — no longer.** The rules files became `token-docs.md` and `component-docs.md`, each now opening with a line saying it is an instruction for Claude, not design-system content. Nothing linked to them by name, so nothing broke. `figma-registries.md` already set the precedent that rules files are named for their subject |
| 10 Sep 2026 | **`-index` added to the filename grammar**, and the two routing pages renamed to `tokens-index.md` and `components-index.md`. They were the only two files in the repo whose names didn't follow the rule `CLAUDE.md` states. 13 links moved and the link check proves none broke. `tokens-index.md` also gained the title it never had |
| 10 Sep 2026 | **`scripts/check-links.py` written** — the repo's first repo-wide check. It answers two questions: is the file there, and is the agent allowed to read it. First run: 1,378 links, **zero broken**, nine dead ends now listed exactly instead of estimated at three |
| 9 Sep 2026 | **The retired files moved to `project/archive/`** — the old plan, the handoff note and all four briefs, with a README saying why each was retired. `decisions.md` and `README.md` deliberately stayed: one holds why the project is shaped this way, the other is the map |
| 9 Sep 2026 | **One task, one chat** — each task starts in a fresh conversation. Written into `CLAUDE.md`, `status.md` and the check skill. Not about context limits; a session that built something is the worst judge of whether it reads clearly to someone who wasn't there |
| 9 Sep 2026 | **Corner radius, shadow and border thickness now have rulesets** an agent is allowed to read — seven of twelve token kinds covered, up from three. Verified: every value matches its token page, 57 links resolve, and all three failures from the test run are now answered, including "a card is not elevated, give it a border" |
| 9 Sep 2026 | **Git simplified: one task = one commit, straight to `main`.** No branches, no pull requests. A passing check now commits and pushes automatically; undoing, deleting and rewriting always ask first. Overrides Gabriel's personal one-task-one-branch rule, for this repo only. `audit/run-001` merged into `main` and deleted; `main` is now the only branch |
| 9 Sep 2026 | The repo now describes the new way of working — the rules file for `project/` carries the loop instead of the old ritual, `README.md` opens on `status.md`, and the plan, briefs and handoff note are marked superseded. Verified: 27 links resolve, no stale skill count, no surviving instruction to write briefs |
| 9 Sep 2026 | `status.md` created — one page replacing eight |
| 9 Sep 2026 | `design-language/` removed, and the two files that described it corrected |
| 8 Sep 2026 | The rules in all four rulesets lost their numbers and gained names |
| 8 Sep 2026 | The component parts list became machine-readable |
| 8 Sep 2026 | The compliance scoring sheet written |
| 8 Sep 2026 | Component selection written and tested cold, three rounds |

### Moved from `status.md`, 25 Sep 2026

_The long write-ups that sat on `status.md` until 25 September, kept word for word. Most repeat a row in the table above, with more detail. Gabriel asked for `status.md` to fit one screen._

#### What we're building

**Priority changed 24 September: the iOS demo replaces the Figma milestone.**
A design skill turns a brief into a neutral spec; `/prototype` builds it on iOS.
The Figma goal below stays until you rewrite it.


A written description of the GSL Design System complete enough that an AI agent
can read it and design a screen in Figma using the right components and the
right colours, with nobody correcting it.

**Deadline: end of September 2026.** What "done" means that day is defined in
one place — [project/the-project.md](the-project.md). Short version: two
unseen briefs, both built and scored with no rule broken.


**Next: re-run `/design` on the same idea** — in `~/gsl-ios`, after
`/prototype-cleanup`. The skill now designs from the need, not the wording.

**Done, 24 September: the design skill can pick a colour token.** The five colour
family pages are in its references now, without their value tables.

**Done, 24 September: the design skill exists.** `skills/design/`, runs from any repo.

- **One flow:** questions, you validate the goals, the screen is designed, `/prototype` builds it.
- **Feedback in the same chat.** Small changes ask nothing.
- **Specs saved in `~/gsl-specs/`**, so they outlive the prototype.
- **Not tested:** it has never run.

**Done, 24 September: every token has its iOS name.** `tokens/tokens-ios-map.md`.

- **248 of 283 proved, 35 not found, none guessed.** Generated by a script from the iOS code.
- **All 22 tokens `spec-001` uses are proved.**
- **Not tested:** no iOS engineer has confirmed it.

**Done, 24 September: the spec format exists.** `specs/spec-rules-ai.md`, with
`brief-001` written in it as `specs/spec-001.md`.

- **Eight sections**, in design-system names only. Goals, acceptance criteria and assumptions included.
- **Each element has a stable ID.** Feedback edits one row, then the prototype rebuilds.
- **The first check failed**, on three contradictions. All three were fixed.
- **Not tested:** no fresh agent has written or built from a spec yet.

**Done, 24 September: every component has its iOS name.**
`components/components-ios-map.md`, 73 of 73.

- **44 proved, 5 guessing, 18 not found, 6 not for iOS.** Counted by script.
- **Read from the remote.** Your local `gsl-ios` was 21 design-system commits behind.
- **No iOS engineer has confirmed it.** The five guesses need one.

**Paused: two eval rows fail because the duty sits beside the rule.**
`Feedback thumb buttons` and `Score tag` must be reported as selected, and the
cold agent missed it. Top of *Define the components to use* in the backlog.

**Done, 24 September: every readiness table's Figma cell is checked.** A
seventh check compares it with the four registries. **75 of 75 agree.**

- **Six cells were wrong**, the same six the 8 September audit found and left.
- **Proved it fails**, on a copy with three planted faults.
- **The audit said all 98 registry entries were verified live.** Two say they
  were not. Corrected.
- **Web, iOS and Android are still unread.** The ruleset line telling agents to
  skip the table stays until they are.

**Done, 23 September: the repo has a sixth check, and it closes the gap you
named as the end goal.** `scripts/check-rules-reach.py` asks whether every
selectable component is named by at least one rule. **73 selectable, all 73
reachable.** Your hand count of 21 September was right, and it is now checked
rather than claimed.

**Nothing could see this before.** `coverage.py` reads the registry against
docs; `check-rules-docs.py` reads docs against rules. Nothing read the inventory
against the rules.

**Proved it fails, three ways** — a planted component, a real one with its
mentions stripped, and the same hole planted in the real file and run through
`check-all.py`. It found a bug in itself first: the first run called 71 of 73
unreachable, `Button` included.

**Seven lines across five files said five checks.** All corrected.
`project/decisions.md` still says five, deliberately — it records what was
believed on a date.

**Done, 23 September: the questions are sorted by whether an agent is blocked.**
Your filter. **Seven of 45** change what an agent builds; they sit above the
line. Two were struck as already answered by the files. 45 before, 45 after.

**The Tasks section was left alone on purpose** — `task-next` picks from its
design-step order, so re-sorting it would break the rule that chooses the work.

**Done, 23 September: the component eval went from 31 intents to 64.** 33 new
rows, one per rule written after the last run that nothing tested — the six
never-select rulings, the navigation family, the built-outside-the-system flag,
the nine components documented on 21 and 22 September, and the chart, legend,
icon and declaration rules.

**One of the existing 31 was wrong, and it would have scored a pass.** Row 17
accepted `Pop-up` for a blocking confirmation **on web**. Your 21 September
ruling made `Pop-up` apps only. Corrected, with a new row 54 for its real case.

**Every expected answer traces to a line in the ruleset** — 34 names checked,
none missing. The run instruction changed too: it said *ask for one component
name*, which tells the agent one exists. Twelve rows have no component as their
answer.

**Nothing here has been run.** Drafting proves the file is well-formed. Whether
a cold agent reaches these 64 answers is what nobody knows, and it is now
unblocked.

**Two rows came off their blocker:** re-running the component eval, and giving
colour its own eval. Both were waiting on this sitting.

**Done, 23 September: a file that examines another is named after it.** Your
rule. Three renames — `components-rules-ai-eval.md`,
`components-rules-ai-audit.md`, `compliance-scorecard-audit.md`. Each pair now
sits together in a directory listing, so nobody has to know what a suffix
targets.

**21 references fixed across 15 files, and the link check passes** — 2,364 links
and 369 filenames. All three are `git mv`, so history follows them.

**`compliance-scorecard.md` was deliberately left alone**, and the reason is now
written where someone will look: no file in this repo is its target, and it has
no answer key. An eval's expected answers are written down, so two runs compare.
A scorecard judges a screen that never existed before.

**One finding outranks the rename. Eight rulesets exist and seven have no eval
at all** — icons, colour, spacing, typography, radius, shadow, border-width.
Your new name is what made that visible. The backlog row recommends starting
with colour rather than doing all seven.

**In the decision log**, with what was rejected — renaming
`compliance-scorecard.md` to an `-eval` — and four costs. The largest: four
dated records now name files that no longer exist, and all four are exempt from
the checker, so nothing will ever flag them.

**Done, 22 September: the five checks are one command, and `/task-check` runs it.**
`scripts/check-all.py` — five checks, one verdict, **0.57 seconds**, and it
changes nothing unless you pass `--write`.

**It is not a check that only ever passes.** A junk line added to the coverage
ledger made it report `STALE`; restoring the file made it report `current`.

**It exits 1 today**, on the five `check-rules-docs.py` disagreements that
predate it. The skill names those five, so a future session can tell a known
defect from one it just made.

**The warning matters more than the runner.** A clean run proves the docs are
**well-formed**, and proves nothing about whether a sentence is **true** —
every check reads this repo against itself. `map-template.md` passes all five
and was written off a component library with nobody who knows the product
reading it. That sentence is now in three places, with the four questions
nobody checks beside it.

**No hook.** Your call, and the reason is worth keeping: a hook fires with
nobody asking, and you already type `/task-check` at the end of every task.

**`components/missing-images.md` is deleted.** The problem it described is gone
— **1,611 image references scanned, zero remote, zero non-pictures.** Its one
lasting lesson was already recorded, in more detail, in the merge skill.

**Five findings went into the backlog**, including your direction: compare the
documentation against Figma, the app monorepo and the web, rather than only
against itself.

**Done, 17 September: every component doc matches the template.** 59 of 59, from
8 that morning. 115 off-template headings are now 0 — and 64 of those 115 were
never drift at all, just the report not knowing that
`### [Variant Category Name]` is a slot that takes any name.

**Done: `Content & UX Writing` has a fixed core and a free slot.** Three rules —
`Capitalization`, `Label Formula`, `Length Limits` — are now asked of every
component and scored individually, so an audit can ask one question of all 57.
That is why the coverage figure fell from 70% to 62%: `Label Formula` is
answered by 12 docs of 57. Nothing got worse, it became visible.

**Done, 18 September: four more merged, and the merge script rebuilt.**
`date-picker` 21 images → 70, `media-upload` 7 → 46, `checkbox` 6 → 42,
`button` 18 → 52. No prose lost, none duplicated, no `zeroheight.com` link
gained, every picture on every page placed.

**The first run of those four looked clean and was not.** `checkbox` silently
lost its three error-state tables — 12 pictures — and the draft script still
reported 42 of 42 placed. Eleven faults came out of chasing it, all fixed and
all written into the skill.

**Two things nobody was checking, now checked on every run:**

- **What the page gave against what the doc holds.** This is the count that
  found the checkbox damage. Nothing had ever compared the two.
- **Whether a file is actually a picture.** **36 files across 12 components are
  Figma JSON saved under a `.png` name** and can never render.
  `scripts/check-links.py` cannot see them — it asks whether a file exists,
  never what it is. Three were `date-picker`'s and are gone; 33 remain, and
  they are in the backlog.

### Done: every merge, and the broken pictures with them

**All 25 components that had an image gap now carry every picture their
Zeroheight page holds.** Done 18 September in four batches, each read by you
before anything landed.

| | |
| --- | --- |
| Components merged | **25** |
| Pictures added | **371** |
| Sentences lost | **0**, on every one |
| Sentences duplicated | **0** |
| `zeroheight.com` links gained | **0** |

Biggest gains: `coach-mark` 3 pictures to 21, `floating-button-group` 3 to 14,
`info-state` 4 to 15, `counter-field` 4 to 19, `button-group` 7 to 38,
`date-picker` 21 to 70.

**The merge script took 28 fixes to get there, and the extractor one.** Nine of
those were found by you reading the drafts, not by any check. Two were found by
re-running components already merged — including one where a fix of mine
destroyed 12 pictures silently. Every fix is commented at the line that caused
it, naming the component and the damage.

**Three things also went, all from the old Confluence migration:**

- **36 files that were not pictures.** Figma node JSON saved under a `.png`
  name, each written into a doc as an image, each rendering broken. Eleven were
  replaced by the pages' own sections; 25 were removed outright, file and
  reference together. **None are left.**
- **60 `<!-- Source: …confluence… -->` lines.** Nothing read them.
- **Every H1.** Each doc opened by repeating its own filename, so you read the
  name twice. Gone from all 62 docs and from the template.

**What is left of this job:** three components — `select-card-group`,
`dropdown` and `modal-bottom-sheet` — were merged on 17 September, before any
of the 29 fixes. Re-running `modal-bottom-sheet` placed **2 pictures the old
run had left behind**. All three are in the backlog.

### Done: every doc now says which variant to pick

**57 of 57.** `floor-selection` and `listing-summary` were the last two blank; *Variant flow* is at 100%. Both written from each doc's own words, no Figma property named, so they stay platform-neutral.

**It came out of assessing the decision-tree article you sent.** The answer was no new trees — this repo already has both layers the article describes, as intent tables and axis lists. What it does **not** have is anything joining a doc's axis name to Figma's property name. That is now a question in the backlog.

**Those same two docs were also the only two with no `When to use` and no `When NOT to use`. Both are now written.** Three sections stand at 57 of 57: *When to use*, *When NOT to use*, *Variant flow*.

**`listing-summary`'s `Related Components` table is written too.** **Four sections now stand at 57 of 57** — *When to use*, *When NOT to use*, *Variant flow*, *Related*. Every component doc answers when to use it, when not, which variant, and what to use instead.

### Done: all 57 variant flows audited

**Nine faults found, seven fixed.** Two were real contradictions, four were an axis-wide rule trapped inside one branch, one was on the wrong branch, one was a gap the source had too.

**The surprise is which flows are weak.** Not the custom components — `energy-tag`, `tables`, `modal-bottom-sheet` and `listing-card` have the best ones. The weak flows are the simple components, and `phone-number-field` is the worst: its whole flow lists runtime states, so the section that should say which variant to pick says nothing. Three task rows in the backlog.

**`radio-button-group`'s contradiction is settled, your way.** The limit is readability, not a number: up to 5 in one column, 6 to 10 in two columns when labels fit 2 lines on mobile, `Dropdown` above 10. Changed in all eight places that stated the old cap.

### Done: five thin flows filled, from their own pages

**The material was already in the docs.** The flows were thin because nobody had read past `Usage`, not because the source was empty. `filter-bar`, `media-upload`, `legend` and `chip-group` gained real axes; `phone-number-field` was rewritten outright, since all four of its axes were runtime states.

**Four invented conditions were cut the same hour.** They are now visible *not documented* lines, in one backlog row — an agent that reads *not documented* asks; one that reads a plausible invention follows it.

**`Phone number field`'s breakpoint table was inverted, and you confirmed it.** Fixed in both the table and the flow: bottom sheet on narrow, dropdown on wide, matching the rest of the system.

### Done: four more components can now be reached

**`State message`, `Score tag`, `Date field` and `Floating button group` have a rule row**, each built from words already in the repo. Components with no rule: 16 → 12.

**Then three more, after you pushed back.** `Image slider`, `Loading state` and `Burger menu` are routed; `Burger menu (profil)` is forbidden on your word. **You were right about the first two** — the web code repo has real standalone components for both, which the repo's own docs never mentioned. ### Done: the check that stops the two sides drifting

**`scripts/check-rules-docs.py` compares each doc's *When NOT to use* against its ruleset row.** Sets of component names, never prose — the doc is meant to rephrase.

**It found 12 disagreements the day it was written, and five were made that same afternoon** by the new rules. All five closed. Seven predate it and are a backlog row.

**Also done:** asking the user has its own ruleset section, and `Feedback bar` versus `Feedback thumb buttons` is now your rule — a notation against a like — replacing my guess. Two open questions in `components-audit.md` are struck through and answered.

**`Button Bar` has a doc — the first written from web code, not Zeroheight.** You read it and approved the shape; the one Figma mention is cut, because a component doc is platform-neutral. **Documented: 57 → 58.**

### Done, 21 September: the five with web code are written

**`Badge`, `Image slider`, `Loading state`, `Text button` and `Tooltip` have docs**, each built from its rule row plus its web source and nothing else. **Documented: 58 → 63.** Selectable components with no doc: 19 → 12.

**They beat the pilot.** `Button Bar` filled 8 of 17 sections; these fill 9 to 12. Where a source gave a capability and no rule for choosing it, the doc says so in the open rather than inventing one.

**Your reading changed five things no check would have caught.** `Badge` may stand alone, not only pinned to a host. `Text button` exists for an alignment constraint, not mainly for "Read more". `Danger` is for erase and delete; `inverted` is a dark surface and nothing more. An external link takes the external icon, and the built component does not add it. A form's submit is a **primary** `Button` — you corrected me on that one.

**Two rules you settled outright.** Attributions leave component docs, because people read them — `CLAUDE.md` says so now. And `Image Ratio` joins **Never select**: a Figma-internal helper for designers, never a component in a product screen.

**Twelve selectable components still have no doc**, and every one needs Figma. **Not ten — the backlog's list had been missing `Burger menu` and `Map template` since it was written.** `coverage.py` counts from the registry and had the right number all along.

### Done, 22 September: the link check reads filenames, not just links

**Eleven stale names, all fixed.** Two rules files had been telling every
session the indexes were `components.md` and `tokens.md` — months after both
were renamed. `tokens/review-progress.md` named a `colors-tokens/` folder that
has never existed, in nine places.

**`check-links.py` now asks a third question:** does a filename in backticks
still name a file that exists? **346 names checked alongside 2,366 links, and
clean.**

**A bare name is a name, not a path.** Every real file is indexed under every
suffix of its path, so `surface.md` in a rules file finds the one in
`tokens/color/`.

**Raw, the scan found 108 unresolved names and was unusable.** Five filters take
it to the 11 real defects — placeholders, grammar fragments like `-audit.md`,
`~/` paths, the two files that record what was true on a date, and `.json`
names, which are mostly the web code repo's and not ours to find.

**Proved twice.** Ten synthetic lines through the matcher, then a throwaway file
carrying `components.md`, which made the real run fail and pass again once
removed.

**One thing it still cannot see, and you should know it:** a filename with no
backticks around it. There is no way to tell one from an ordinary word.

**This page is exempt, and the check itself made the case.** It failed on the
sentence above reporting the fix, because that sentence has to name the old
file. Your done log is a record of what was true on a date, like the backlog and
the decision log — all three are skipped for the same reason. The live pointers
here are links, which are still checked.

### Done, 22 September: no usage doc names a tool any more

**58 lines across 22 docs, gone.** `scripts/check-tool-neutral.py` reports clean. **Every one of the 58 named Figma** — Zeroheight, Confluence and Storybook were already right, appearing only inside links.

**About 40 were mine**, written in the last four days.

**Ten `-figma.md` sub-pages exist, and none is empty.** `action-menu`, `alert`, `autocomplete`, `carousel`, `cell-content`, `dropdown`, `info-state`, `media-upload`, `navigation-bar`, `wizard`. Each usage doc links to its own. The other 66 components have no such page, which is your rule working.

**What moved rather than died:** the six tips, the "feel free to detach the component" sentence, `cell-content`'s `Placeholder left alignement` misspelling, and the navigation bar's single-variant explanation.

**What was simply cut:** sourcing lines, and the words *in Figma* hanging off measurements. A measurement is a property of the component — *"Height: 20 in Figma"* is now *"Height: 20"*.

**I got it wrong twice and the checker caught both.** It first treated any line holding a URL as a link, which let three tips through. Then the sentence I added pointing at each sub-page said *"Working in Figma?"* — ten fresh findings from my own fix. Neither would have been caught by reading.

**The rule is now in four places** so it cannot drift back: the template, the filename grammar in `README.md` and `CLAUDE.md`, the folder rules, and the checker.

### Done, 22 September: the last four, and the gap is down to one

**`Badge store`, `Feedback thumb buttons`, `Estimation card` and `Map template`
have docs.** All four written from a live library read. **Documented: 71 → 75.
Selectable gaps: 5 → 1** — and the one left is `Pop-up`, which you parked.

They fill 13 to 14 of 16 sections, against 9 to 12 for the five written
yesterday. Two tool specification pages came with them, because both had
something real to say. Ten → twelve.

**One finding outranks the four docs. `Map template` is built entirely on
components the library itself marks outdated** — 81 superseded price pins, three
in each of its 27 variants, plus 27 superseded floating actions. **The current
pin sets are used zero times.** Proved twice, by two differently-written reads.

**It makes one ruleset line false today:** the pin sets are said to be reached
by placing `Map template`, and placing it reaches the old set. You said the map
is one product's, already implemented, and not a priority — so it is a question
in the backlog, not a task.

**`Estimation card` embeds `Feedback thumb buttons`.** First place in the repo
where that unadopted component is shown genuinely in use.

**Three names are misspelled in the libraries and are preserved word for word**,
because a sync matches the literal string: `Plateform`, `Very zommed / 3D`, and a
confidence axis still called `Property 1`.

**Seventeen variant combinations are missing across the two Experiences.** All
recorded as untested rather than guessed at.

**Reading live Figma works, with one trap worth knowing.** It reaches **one file
at a time, and it is the file the plugin was launched in — not the tab in
front of you.** The documented way to target another file **fails silently**,
returning the wrong file with no error. Every read this session confirmed the
file name first.

### Done, 22 September: the web navigation, written as one system

**Four docs.** `Navigation bar` rewritten, `Mega menus`, `Burger menu` and `Menus` written from scratch. **Documented: 68 → 71. Selectable gaps: 7 → 5.**

**The ruleset now says the three belong together**, where an agent chooses. The bar holds the entries, the mega menu is what one opens on desktop, the burger menu is what replaces it on mobile. **The width decides, and it is never a free choice** — mega menus from 1024 up, burger menu at 320 and 768.

**One readiness row was wrong.** `navigation-bar.md` claimed **Ready** on web while its own next sentence said the component had not been developed. No navigation bar exists in the web code, in any package. Corrected.

**Your single-variant answer is written in.** Deliberate, a Figma constraint, better for you — breakpoints run on Figma variables rather than variants, so one component works at every width. **The dev architecture does not mirror it**, and the doc says so, to stop a reader treating the Figma structure as a build description.

**`Mega menus`' ownership note was wrong and is corrected** to your account: the Figma was built by the design system team, the component is not stored here yet, and the transfer is ongoing.

**`Menus` got a doc despite being never-select**, opening with a banner saying so. It is the profile and language panel — six branded profile variants, one unbranded language variant. That is the first concrete account of what it is, which your investigation needs.

**Two things I left alone and filed as questions:** the stray `.❌ Navigation bar (outdated)` instance and the detached bar on the Mega menus page, and a text node reading **"❖ Bad component"** sitting inside the live navigation bar component.

### Done, 21 September: the first two Patterns docs

**`Date field` and `Feedback bar` have docs.** Written from the Patterns library. **Documented: 66 → 68. Selectable gaps: 9 → 7.**

**`Date field` contradicts the rule that sends an agent to it.** The rule says to choose it when *no calendar is offered*. The Figma component has a calendar button at the end of the field. Nobody has established whether it opens a calendar, is decorative, or is a leftover from `Date picker`, whose page it shares. **The doc says to follow the rule, not the button** — your call when you want it settled.

**There is no standalone date field on web.** `DateField` exists only inside `DatePicker` and is not exported. A web build places a picker, never a field.

**`Feedback bar`'s scale is one to five**, built from a button group set to seven slots with two hidden — a Figma leftover, not a rule. **The scale has no anchor words**, so nothing says what 1 and 5 mean.

**I held back `Burger menu` and `Mega menus`.** Both belong to the navigation topic, and writing them now would mean writing them twice.

**One correction.** The *"will be moved to the product library"* banner I cited as evidence about `Feedback thumb buttons` also sits on the `Feedback bar` page. It is not about the thumb buttons. Your ruling stands on your own knowledge; the backlog note now says so.

### Done, 21 September: six rulings written in, and one of my own facts was wrong

**`Score tag` is built on web.** The doc I wrote this morning said it was not. It lives in the **patterns** package, not the core one, which is why every search missed it. Its four tier labels are translated into English, French, German and Dutch — and **Figma opens on `Diamond` while web defaults to `bronze`**.

**The same blind spot ran through the colour audit.** `content.md` said the score tokens served a component with no implementation. Corrected, with the lesson written in: **a zero in that file means "not used in `libraries/ui`", never "not used"**. Five other components sit in that unread package.

**A component can now be flagged as built outside the design system.** Your wording: usable by any team, and **selecting one is the signal it should be adopted properly**. `Score tag`, `Feedback thumb buttons` and `Badge store` carry it.

**Also in:** `Alert` is a fixed-shape modal expecting a decision back, replicable from `Modal bottom sheet` until it is built. `Pop-up` is apps only and stays undocumented until its overlap is settled. `Navigation Bar (App)` has its own rule row, in your words.

**`Button Card` is forbidden, and everything pointing at it moved.** Available on no platform, so not selectable — your call. Three rule rows and three docs now route to `Card`, with the whole container as the action. `Card`'s own row had been deferring that case to `Button card` all along, so it simply comes back. **Removal from Figma is separate and still open** — you asked for an investigation first, and nobody has said what it should check.

**The navigation family is now one topic, not six loose ends.** `Navigation bar` is a **Pattern**; `Navigation Bar (App)` is a **Component**. Two tiers, near-identical names, and nothing says why. The row covers both, plus `Burger menu`, `Mega menus`, `Menus` and `Burger menu (profil)` — three of them Patterns-tier gaps, so it belongs in the same sitting as the other Patterns docs.

**It also swallows a live defect.** `check-rules-docs.py` folds the two navigation names into one, so the line it prints about `navigation-bar.md` is false today. Fixing it alone would make a wrong answer quiet, so it waits for the naming decision.

### Done, 21 September: three docs written from Figma itself

**`State message`, `Navigation bar (app)` and `Score tag` have docs.** The first three built from a live read of the Figma library, rather than from web code or Zeroheight. **Documented: 63 → 66. Selectable components with no doc: 12 → 9.**

**Figma answered things nothing else in the repo knew.** `Navigation bar (app)` is 49 high on iOS and 80 on Android. The label moves beside the icon on iOS tablet only. Android tablet tabs stop at 168 wide instead of stretching, so a three-tab Android tablet bar does not fill its width.

**`Score tag` wraps a `Tag` that is not exposed.** Its four tiers are fixed in the component, not a convention someone could break. Its colour tokens exist and have zero recorded uses on web — there is no scoring UI built.

**Four rulings of yours are now in the ruleset.** `Feedback thumb buttons` stays selectable and choosing it is the signal to adopt it properly; it is web only. `State Message` is the name. `Navigation bar (app)` is placeable in Figma and never ships on web. The three short variant grids are deliberate.

**`Pop-up` and `Modal bottom sheet` contradict each other three ways**, and it is a backlog row with the evidence, waiting on an investigation.

**One gap opened by the new doc:** no rule sends an agent to `Navigation bar (app)`. Proposed as its own row.

**Components with no rule: 16 → 0.** Every component in the four libraries can now be reached by a rule, or is forbidden on purpose with the reason written down.

**You described five of the last six from memory.** The sixth came out of its Figma properties. Two things are recorded as unknown rather than guessed: the thumb buttons have no usage rules, and the boundary between the two feedback components is read from Figma, not from any rule.

**`Menus` is forbidden provisionally, not permanently.** Your reason is in the ruleset with the date, and a backlog row carries the trigger to revisit — a provisional entry with nothing scheduled to re-open it is just a permanent one that reads as temporary.

**Then: settle whether `Spacing/56` is page rhythm or forbidden.** The ruleset
says both, 83 lines apart — and the donut appears to use 56 internally.

**Then: give grid and breakpoint a ruleset an agent may read.** Half a day, and
it finishes the last thing an agent needs.

**Waiting on you in Figma:** `Donut chart` cannot place its legend below, and its
gap to the chart is fixed at 56 — above the 48 ceiling you set.

#### The question I'd like answered first

> **Still open: the energy filter slider ships on web and was never added to the
> Figma libraries.** An agent asked to build one has working web code to copy
> and no Figma component to place. Get it added, or tell agents to compose it?

Around two dozen questions are waiting, all in
[project/backlog.md](backlog.md). I'll bring them one at a time.


#### How we work

```
   pick the next task
          │
          ▼
   I explain it: what, why, how long  ──►  you approve
          │
          ▼
   I do it
          │
          ▼
   we check it worked
          │
     ┌────┴────┐
    yes        no
     │          │
     ▼          ▼
   log it    sub-task to fix it ──┐
     │                            │
     └────►  next task  ◄─────────┘
```

Two commands drive it:

| Type this | What happens |
| --- | --- |
| **`/task-next`** | I propose one task — what, why, how long, whether it needs you. **I wait for your go.** Then I do it and report in three lines |
| **`/task-check`** | I verify it actually worked and tell you pass or fail. Passed → logged, saved, next task named. Failed → a sub-task to fix it becomes next |

**One task, one chat.** Start each task in a new conversation with `/task-next`.
You never have to watch anything — `/task-check` finishing is the signal. Stay
put within a task, and for conversations like this one.

**Nothing is written without your approval.** Reading and searching are free.
The handful of exceptions — this page, the backlog, and adding a file's name to
a list of files — are written down in one place, `CLAUDE.md`.

##### How the work gets saved

**One task = one commit.** No branches, no pull requests — you work alone, and
the loop already does what a branch was doing.

| When | What happens |
| --- | --- |
| The check **passes** | I commit and push straight away. That's your backup — if the laptop dies, the work exists |
| The check **fails** | Nothing is committed, and I'll tell you the work isn't saved yet |

Committing and pushing only ever **add** — they can't lose anything, which is
why they don't need your approval. The operations that could lose something —
undoing, deleting, rewriting history — **always ask you first, every time.**

**Anything that surfaces mid-task gets written into the backlog, not into the
conversation** — as a task I can do, a question only you can answer, or a note.
That's the rule that stops findings from disappearing into chat.

