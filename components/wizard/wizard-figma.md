_What is true of `Wizard` **in Figma** and nowhere else. Layer names, quirks, and
how to work with the component in the tool._

_**This is not the usage documentation.** [`wizard.md`](wizard.md) describes the component; this page describes the tool. Identity —
keys, node IDs, variant counts, property definitions — lives in
`figma/*-registry.json` and is never restated here._

---

## The lines between steps

If the vertical lines between steps show the wrong colour, select the layer
`Top line` or `Bottom line` and change its variant to `Done`, `To do` or
`Disabled`. On the first and last step one of the two is hidden instead.
