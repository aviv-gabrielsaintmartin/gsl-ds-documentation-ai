_What is true of `Carousel` **in Figma** and nowhere else. Layer names, quirks, and
how to work with the component in the tool._

_**This is not the usage documentation.** [`carousel.md`](carousel.md) describes the component; this page describes the tool. Identity —
keys, node IDs, variant counts, property definitions — lives in
`figma/*-registry.json` and is never restated here._

---

## Simulating a slide position

To show a different slide position, change the item alignment from left to
centre. The component has no variant for it.
